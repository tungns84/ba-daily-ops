---
phase: 03-ba-mermaid-diagram-operator
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - .agents/ba-daily-operators/ba-tools/tests/test_mermaid_trace_index.py
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/mermaid/index_requirements.json
autonomous: true
requirements: [MMD-02]
must_haves:
  truths:
    - "Per D-03: the agent writes req_ids into the .md frontmatter and the full route reads them into the trace write --req-ids hand-off"
    - "Per D-03a: no new ba-tools parser — the existing explicit --req-ids flag on trace write carries the IDs (frontmatter→flag hand-off only)"
    - "Per D-04: the full route runs author → trace write --kind mermaid → index update, populating the INDEX.md mermaid column"
    - "Per D-05 (orphan): cited REQ-IDs are validated DOWNSTREAM by index update — a real-ID diagram introduces no orphans; trace write records what it is given without validation"
  prohibitions:
    - "Per D-05 (orphan)/CONTEXT: no change to trace_cmd.py or index_cmd.py — they are reused as-is; this plan only adds a test + fixture"
  artifacts:
    - path: ".agents/ba-daily-operators/ba-tools/tests/test_mermaid_trace_index.py"
      provides: "MMD-02 + criterion-2 integration test (req_ids → INDEX mermaid column, no orphans; orphan surfaced for an invented ID)"
      contains: "index update"
    - path: ".agents/ba-daily-operators/ba-tools/tests/fixtures/mermaid/index_requirements.json"
      provides: "fixture requirements.json with real REQ-IDs for the trace+index integration test"
      contains: "FR-001"
  key_links:
    - from: "ba-tools trace write --kind mermaid"
      to: "ba-tools index update → INDEX.md mermaid column"
      via: "trace record consumed by index update; cited REQ-IDs appear in the mermaid column"
      pattern: "mermaid"
---

<objective>
Add the end-to-end integration test that proves MMD-02 and ROADMAP success criterion 2: after a
diagram's `req_ids` are written to frontmatter, the `full` route's `trace write --kind mermaid`
records them and `index update` surfaces those REQ-IDs in the INDEX.md mermaid column with NO
orphans introduced — while an invented (unknown) REQ-ID is correctly surfaced in the INDEX Orphans
section (D-05 downstream validation).

Purpose: The traceability spine is the core project value — a mermaid diagram's REQ-IDs must show
up in INDEX.md under the mermaid column. This plan does not modify `trace_cmd.py` or `index_cmd.py`
(reused as-is per CONTEXT); it CONSUMES them and the workflow/command from Plans 03-01 and 03-02 to
prove the wiring end-to-end.

Output: one integration test module + a requirements.json fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ba-mermaid-diagram-operator/03-CONTEXT.md
@.planning/phases/03-ba-mermaid-diagram-operator/03-RESEARCH.md
@.planning/phases/03-ba-mermaid-diagram-operator/03-PATTERNS.md
@.agents/ba-daily-operators/ba-tools/tests/test_trace.py
@.agents/ba-daily-operators/ba-tools/tests/test_index.py
@.agents/ba-daily-operators/ba-tools/ba_tools/commands/trace_cmd.py
@.agents/ba-daily-operators/ba-tools/ba_tools/commands/index_cmd.py
</context>

<artifacts_this_phase_produces>
**Plan 03-03 (this plan):**
- New integration test: `tests/test_mermaid_trace_index.py`
- New fixture: `tests/fixtures/mermaid/index_requirements.json`

**Cross-reference (consumed, not produced here):**
- Plan 03-01: `mermaid-render` command (not exercised by this test — this test covers the author→trace→index path).
- Plan 03-02: `ba-mermaid.md` workflow `full` route (the trace write + index update invocation this test mirrors with real CLI calls) and the `req_ids` frontmatter convention.
- Reused as-is (no edit): `trace write --kind mermaid`, `index update` (Phase-2 D-04/D-05/D-13 contract).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration test — req_ids → INDEX mermaid column, no orphans (criterion 2)</name>
  <read_first>
    - .agents/ba-daily-operators/ba-tools/tests/test_trace.py (analog — trace write CLI invocation via subprocess, --kind/--req-ids/--artifact/--source-doc usage, repo-root fixture)
    - .agents/ba-daily-operators/ba-tools/tests/test_index.py (analog — index update invocation + INDEX.md assertions, mermaid column, Orphans section parsing)
    - .agents/ba-daily-operators/ba-tools/ba_tools/commands/trace_cmd.py (CONSUME — confirm exact flag spellings: --kind, --slug, --artifact, --source-doc, --requirements, --req-ids)
    - .agents/ba-daily-operators/ba-tools/ba_tools/commands/index_cmd.py (CONSUME — confirm mermaid column header + Orphans section markers in INDEX.md)
    - .planning/phases/03-ba-mermaid-diagram-operator/03-RESEARCH.md (Validation Architecture — criterion 2 row; Pattern 3 trace write call shape)
    - .planning/phases/03-ba-mermaid-diagram-operator/03-PATTERNS.md (test module patterns; reuse-as-is table)
  </read_first>
  <files>
    .agents/ba-daily-operators/ba-tools/tests/test_mermaid_trace_index.py,
    .agents/ba-daily-operators/ba-tools/tests/fixtures/mermaid/index_requirements.json
  </files>
  <behavior>
    - test_req_ids_appear_in_index_mermaid_column: given a real `requirements.json` with FR-001 and FR-002 and a mermaid diagram `.md` citing `req_ids: [FR-001]`, after `trace write --kind mermaid` + `index update`, INDEX.md shows the slug's mermaid cell as non-empty (lists FR-001) and the Orphans section does NOT list FR-001.
    - test_no_orphans_for_real_ids: with all cited IDs present in requirements.json, the INDEX Orphans section is empty (no orphan introduced — criterion 2 "no orphans introduced").
    - test_invented_id_surfaces_as_orphan: when the mermaid trace cites an ID absent from requirements.json (e.g. FR-999), `index update` lists FR-999 under the Orphans section (D-05 downstream validation — the slip is surfaced, not swallowed).
  </behavior>
  <action>
    Create `tests/fixtures/mermaid/index_requirements.json` shaped like a Phase-2 SRS `requirements.json` (mirror the structure used by `test_index.py`/`test_trace.py` fixtures) containing at least FR-001 and FR-002 with their statement text. Create `tests/test_mermaid_trace_index.py` mirroring `test_trace.py`/`test_index.py`: build a tmp repo with `.ba-ops/srs/<slug>/requirements.json` (copied from the fixture) and `.ba-ops/mermaid/<slug>/diagram.md` (frontmatter `req_ids: [FR-001]` + an inline mermaid fence). Drive the real CLI via subprocess `[sys.executable, "-m", "ba_tools", ...]`: run `trace write --kind mermaid --slug <slug> --artifact .ba-ops/mermaid/<slug>/diagram.md --source-doc .ba-ops/srs/<slug>/requirements.json --requirements .ba-ops/srs/<slug>/requirements.json --req-ids FR-001`, then `index update`. Parse the generated INDEX.md and assert the slug's mermaid column cell contains FR-001 and the Orphans section does not. Add the no-orphans assertion (Orphans section empty for all-real IDs) and the invented-ID test (re-run trace write with `--req-ids FR-001,FR-999`, index update, assert FR-999 appears under Orphans). Use the exact column header / Orphans markers found by reading `index_cmd.py` and `test_index.py` — do NOT guess the strings. This test only CONSUMES `trace write`/`index update`; make NO edit to `trace_cmd.py` or `index_cmd.py`.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest tests/test_mermaid_trace_index.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All three tests in `tests/test_mermaid_trace_index.py` pass.
    - After `trace write --kind mermaid --req-ids FR-001` + `index update`, INDEX.md's mermaid column for the slug lists FR-001 and the Orphans section does not list FR-001 (criterion 2; MMD-02).
    - With an invented ID (FR-999) cited, `index update` lists FR-999 under the Orphans section (D-05 downstream validation).
    - `git status` shows `trace_cmd.py` and `index_cmd.py` unmodified (reused as-is).
    - The test uses the literal column/Orphans markers read from `index_cmd.py`, not hard-coded guesses.
  </acceptance_criteria>
  <done>MMD-02 / criterion 2 are provable end-to-end: a real-ID mermaid diagram's req_ids reach the INDEX mermaid column with no orphans, and an invented ID is surfaced as an orphan — all without touching the Phase-2 trace/index code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--req-ids` (from frontmatter) → `trace write` | The explicit ID list crosses from the agent-authored frontmatter into the trace record |
| trace record → `index update` → INDEX.md | Cited IDs are reconciled against requirements.json; unknown IDs are flagged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Spoofing | a diagram cites a REQ-ID not in the registry | mitigate | `index update` (Phase-2 D-05) flags it in the INDEX Orphans section — test `test_invented_id_surfaces_as_orphan` proves the slip is surfaced, not silently swallowed |
| T-03-10 | Repudiation | trace record loses which SRS state was depicted | mitigate | `trace write` records `source_hash` of `--source-doc` (= requirements.json), giving a tamper-evident link to the depicted SRS state (Phase-2 D-06) — exercised by the integration test |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed this plan (RESEARCH Package Legitimacy Audit: none); test invokes only existing ba-tools subcommands via `sys.executable -m ba_tools` |
</threat_model>

<verification>
- `test_mermaid_trace_index.py` green: req_ids → INDEX mermaid column, no orphans for real IDs, orphan surfaced for invented ID.
- Full ba-tools suite stays green: `python -m pytest .agents/ba-daily-operators/ba-tools/tests/ -q`.
- `git status` confirms `trace_cmd.py` and `index_cmd.py` untouched.
</verification>

<success_criteria>
- ROADMAP criterion 2 met: after `trace write --kind mermaid` + `index update`, cited REQ-IDs appear in the INDEX mermaid column with no orphans introduced.
- MMD-02 satisfied: each diagram cites `req_ids`; those IDs reach the traceability matrix.
- Phase-2 trace/index machinery reused with zero modification.
</success_criteria>

<output>
Create `.planning/phases/03-ba-mermaid-diagram-operator/03-03-SUMMARY.md` when done.
</output>
