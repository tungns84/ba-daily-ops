---
phase: 03-ba-mermaid-diagram-operator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/ba-mermaid/SKILL.md
  - .agents/skills/ba-mermaid/agents/openai.yaml
  - .agents/ba-daily-operators/ba-core/workflows/ba-mermaid.md
  - .agents/ba-daily-operators/ba-core/agents/ba-diagrammer.md
  - .agents/ba-daily-operators/ba-tools/tests/test_mermaid_author.py
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/mermaid/authored_diagram.md
autonomous: true
requirements: [MMD-01]
must_haves:
  truths:
    - "Per D-01: ba-mermaid input is an existing SRS --slug; the agent reads .ba-ops/srs/<slug>/requirements.json and artifacts land under .ba-ops/mermaid/<slug>/"
    - "Per D-01a: REQ-ID subset selection is agent judgement — the ba-diagrammer prompt instructs the agent to pick the depicted subset and never invent IDs"
    - "Per D-02: one diagram per invocation; the agent chooses the fitting Mermaid type (flowchart/sequence/state/ER/class)"
    - "Per D-02a: optional --diagram-type override is honored when present in the payload"
    - "Per D-03: the agent writes depicted REQ-IDs into the .md YAML frontmatter (req_ids: [...]) — human-visible and machine-readable"
    - "Per D-04: the author route writes the inline ```mermaid .md artifact only; full = author → trace write → index update; render = export-only from an existing .md"
  prohibitions:
    - "Per D-04: the author route invokes NO CLI and writes NO trace (pure authoring); full does NOT run render; render is never auto-invoked by author/full"
    - "Per D-05cmd: the ba-diagrammer agent NEVER shells out to mmdc or any render CLI — only ba-tools renders (determinism boundary)"
  artifacts:
    - path: ".agents/skills/ba-mermaid/SKILL.md"
      provides: "Codex skill discovery index (name + description only)"
      contains: "name: ba-mermaid"
    - path: ".agents/skills/ba-mermaid/agents/openai.yaml"
      provides: "Codex skill config: interface.* + policy.allow_implicit_invocation: false"
      contains: "allow_implicit_invocation"
    - path: ".agents/ba-daily-operators/ba-core/workflows/ba-mermaid.md"
      provides: "thin per-route workflow (author/render/full) with frontmatter→--req-ids hand-off"
      contains: "Route: author"
    - path: ".agents/ba-daily-operators/ba-core/agents/ba-diagrammer.md"
      provides: "diagram-author agent role contract (type selection + req_ids discipline)"
      contains: "ba-diagrammer"
  key_links:
    - from: ".agents/ba-daily-operators/ba-core/workflows/ba-mermaid.md"
      to: ".agents/ba-daily-operators/ba-core/agents/ba-diagrammer.md"
      via: "author route step opens the ba-diagrammer role contract"
      pattern: "ba-diagrammer"
    - from: ".agents/ba-daily-operators/ba-core/workflows/ba-mermaid.md"
      to: "ba-tools trace write --kind mermaid"
      via: "full route Step 2 reads req_ids frontmatter → --req-ids flag (D-03/D-03a)"
      pattern: "trace write"
---

<objective>
Create the `ba-mermaid` Codex skill, thin workflow, and diagram-author agent prompt — copying the
Phase-2 `ba-srs-analyze` layout exactly. The skill exposes three routes: `author` (default — write
the inline ```mermaid `.md` only, no CLI, no trace), `full` (author → `trace write` → `index
update`, no render), and `render` (opt-in `mmdc` export from an existing `.md`). Implements MMD-01
and ROADMAP success criterion 1 (author route produces an inline ```mermaid block with no Mermaid
CLI invoked).

Purpose: This is the agent-owned half of the operator. The determinism boundary holds — the agent
judges diagram type and REQ-ID subset and authors the `.md`; `ba-tools` does only file/hash/CLI
work. The workflow performs the frontmatter→`--req-ids` hand-off (D-03/D-03a) using the existing
`trace write` flag — no new parser.

Output: 4 new skill/workflow/agent files + an author-route test + a fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ba-mermaid-diagram-operator/03-CONTEXT.md
@.planning/phases/03-ba-mermaid-diagram-operator/03-RESEARCH.md
@.planning/phases/03-ba-mermaid-diagram-operator/03-PATTERNS.md
@.agents/skills/ba-srs-analyze/SKILL.md
@.agents/skills/ba-srs-analyze/agents/openai.yaml
@.agents/ba-daily-operators/ba-core/workflows/ba-srs-analyze.md
@.agents/ba-daily-operators/ba-core/agents/ba-srs-writer.md
</context>

<artifacts_this_phase_produces>
**Plan 03-02 (this plan):**
- New skill index: `.agents/skills/ba-mermaid/SKILL.md` (frontmatter `name: ba-mermaid` + `description`)
- New skill config: `.agents/skills/ba-mermaid/agents/openai.yaml` (`interface.display_name`, `interface.short_description`, `interface.default_prompt`, `policy.allow_implicit_invocation: false`)
- New workflow: `.agents/ba-daily-operators/ba-core/workflows/ba-mermaid.md` (frontmatter `operator: ba-mermaid`, `default_route: author`, `routes: [author, render, full]`; route sections `author`/`full`/`render`)
- New agent prompt: `.agents/ba-daily-operators/ba-core/agents/ba-diagrammer.md` (role contract; output schema with `req_ids`/`diagram_type`/`slug` frontmatter + inline ```mermaid block)
- New test + fixture: `test_mermaid_author.py`, `tests/fixtures/mermaid/authored_diagram.md`

**Cross-reference (not produced here):** Plan 03-01 produces `mermaid-render` + `mermaid_render_cmd.py`. The `render` route in this workflow CONSUMES that command (it must already exist for the render route to function — see depends/wave).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the ba-mermaid skill index + openai.yaml</name>
  <read_first>
    - .agents/skills/ba-srs-analyze/SKILL.md (analog — exact frontmatter; name + description ONLY)
    - .agents/skills/ba-srs-analyze/agents/openai.yaml (analog — interface.* + policy.allow_implicit_invocation nesting)
    - .planning/phases/03-ba-mermaid-diagram-operator/03-PATTERNS.md (SKILL.md + openai.yaml sections — complete copy-patterns with the ba-mermaid wording)
    - CLAUDE.md (Codex skill contract: SKILL.md frontmatter name+description only; openai.yaml nesting; byte budget < 32,768 B)
  </read_first>
  <files>
    .agents/skills/ba-mermaid/SKILL.md,
    .agents/skills/ba-mermaid/agents/openai.yaml
  </files>
  <action>
    Create `.agents/skills/ba-mermaid/SKILL.md` with YAML frontmatter containing ONLY `name: ba-mermaid` and a keyword-dense `description` (turn a UC/requirement into a Mermaid diagram authored inline as a fenced mermaid block in a .md artifact with YAML frontmatter req_ids; routes author|render|full default author; no Mermaid CLI on author; render hard-fails exit 2 when mmdc absent; trigger phrases like draw diagram / create diagram / mermaid diagram / sequence diagram / flowchart / state diagram / $ba-mermaid). Add NO other frontmatter keys (Codex enforces name+description only). Body is a one-line HTML comment pointing at the workflow file. Create `.agents/skills/ba-mermaid/agents/openai.yaml` mirroring the ba-srs-analyze nesting: `interface.display_name` ("BA Mermaid"), `interface.short_description` (UC/requirement → Mermaid diagram .md with req_ids frontmatter; traceability via trace write + index update), `interface.default_prompt` (multi-line: instruct to open the ba-mermaid workflow and follow the author route — confirm route via `ba-tools resolve-route ba-mermaid`, init via `ba-tools init ba-mermaid`, read `.ba-ops/srs/<slug>/requirements.json`, follow `ba-diagrammer.md`, use route `full` for traceability), and `policy.allow_implicit_invocation: false`. Keep both files well under 32,768 B.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest tests/test_skill_schema.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.agents/skills/ba-mermaid/SKILL.md` frontmatter contains exactly the keys `name` and `description` (no others); `name` is `ba-mermaid`.
    - `.agents/skills/ba-mermaid/agents/openai.yaml` contains `interface.display_name`, `interface.short_description`, `interface.default_prompt`, and `policy.allow_implicit_invocation: false` (note: nested under `policy:`, not flat).
    - `test_skill_schema.py` passes (it validates the flat-skill frontmatter + openai.yaml contract across operators including ba-mermaid).
    - Each file is < 32,768 bytes.
  </acceptance_criteria>
  <done>The `ba-mermaid` skill is discoverable by Codex with valid frontmatter + openai.yaml; schema test green.</done>
</task>

<task type="auto">
  <name>Task 2: Create the ba-mermaid thin workflow + ba-diagrammer agent prompt</name>
  <read_first>
    - .agents/ba-daily-operators/ba-core/workflows/ba-srs-analyze.md (analog — frontmatter, determinism-boundary block, per-route Steps sections)
    - .agents/ba-daily-operators/ba-core/agents/ba-srs-writer.md (analog — role header, paths-only inputs, output schema, discipline rules)
    - .planning/phases/03-ba-mermaid-diagram-operator/03-PATTERNS.md (workflow + ba-diagrammer sections — full copy-patterns incl. the trace write invocation with --source-doc)
    - .planning/phases/03-ba-mermaid-diagram-operator/03-RESEARCH.md (Pattern 3 — trace write --source-doc = requirements.json reasoning; Open Question 1 frontmatter parsing recommendation)
  </read_first>
  <files>
    .agents/ba-daily-operators/ba-core/workflows/ba-mermaid.md,
    .agents/ba-daily-operators/ba-core/agents/ba-diagrammer.md
  </files>
  <action>
    Create `.agents/ba-daily-operators/ba-core/workflows/ba-mermaid.md` with frontmatter `operator: ba-mermaid`, `default_route: author`, `routes: [author, render, full]`. Include a determinism-boundary block (CLI = file/command/hash-provable only; agent owns type + subset selection + authoring) and a "pass paths, not content" note. Add three `## Route:` sections. `author`: write the inline ```mermaid `.md` only, NO CLI, NO trace — steps run `ba-tools resolve-route ba-mermaid` (confirm `author`), `ba-tools init ba-mermaid`, open `ba-diagrammer.md`, pass a paths-only payload (`requirements_json`, `slug`, optional `diagram_type`, `route`), diagrammer writes `.ba-ops/mermaid/<slug>/diagram.md`. `full`: Step 1 = author steps; Step 2 = read the `req_ids:` list from the diagram `.md` YAML frontmatter and run `ba-tools trace write --kind mermaid --slug <slug> --artifact .ba-ops/mermaid/<slug>/diagram.md --source-doc .ba-ops/srs/<slug>/requirements.json --requirements .ba-ops/srs/<slug>/requirements.json --req-ids <comma-separated ids from frontmatter>` (note: `--source-doc` is the SRS requirements.json so source_hash tracks the depicted SRS state per Phase-2 D-06, NOT the diagram .md); Step 3 = `ba-tools index update`. `render`: opt-in export — confirm `diagram.md` exists, run `ba-tools mermaid-render --slug <slug> --artifact .ba-ops/mermaid/<slug>/diagram.md --format svg`; on exit 2 NO_MERMAID_CLI instruct to install mmdc and retry, and explicitly state: do not generate a synthetic image. State that render is never auto-invoked by author or full. Then create `.agents/ba-daily-operators/ba-core/agents/ba-diagrammer.md` with: role header + determinism-boundary (you author and judge; you NEVER shell out to mmdc or any CLI; you READ source files and WRITE Markdown); a paths-only Inputs block (`requirements_json`, `slug`, optional `diagram_type`, `route`); an Output section specifying the `.ba-ops/mermaid/<slug>/diagram.md` schema (YAML frontmatter `req_ids: [...]`, `diagram_type`, `slug`, then a `# Title` and one inline ```mermaid block); a diagram-type selection table (flowchart default / sequenceDiagram / stateDiagram-v2 / erDiagram / classDiagram) honoring `--diagram-type` override when present; and a req_ids discipline section (read all IDs from requirements.json, depict only the focused subset, NEVER invent IDs — unknown IDs surface as orphans via index update). Keep both files < 32,768 B.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest tests/test_workflow_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ba-mermaid.md` frontmatter has `operator: ba-mermaid`, `default_route: author`, and `routes` listing `author`, `render`, `full`.
    - `ba-mermaid.md` contains three `## Route:` headings (`author`, `full`, `render`); the `author` section contains no `trace write` and no `mermaid-render` invocation; the `full` section contains `trace write --kind mermaid` and `index update` but no `mermaid-render`; the `render` section contains `mermaid-render`.
    - The `full` route's `trace write` call passes `--source-doc .ba-ops/srs/<slug>/requirements.json` (not the diagram .md).
    - `ba-diagrammer.md` contains a diagram-type table with at least flowchart/sequenceDiagram/stateDiagram-v2/erDiagram/classDiagram and a req_ids-discipline rule forbidding invented IDs.
    - `test_workflow_contract.py` passes (validates operator workflows incl. route table consistency with resolve-route/init).
    - Both files < 32,768 bytes.
  </acceptance_criteria>
  <done>The thin workflow resolves route→steps for author/full/render; the ba-diagrammer prompt encodes type selection + req_ids discipline; workflow-contract test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author-route no-CLI proof test (criterion 1)</name>
  <read_first>
    - .agents/ba-daily-operators/ba-tools/tests/test_render.py (analog — subprocess CLI test shape)
    - .planning/phases/03-ba-mermaid-diagram-operator/03-RESEARCH.md (Validation Architecture — criterion 1 row: assert inline mermaid fence present, assert subprocess.run NOT called / no mmdc spawned)
    - .planning/phases/03-ba-mermaid-diagram-operator/03-PATTERNS.md (test module patterns)
  </read_first>
  <files>
    .agents/ba-daily-operators/ba-tools/tests/test_mermaid_author.py,
    .agents/ba-daily-operators/ba-tools/tests/fixtures/mermaid/authored_diagram.md
  </files>
  <behavior>
    - test_author_artifact_has_inline_fence: an authored diagram `.md` (the fixture, representing what the ba-diagrammer agent produces on the author route) contains a ```mermaid opening fence and a matching closing fence, plus YAML frontmatter with a non-empty `req_ids` list.
    - test_author_route_invokes_no_render_cli: parsing `ba-mermaid.md`, the `author` route section contains neither a `mermaid-render` invocation nor an `mmdc` call (criterion 1 — no Mermaid CLI on the author route).
  </behavior>
  <action>
    Create `tests/fixtures/mermaid/authored_diagram.md` representing an author-route output: YAML frontmatter with `req_ids: [FR-001, FR-002]`, `diagram_type: flowchart`, `slug: test-slug`, a `# Title` line, and one inline ```mermaid fence with a minimal flowchart body. Create `tests/test_mermaid_author.py` with the two tests in `<behavior>`. For `test_author_artifact_has_inline_fence`, read the fixture and assert the opening/closing fence markers and a parsed non-empty `req_ids` list (simple line scan over frontmatter — no YAML lib needed). For `test_author_route_invokes_no_render_cli`, read `.agents/ba-daily-operators/ba-core/workflows/ba-mermaid.md`, slice the text between the `## Route: author` heading and the next `## Route:` heading, and assert that slice contains neither `mermaid-render` nor `mmdc`. This proves the author route is CLI-free (criterion 1). Do NOT add a head-comment containing the literal `mermaid-render` that a region grep would falsely match; the substring checks inside the test body are the legitimate use.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest tests/test_mermaid_author.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Both tests in `tests/test_mermaid_author.py` pass.
    - The `## Route: author` slice of `ba-mermaid.md` contains no `mermaid-render` and no `mmdc` token — the author route invokes no Mermaid CLI (ROADMAP criterion 1).
    - The fixture `authored_diagram.md` contains a literal ```mermaid fence and a non-empty `req_ids` frontmatter list.
  </acceptance_criteria>
  <done>Criterion 1 is provable by test: author-route artifacts carry an inline mermaid fence + req_ids, and the author route runs no render CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → diagram `.md` artifact | The ba-diagrammer agent writes untrusted-content frontmatter (`req_ids`) + a mermaid fence into a `.md` file |
| workflow → `ba-tools trace write` | The `full` route extracts `req_ids` from frontmatter and forwards as `--req-ids` (explicit list) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Spoofing | agent invents REQ-IDs not in requirements.json | mitigate | ba-diagrammer req_ids-discipline forbids invented IDs; any slip is surfaced (not swallowed) as an orphan by `index update` (D-05) — validated downstream in Plan 03-03 |
| T-03-07 | Information Disclosure | author route accidentally shelling out to mmdc (CLI dependency on default path) | mitigate | Author route section contains no CLI invocation; `test_author_route_invokes_no_render_cli` asserts the section is mermaid-render/mmdc-free (criterion 1) |
| T-03-08 | Tampering | malformed `req_ids` frontmatter parsed into the `--req-ids` hand-off | accept | The agent reads the list it wrote; `trace write` validates kind/slug regex and records the explicit list; downstream `index update` flags any orphan. Low risk — single-author, no untrusted external supplier |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed this plan (RESEARCH Package Legitimacy Audit: none); the workflow references `npx -p @mermaid-js/mermaid-cli mmdc` only as the render-route resolution form, never an install step |
</threat_model>

<verification>
- `test_skill_schema.py`, `test_workflow_contract.py`, `test_mermaid_author.py` all green.
- The `## Route: author` section of `ba-mermaid.md` contains no `mermaid-render`/`mmdc` token (criterion 1).
- All four new agent/skill/workflow files are < 32,768 bytes.
</verification>

<success_criteria>
- ROADMAP criterion 1 met: author route produces an inline ```mermaid block in a `.md` with no Mermaid CLI invoked.
- MMD-01 satisfied: UC/requirement → Mermaid diagram, MD-inline first.
- Skill/workflow/agent layout mirrors ba-srs-analyze and passes the skill + workflow contract tests.
</success_criteria>

<output>
Create `.planning/phases/03-ba-mermaid-diagram-operator/03-02-SUMMARY.md` when done.
</output>
