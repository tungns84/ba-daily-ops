---
phase: BAOPS-01-harness-foundation
plan: 10
type: execute
wave: 8
depends_on:
  - "01-09"
files_modified: []
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - NFR-06
  - NFR-03
  - NFR-04
  - NFR-05
must_haves:
  truths:
    - "A GitHub Actions run for the exact Phase 1 commit completed successfully for the single current-scope job: windows-latest + Python 3.14."
    - "The successful job ran Ruff, the complete non-online suite, and the offline generated-launcher smoke rather than only validating workflow syntax."
    - "The Windows evidence records a Windows PowerShell Desktop 5.1-or-newer host and cannot be satisfied by a `pwsh`-only job."
    - "No CI-green claim exists when a run is absent, pending, cancelled, skipped, stale, or attached to a different commit."
    - "Multi-OS, Python 3.11, and six-job matrix success are explicitly NOT required for this plan — deferred to Phase 7 (NFR-02)."
  artifacts:
    - path: ".planning/phases/BAOPS-01-harness-foundation/01-10-SUMMARY.md"
      provides: "Foundation CI Run Evidence with exact commit, run URL, single job identity, conclusion, and required-step evidence"
      contains: "Foundation CI Run Evidence"
  key_links:
    - from: ".github/workflows/foundation.yml"
      to: ".planning/phases/BAOPS-01-harness-foundation/01-10-SUMMARY.md"
      via: "GitHub run metadata and logs prove the declared Windows/Python 3.14 job actually executed and passed"
      pattern: "headSha|jobs|conclusion|url"
  prohibitions:
    - statement: "Workflow source existence or local pytest success MUST NOT be reported as a successful remote CI matrix."
      status: resolved
      verification: judgment
    - statement: "Six-job or multi-OS CI success MUST NOT be inferred from this one-job evidence plan."
      status: resolved
      verification: judgment
---

## Phase Goal

**As a** Business Analyst, **I want to** bootstrap a healthy harness workspace on Windows 10+ with CPython 3.14, **so that** I can use the deterministic workflow on the current supported platform.

<objective>
Obtain honest remote execution evidence for the **current-scope** foundation CI job: one `windows-latest` matrix entry with Python 3.14.

Purpose: Separate workflow authoring from proof that the hosted Windows job actually ran the complete locked offline path for NFR-06.
Output: A blocking summary record tied to one exact commit and one successful one-job run.
</objective>

<execution_context>
@C:/Users/TungNS/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/TungNS/.cursor/gsd-core/templates/summary.md
@C:/Users/TungNS/.cursor/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/BAOPS-01-harness-foundation/01-SCOPE.md
@.planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md
@.planning/phases/BAOPS-01-harness-foundation/01-09-SUMMARY.md
@.github/workflows/foundation.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the actual Windows/Python 3.14 foundation run</name>
  <files>None — inspect GitHub Actions and record evidence in 01-10-SUMMARY.md</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-SCOPE.md, .planning/phases/BAOPS-01-harness-foundation/01-09-SUMMARY.md, .github/workflows/foundation.yml, .planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md</read_first>
  <action>After the exact Phase 1 commit is available to GitHub Actions, identify the matching `foundation` run with `gh run list` and inspect it with `gh run view`. Record run URL/ID, event, branch, `headSha`, workflow file hash, start/completion timestamps, overall conclusion, and the single required matrix job name/conclusion (windows-latest + Python 3.14). For that job, verify logs show locked development-environment creation, Ruff, the full non-online pytest suite, wheelhouse setup, network-denied offline `smoke_foundation.py`, generated launcher use, and evidence upload. Verify the recorded host is `PSEdition=Desktop` with PowerShell version at least 5.1 and that the installer ran through `powershell.exe`; a successful `pwsh` compatibility step is supplementary only. Confirm the run commit equals the commit containing the workflow and all Phase 1 source. If the job is missing, pending, cancelled, skipped, stale, or unsuccessful, keep the checkpoint blocked and record the exact job/step failure. Do NOT require macOS, Linux, or Python 3.11 jobs — those are deferred per 01-SCOPE.md.</action>
  <verify>
    <automated>gh run view FOUNDATION_RUN_ID --json headSha,conclusion,event,jobs,url,startedTime,updatedAt</automated>
    <human-check>Confirm the returned run matches the exact Phase 1 commit and the single Windows/Python 3.14 job concluded success after running the required lint, full-suite, and offline-smoke steps.</human-check>
  </verify>
  <acceptance_criteria>
    - One run URL and exact `headSha` are recorded and match the phase commit.
    - Exactly one required job (windows-latest + Python 3.14) is present and concludes `success`.
    - The job has evidence for Ruff, full non-online suite, and offline generated-launcher smoke.
    - Windows evidence identifies Windows PowerShell Desktop 5.1 or newer and uses `powershell.exe`.
    - The workflow's explicit read-only permission test passed in the run.
    - Missing, skipped, cancelled, stale, or failing evidence prevents approval.
    - Summary explicitly states multi-OS/six-job matrix is deferred (NFR-02 / Phase 7).
  </acceptance_criteria>
  <done>The exact Phase 1 commit has one successful ordinary Windows/Python 3.14 foundation run, and complete redacted evidence is recorded in 01-10-SUMMARY.md.</done>
  <what-built>A statically verified least-privilege workflow and complete local suite from Plan 01-09; this checkpoint verifies its real hosted execution for current scope only.</what-built>
  <how-to-verify>Open the recorded run URL, compare `headSha`, inspect the single job conclusion and required steps, and confirm the Windows PowerShell 5.1 host record.</how-to-verify>
  <resume-signal>Type "approved" with the run URL/ID, or describe the missing/failing job evidence.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/BAOPS-01-harness-foundation/01-10-SUMMARY.md` — section `Foundation CI Run Evidence` with run ID/URL, event, branch, exact head SHA, workflow hash, timestamps, overall conclusion, single job name/conclusion, required-step evidence, Windows PowerShell edition/version, deferred-matrix note, and approval status.
- No product source, workflow source, class, function, flag, field, schema, generated runtime path, or test function is created by this evidence-only plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub run metadata/logs → project compatibility claim | Remote identities and conclusions become release evidence for NFR-06 only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-BAOPS-01-10-EVIDENCE | Spoofing / Repudiation | CI run evidence | high | mitigate | Bind URL, head SHA, workflow hash, named job, required steps, and conclusion; reject stale or incomplete runs; do not over-claim multi-OS scope. |
| T-BAOPS-01-10-LEAK | Information Disclosure | CI logs/summary | medium | mitigate | Record normalized job/step identities and hashes without secrets, absolute runner paths, or environment dumps. |
| T-BAOPS-01-SC | Tampering | CI dependency setup | high | mitigate | Verify logs use approved exact locks, hash/binary enforcement, and local install without dependency resolution. |
</threat_model>

<verification>
- [ ] `gh run view` returns the exact commit and successful overall conclusion.
- [ ] The single Windows/Python 3.14 job/step record satisfies the acceptance criteria.
- [ ] Summary redaction contains no secret or absolute runner path.
- [ ] Summary documents deferred multi-OS matrix (NFR-02).
</verification>

<success_criteria>
- Remote CI success for current scope is evidenced, not inferred from YAML or local tests.
- Hosted Windows + Python 3.14 compatibility is proven for NFR-06.
- Exact minimum-OS and multi-OS claims remain reserved for Plan 01-11 handoff / Phase 7.
</success_criteria>

<output>
Create `.planning/phases/BAOPS-01-harness-foundation/01-10-SUMMARY.md` when done.
</output>
