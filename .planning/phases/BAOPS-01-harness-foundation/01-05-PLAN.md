---
phase: BAOPS-01-harness-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - packages/ba-tools/src/ba_tools/paths.py
  - packages/ba-tools/src/ba_tools/state/atomic.py
  - packages/ba-tools/src/ba_tools/state/locking.py
  - packages/ba-tools/tests/security/test_path_containment.py
  - packages/ba-tools/tests/reliability/test_atomic_write.py
  - packages/ba-tools/tests/reliability/test_locking.py
autonomous: true
requirements:
  - FOUND-05
  - FOUND-06
  - NFR-04
must_haves:
  truths:
    - "[EDGE FOUND-05/empty] Null, empty, dot-only, or missing business paths are rejected before candidate filesystem access."
    - "[EDGE FOUND-05/encoding] Accepted paths are UTF-8 POSIX-relative strings; absolute, drive, UNC, backslash, NUL, traversal, symlink, and reparse escapes are rejected without lossy normalization."
    - "[EDGE FOUND-05/concurrency] Concurrent operations reuse resolved-root containment and reparse checks; no race authorizes an outside-root target."
    - "[EDGE FOUND-06/idempotency] Repeated publication yields complete canonical bytes and never promotes or leaks an abandoned temporary file."
    - "[EDGE FOUND-06/concurrency] One bounded native workspace lock serializes writers; timeout, interruption, crash, and injected faults preserve the prior complete canonical file."
  artifacts:
    - path: "packages/ba-tools/src/ba_tools/paths.py"
      provides: "Strict canonical path grammar, resolved ancestry, and symlink/reparse rejection"
      contains: "def resolve_business_path("
    - path: "packages/ba-tools/src/ba_tools/state/atomic.py"
      provides: "Distinct atomic create, replace, and abandoned-temp quarantine operations"
      contains: "def atomic_replace("
    - path: "packages/ba-tools/src/ba_tools/state/locking.py"
      provides: "Native lock owner metadata and fail-closed stale-owner recovery"
      contains: "def recover_stale_owner_metadata("
    - path: "packages/ba-tools/tests/reliability/test_atomic_write.py"
      provides: "Durability-boundary fault and quarantine evidence"
      contains: "test_fault_preserves_previous_canonical"
  key_links:
    - from: "packages/ba-tools/src/ba_tools/state/locking.py"
      to: ".ba-ops.lock.owner.json"
      via: "owner metadata is written only after native acquisition and interpreted fail-closed"
      pattern: "recover_stale_owner_metadata"
    - from: "packages/ba-tools/src/ba_tools/state/atomic.py"
      to: ".ba-ops/quarantine/atomic"
      via: "recognizable abandoned same-directory temps are moved under the workspace lock and never read as canonical"
      pattern: "quarantine_abandoned_temps"
    - from: "packages/ba-tools/src/ba_tools/paths.py"
      to: "packages/ba-tools/src/ba_tools/state/atomic.py"
      via: "storage functions accept validated path capabilities rather than unchecked strings"
      pattern: "ResolvedBusinessPath"
  prohibitions:
    - statement: "Recovery MUST NOT delete a native lock as stale based on age, promote an abandoned temporary file, or replace an existing file through create-only publication."
      status: resolved
      verification: test
---

## Phase Goal

**As a** Business Analyst, **I want to** bootstrap a portable, healthy harness workspace, **so that** I can use the same deterministic workflow on every supported operating system.

<objective>
Complete the filesystem reliability boundary independently of schema/state-transition logic.

Purpose: Implement D-16 through D-19 and full FOUND-05/FOUND-06 evidence for containment, native ownership, create/replace publication, faults, and quarantine.
Output: Hardened path, lock, and atomic modules plus adversarial, multiprocessing, and fault-injection tests.
</objective>

<execution_context>
@C:/Users/TungNS/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/TungNS/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md
@.planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md
@.planning/phases/BAOPS-01-harness-foundation/01-PATTERNS.md
@.planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md
@.planning/phases/BAOPS-01-harness-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED containment, lock-owner, and durability matrices</name>
  <files>packages/ba-tools/tests/security/test_path_containment.py, packages/ba-tools/tests/reliability/test_atomic_write.py, packages/ba-tools/tests/reliability/test_locking.py</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md, .planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md, packages/ba-tools/tests/conftest.py, packages/ba-tools/src/ba_tools/paths.py, packages/ba-tools/src/ba_tools/state/atomic.py, packages/ba-tools/src/ba_tools/state/locking.py</read_first>
  <behavior>
    - Path cases cover null/empty/dot, absolute, drive-relative, UNC, alternate separator, NUL, traversal, sibling prefix, Unicode, symlink, junction, and reparse objects.
    - Lock cases cover two writers, bounded timeout, holder crash, live/dead/foreign/unknown owner metadata, interruption, and permission-denied liveness.
    - Atomic cases inject faults before and after write, flush, fsync, create link, replace, cleanup, parent sync, and quarantine.
  </behavior>
  <action>Author behavior-first process/filesystem tests for D-16 through D-19. Snapshot canonical bytes, SHA-256, mtimes, lock paths, owner sidecar, temp families, and quarantine before each no-op/failure. Use process barriers/events and bounded joins, never timing sleeps as correctness. Test stale recovery with same-host dead PID, live PID, foreign host, malformed/unreadable metadata, and access-denied liveness; only provably dead same-host ownership may recover after native acquisition. Require canonical absent-or-previous-complete across every injected durability boundary and preserve recognizable temp evidence. Exercise symlink/junction/reparse behavior when the host can create it and give a precise capability reason for a platform skip. Run RED for missing hardening behavior.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/security/test_path_containment.py packages/ba-tools/tests/reliability -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/security/test_path_containment.py packages/ba-tools/tests/reliability -q</automated>
    <expected>RED: owner recovery, quarantine, adversarial containment, and durability fault cases fail for absent production behavior.</expected>
  </verify>
  <acceptance_criteria>
    - Test IDs cite D-16, D-17, D-18, or D-19 and cover every listed matrix row.
    - Concurrency uses deterministic synchronization and bounded process cleanup.
    - Failure cases compare canonical bytes/hash/mtime and preserved recovery evidence.
    - Platform skips state the unavailable filesystem capability and do not hide ordinary path cases.
  </acceptance_criteria>
  <done>The reliability matrix is complete and fails only at the unimplemented containment/recovery/durability contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden containment, native ownership, publication, and quarantine</name>
  <files>packages/ba-tools/src/ba_tools/paths.py, packages/ba-tools/src/ba_tools/state/atomic.py, packages/ba-tools/src/ba_tools/state/locking.py</files>
  <read_first>packages/ba-tools/tests/security/test_path_containment.py, packages/ba-tools/tests/reliability/test_atomic_write.py, packages/ba-tools/tests/reliability/test_locking.py, packages/ba-tools/src/ba_tools/paths.py, packages/ba-tools/src/ba_tools/state/atomic.py, packages/ba-tools/src/ba_tools/state/locking.py</read_first>
  <behavior>
    - A path capability is returned only after grammar, resolved ancestry, and existing-component symlink/reparse checks.
    - One native `.ba-ops.lock` and owner sidecar serialize all mutations for a fixed five-second production timeout.
    - Same-directory bytes are synced and published through distinct create-only and replace functions; abandoned temps are quarantined under lock.
  </behavior>
  <action>Reject all unsafe path forms before candidate access and verify ancestry with path-aware operations. Implement `WorkspaceLock` using native `filelock.FileLock`; keep `.ba-ops.lock` persistent and manage `.ba-ops.lock.owner.json` with `LockOwner(schema_version, pid, hostname, owner_id)` only while the native lock is held. Per D-17, recover sidecar evidence only after acquisition and same-host dead-process proof; live, foreign, malformed, unreadable, access-denied, and unknown states fail closed without altering evidence. Keep `atomic_create` and `atomic_replace` separate: secure same-directory temp, binary write, flush, fsync, close, then create-only link or `os.replace`, followed by parent sync where supported. Per D-18/D-19, sort recognizable abandoned temps and move them without overwrite to `.ba-ops/quarantine/atomic/`; preserve bytes and repo-relative target identity, and never use quarantine as canonical input.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/security/test_path_containment.py packages/ba-tools/tests/reliability -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/security/test_path_containment.py packages/ba-tools/tests/reliability -q</automated>
  </verify>
  <acceptance_criteria>
    - Every rejected path fails before candidate open/stat/write and cannot become accepted by normalization.
    - Contention returns `WORKSPACE_LOCK_TIMEOUT` near five seconds and writes no canonical/temp/quarantine bytes.
    - Live or ambiguous owner evidence remains byte-identical; provably dead same-host evidence changes only after native acquisition.
    - Every fault leaves canonical absent or previous-complete; no partial canonical is observable.
    - Quarantine preserves bytes, reports repo-relative identities, and never promotes a temp.
  </acceptance_criteria>
  <done>All containment, native lock ownership, fault, and quarantine tests pass through the locked development interpreter.</done>
</task>

</tasks>

## Artifacts this phase produces

- `packages/ba-tools/src/ba_tools/paths.py` additions — `PathPolicy`, `ResolvedRepoRoot`, `ResolvedBusinessPath`, `parse_business_path`, `resolve_business_path`, `reject_symlink_or_reparse_components`, `assert_resolved_containment`.
- `packages/ba-tools/src/ba_tools/state/locking.py` additions — `LockOwner(schema_version, pid, hostname, owner_id)`, `ProcessLiveness`, `probe_process_liveness`, `recover_stale_owner_metadata`, `WorkspaceLock`, `acquire_workspace_lock`.
- `packages/ba-tools/src/ba_tools/state/atomic.py` additions — `AbandonedTemp`, `prepare_temp`, `atomic_create`, `atomic_replace`, `sync_parent_directory`, `quarantine_abandoned_temps`.
- Generated paths `.ba-ops.lock`, `.ba-ops.lock.owner.json`, `.ba-ops/quarantine/atomic/*`, and recognizable same-directory `.*.ba-tmp-*` files.
- `packages/ba-tools/tests/security/test_path_containment.py` — `test_rejects_empty_and_non_relative_forms`, `test_rejects_traversal_and_sibling_prefix`, `test_rejects_symlink_and_reparse_escape`, `test_unicode_contained_path_is_accepted`, `test_concurrent_resolution_never_escapes_root`.
- `packages/ba-tools/tests/reliability/test_atomic_write.py` — `test_atomic_create_never_replaces`, `test_atomic_replace_is_complete`, `test_fault_preserves_previous_canonical`, `test_abandoned_temp_is_quarantined_not_promoted`, `test_quarantine_preserves_bytes_and_identity`.
- `packages/ba-tools/tests/reliability/test_locking.py` — `test_lock_timeout_is_bounded`, `test_concurrent_writers_are_serialized`, `test_crashed_holder_releases_native_lock`, `test_only_dead_same_host_owner_recovers`, `test_ambiguous_owner_fails_closed`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository path text → OS filesystem object | Alternate path grammars and filesystem indirection can escape containment. |
| Concurrent process → workspace mutation | Writers, crashes, and stale metadata contend for canonical state. |
| Temporary bytes → canonical/quarantine name | Faults must never expose partial or incorrectly promoted bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-BAOPS-01-05-PATH | Tampering / Information Disclosure | `paths.py` | high | mitigate | Strict grammar, resolved ancestry, symlink/reparse rejection, and concurrent adversarial tests. |
| T-BAOPS-01-05-LOCK | Tampering / Denial of Service | Native lock and owner sidecar | high | mitigate | Bounded kernel lock, owner metadata only under ownership, dead-process proof, and fail-closed ambiguity tests. |
| T-BAOPS-01-05-PARTIAL | Tampering / Denial of Service | Atomic publication | high | mitigate | Same-directory secure temp, fsync, distinct create/replace operations, parent sync, fault injection, and quarantine without promotion. |
| T-BAOPS-01-SC | Tampering | `filelock` dependency | high | mitigate | Use only the exact approved hash lock and assert a native backend on supported hosts. |
</threat_model>

<verification>
- [ ] Path containment suite passes across ordinary and platform-specific cases.
- [ ] Reliability suite passes every synchronized process and fault boundary.
- [ ] Walking-skeleton tests remain green after hardening.
</verification>

<success_criteria>
- All five assigned path/reliability edge assertions pass.
- D-16 through D-19 have implementation and process/fault evidence.
- No timeout, ambiguity, crash, or fault can alter previous complete canonical bytes.
</success_criteria>

<output>
Create `.planning/phases/BAOPS-01-harness-foundation/01-05-SUMMARY.md` when done.
</output>
