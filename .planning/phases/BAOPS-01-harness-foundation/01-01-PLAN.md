---
phase: BAOPS-01-harness-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - FOUND-03
  - NFR-03
must_haves:
  truths:
    - "No final lock, virtual environment, or installed distribution exists before a human approves the binary-only dependency closure."
    - "Approval covers runtime/bootstrap and development closures for CPython 3.11 and 3.14 on Windows x64, macOS 12 x64/arm64, and Linux x64."
    - "Any approved substitution is applied to every target resolution before approval; the recorded per-target sets and their normalized union are authoritative for Plan 01-02."
  artifacts:
    - path: ".planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md"
      provides: "Package Legitimacy Approval with direct pins, substitutions, target matrix, per-target closures, normalized unions, provenance, and disposition"
      contains: "Package Legitimacy Approval"
  key_links:
    - from: ".planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md"
      to: ".planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md"
      via: "binary-only target reports expand the audited direct package candidates into exact runtime and development closures"
      pattern: "Runtime Closure|Development Closure|Approved Substitutions"
  prohibitions: []
---

## Phase Goal

**As a** Business Analyst, **I want to** bootstrap a portable, healthy harness workspace, **so that** I can use the same deterministic workflow on every supported operating system.

<objective>
Clear the dependency supply-chain gate for every supported target before lock generation or installation.

Purpose: Honor D-05 and D-06 while ensuring target-specific transitives, wheel availability, and approved substitutions cannot diverge from the final locks.
Output: One blocking approval record that Plan 01-02 can consume as an exact data contract.
</objective>

<execution_context>
@C:/Users/TungNS/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/TungNS/.cursor/gsd-core/templates/summary.md
@C:/Users/TungNS/.cursor/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md
@.planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md
@.planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve every binary-only target closure</name>
  <files>None — write the decision and exact closure tables to 01-01-SUMMARY.md</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md, .planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md, .planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md</read_first>
  <action>Start with exact candidates `click==8.4.2`, `filelock==3.29.6`, `jsonschema==4.26.0`, `hatchling==1.29.0`, `pytest==9.1.1`, and `ruff==0.15.22`. Per D-06, resolve without installation using pip dry-run reports with binary-only enforcement for the Cartesian target matrix CPython 3.11/3.14 × Windows x64, macOS 12 x64, macOS 12 arm64, and Linux x64. Produce separate runtime/bootstrap roots and development roots, normalize names with the PEP 503 rule, and retain exact versions, environment markers, wheel filenames/tags, and SHA-256 values. Compute each per-target set and the two unions. Run the package-legitimacy check over every union name, then present PyPI and attributable source links. If the user approves a substitute name or version, replace the candidate, rerun every affected target report, recompute both unions, and re-present the complete closure; never carry the rejected package into an approved table. Per D-05, state that approval authorizes only the exact recorded artifacts and does not persist installer network consent. Record headings `Direct Pins`, `Approved Substitutions`, `Target Matrix`, `Runtime Closure`, `Development Closure`, `Binary-Only Evidence`, and `Package Legitimacy Approval` in the summary.</action>
  <verify>
    <automated>node "C:\Users\TungNS\.cursor\gsd-core\bin\gsd-tools.cjs" package-legitimacy check --ecosystem pypi click filelock jsonschema hatchling pytest ruff</automated>
    <human-check>Confirm every name/version in both normalized unions has acceptable registry/source provenance, every target report is binary-only, and any substitution appears in all recomputed target tables.</human-check>
  </verify>
  <acceptance_criteria>
    - The target matrix has eight resolved rows for runtime/bootstrap and eight for development, with no installed-file action.
    - Every per-target package has a compatible wheel and exact version; source-only fallback blocks approval.
    - The audited normalized-name set equals the union of all target closures.
    - Approved direct pins and substitutions are unambiguous; rejected package names and versions are absent from approved closures.
    - No lock file, `.ba-tools-runtime/dev`, or installed project distribution is created before approval.
    - `SLOP` packages block execution; every `SUS` or `ASSUMED` package has an explicit human disposition.
  </acceptance_criteria>
  <done>The user approves exact direct pins, substitutions, every per-target runtime/development closure, and both normalized unions; all data is recorded in 01-01-SUMMARY.md.</done>
  <what-built>A read-only, binary-only provenance and compatibility report for every package artifact that can enter a supported target.</what-built>
  <how-to-verify>Review each target table against its dry-run report, verify registry/source ownership, confirm compatible wheel tags and hashes, then approve the complete batch or identify substitutions.</how-to-verify>
  <resume-signal>Type "approved" to authorize exactly the recorded closures, or list rejected packages and approved substitutions.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md` — the only created file; its `Package Legitimacy Approval` record contains `Direct Pins`, `Approved Substitutions`, `Target Matrix`, `Runtime Closure`, `Development Closure`, `Binary-Only Evidence`, normalized package names, exact versions, markers, wheel tags/files, SHA-256 values, registry/source URLs, verdicts, and human dispositions.
- No product source, package manifest, lock, environment, launcher, class, function, flag, field, schema, generated `.ba-ops/` path, or test function is created by this checkpoint.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Package registry/source repository → approval record | Untrusted package identity, ownership, metadata, target compatibility, and artifact hashes become authorized inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-BAOPS-01-SC | Tampering | Direct and transitive Python dependencies | high | mitigate | Block lock generation and installation; resolve all target closures with binary-only enforcement, audit the exact unions, recompute after substitutions, and require explicit human approval. |
| T-BAOPS-01-01-SPOOF | Spoofing | Package names and source ownership | high | mitigate | Pair every normalized registry name with its official PyPI page and attributable source repository, then record a human disposition. |
| T-BAOPS-01-01-INFO | Information Disclosure | Dry-run reports | low | accept | Reports contain public package metadata and temporary local paths only; summaries retain no credential or private repository path. |
</threat_model>

<verification>
- [ ] Every supported OS/architecture and Python 3.11/3.14 target has runtime and development binary-only reports.
- [ ] The legitimacy input equals the normalized union of all report package names.
- [ ] Approved substitutions have replaced rejected candidates in every affected report.
- [ ] No final lock or environment exists before the blocking approval.
</verification>

<success_criteria>
- The package approval record is an exact, target-aware contract rather than a host-only snapshot.
- D-05 consent remains invocation-scoped and D-06 exact-pin policy is preserved.
- Plan 01-02 can prove final lock equality without introducing or dropping a package.
</success_criteria>

<output>
Create `.planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md` when done.
</output>
