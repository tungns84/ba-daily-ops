---
phase: BAOPS-01-harness-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - packages/ba-tools/pyproject.toml
  - packages/ba-tools/requirements.lock
  - packages/ba-tools/requirements-dev.lock
  - packages/ba-tools/src/ba_tools/__init__.py
  - packages/ba-tools/src/ba_tools/__main__.py
  - packages/ba-tools/src/ba_tools/state/resources/defaults/config.json
  - packages/ba-tools/src/ba_tools/state/resources/defaults/coverage-policy.json
  - packages/ba-tools/src/ba_tools/state/resources/defaults/business-goals.json
  - packages/ba-tools/tests/e2e/test_walking_skeleton.py
  - scripts/verify_dependency_locks.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-04
  - NFR-03
  - NFR-04
  - NFR-05
must_haves:
  truths:
    - "The exact Phase 1 distribution version is 0.1.0 in project metadata and `ba_tools.__version__`."
    - "For every approved target, each final lock activates exactly the approved closure after marker evaluation; both lock unions equal the approved unions and contain no extra package."
    - "A development environment built only from the approved development lock provides the interpreter used by every later local Python verification."
    - "The walking-skeleton end-to-end test collects and fails for absent CLI/state behavior, not for package, dependency, import, or fixture failure."
  artifacts:
    - path: "packages/ba-tools/pyproject.toml"
      provides: "Exact package version 0.1.0, Python floor, approved dependency roots, entry point, and test/lint configuration"
      contains: "version = \"0.1.0\""
    - path: "packages/ba-tools/requirements.lock"
      provides: "Universal target-aware runtime/bootstrap exact hash lock"
      contains: "--hash=sha256:"
    - path: "packages/ba-tools/requirements-dev.lock"
      provides: "Universal target-aware development exact hash lock"
      contains: "--hash=sha256:"
    - path: "scripts/verify_dependency_locks.py"
      provides: "Deterministic target marker and exact-set comparator"
      contains: "def verify_exact_lock_sets("
    - path: "packages/ba-tools/tests/e2e/test_walking_skeleton.py"
      provides: "Fail-first repository-root install-to-init contract"
      contains: "test_repo_root_init_walking_skeleton"
  key_links:
    - from: ".planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md"
      to: "packages/ba-tools/requirements.lock"
      via: "approved substitutions, per-target closures, and unions are compared for exact equality before installation"
      pattern: "verify_exact_lock_sets"
    - from: "packages/ba-tools/requirements-dev.lock"
      to: ".ba-tools-runtime/dev/{Scripts/python.exe|bin/python}"
      via: "hash-required binary-only installation creates the canonical development interpreter"
      pattern: "requirements-dev.lock"
  prohibitions: []
---

## Phase Goal

**As a** Business Analyst, **I want to** bootstrap a portable, healthy harness workspace, **so that** I can use the same deterministic workflow on every supported operating system.

<objective>
Materialize the approved package contract, exact version, universal locks, locked development interpreter, packaged defaults, and RED walking-skeleton test.

Purpose: Establish a reproducible execution/test base before implementing the end-to-end CLI-to-state path.
Output: Package metadata and defaults, exact target-aware locks, lock equality evidence, `.ba-tools-runtime/dev`, and one behavior-first failing test.
</objective>

<execution_context>
@C:/Users/TungNS/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/TungNS/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/BAOPS-01-harness-foundation/SKELETON.md
@.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md
@.planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md
@.planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md
@.planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate approved universal locks and exact project metadata</name>
  <files>packages/ba-tools/pyproject.toml, packages/ba-tools/requirements.lock, packages/ba-tools/requirements-dev.lock, scripts/verify_dependency_locks.py</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md, .planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md, .planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md, .planning/phases/BAOPS-01-harness-foundation/SKELETON.md</read_first>
  <action>Per D-06, declare exact `project.version = "0.1.0"`, Python `>=3.11`, package resource inclusion, approved dependency groups, and `ba-tools = "ba_tools.__main__:entrypoint"`. Read approved direct pins and substitutions from 01-01-SUMMARY.md instead of assuming original candidates. Generate `requirements.lock` and `requirements-dev.lock` as universal marker-aware locks whose active package set for each approved target equals its approved runtime or development closure. Include every approved wheel SHA-256 needed by Windows x64, macOS 12 x64/arm64, and Linux x64 on CPython 3.11/3.14. Create `verify_dependency_locks.py` using only the standard library with `parse_approval_summary`, `parse_lock`, `evaluate_target_markers`, `verify_exact_lock_sets`, and `main`; reject missing targets, unapproved names/versions, absent direct packages, source-only artifacts, unhashed lines, duplicate conflicting pins, union mismatch, and any extra package. Run it with the prerequisite host Python before creating an environment.</action>
  <verify>
    <automated>python scripts/verify_dependency_locks.py --approval .planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md --runtime-lock packages/ba-tools/requirements.lock --dev-lock packages/ba-tools/requirements-dev.lock</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains exact project version `0.1.0`; versioning is not dynamic.
    - Approved substitutions replace original candidates in metadata and locks.
    - For all eight targets, evaluated runtime and development sets equal the approved per-target sets.
    - Runtime and development unions equal the approved unions; the comparator reports zero missing and zero extra packages.
    - Every active requirement is exact-pinned and has approved SHA-256 wheel hashes; no source distribution is admitted.
  </acceptance_criteria>
  <done>Exact project metadata, both target-aware locks, and the lock comparator agree with the approval record before installation.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize package version surfaces and create the locked interpreter</name>
  <files>packages/ba-tools/src/ba_tools/__init__.py, packages/ba-tools/src/ba_tools/__main__.py</files>
  <read_first>packages/ba-tools/pyproject.toml, packages/ba-tools/requirements-dev.lock, packages/ba-tools/src/ba_tools/__init__.py, packages/ba-tools/src/ba_tools/__main__.py</read_first>
  <action>Set literal `ba_tools.__version__ = "0.1.0"` and expose an importable `entrypoint(argv: list[str] | None = None) -> int` so the package can build and subprocess startup reaches the application boundary. Create ignored `.ba-tools-runtime/dev`, install `requirements-dev.lock` with pip hash mode and binary-only enforcement, and install the local distribution with `--no-deps --no-build-isolation`. Re-run `verify_dependency_locks.py` through the resulting interpreter and compare `importlib.metadata.version("ba-tools")`, `ba_tools.__version__`, and `project.version`; all must equal `0.1.0`. Record the platform-aware interpreter contract: Windows `.\.ba-tools-runtime\dev\Scripts\python.exe`; POSIX `./.ba-tools-runtime/dev/bin/python`. Every later Python test, lint, and support command uses that interpreter.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -c "import ba_tools,importlib.metadata as m; assert ba_tools.__version__ == m.version('ba-tools') == '0.1.0'"; POSIX: ./.ba-tools-runtime/dev/bin/python -c "import ba_tools,importlib.metadata as m; assert ba_tools.__version__ == m.version('ba-tools') == '0.1.0'"</automated>
  </verify>
  <acceptance_criteria>
    - Project metadata, installed metadata, and `ba_tools.__version__` are exactly `0.1.0`.
    - `.ba-tools-runtime/dev` is created only after the lock comparator succeeds.
    - Dependency installation uses hash and binary-only enforcement; local installation uses no dependency resolution or build isolation.
    - The package imports successfully through both platform-specific development interpreter paths.
  </acceptance_criteria>
  <done>The synchronized exact-version package is installed in the canonical locked development environment used by all later verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Establish exact defaults and the RED walking-skeleton path</name>
  <files>packages/ba-tools/src/ba_tools/state/resources/defaults/config.json, packages/ba-tools/src/ba_tools/state/resources/defaults/coverage-policy.json, packages/ba-tools/src/ba_tools/state/resources/defaults/business-goals.json, packages/ba-tools/tests/e2e/test_walking_skeleton.py</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md, .planning/phases/BAOPS-01-harness-foundation/01-UI-SPEC.md, packages/ba-tools/pyproject.toml, packages/ba-tools/src/ba_tools/__init__.py, packages/ba-tools/src/ba_tools/__main__.py</read_first>
  <behavior>
    - From outside a Unicode repository path containing spaces, invoke the locked development module with `-X utf8`, `--repo-root`, and `init`.
    - Fresh init must return one stdout success envelope, empty stderr, and the three exact canonical `.ba-ops/` files.
    - A repeated invocation must return `changed:false`, `created:[]`, and preserve bytes and mtimes.
    - `--version` must report exactly `0.1.0` in the normal success envelope.
  </behavior>
  <action>Write the three D-15 packaged defaults as canonical UTF-8 JSON: config profile `light`; the exact light coverage policy with empty `per_req` and `waivers`; and versioned `business_goals: []`. Author `test_repo_root_init_walking_skeleton`, `test_repo_root_init_is_idempotent`, and `test_cli_version_is_exactly_0_1_0`. The tests invoke the platform-aware development interpreter from a CWD outside the repository, parse raw bytes, assert one document on one stream, validate exact generated paths and file bytes, and snapshot mtimes. Keep `__main__.py` importable so collection and subprocess startup succeed; the RED result must identify absent command/state behavior.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/e2e/test_walking_skeleton.py -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/e2e/test_walking_skeleton.py -q</automated>
    <expected>RED: the three named tests collect and fail on missing CLI/state assertions; package import, dependency loading, and subprocess startup succeed.</expected>
  </verify>
  <acceptance_criteria>
    - The resource files contain the exact D-15 payloads, UTF-8 without BOM and one trailing LF.
    - The repository path contains Vietnamese characters and spaces; invocation CWD is outside it.
    - Test failure is behavioral and names missing `init`, output, file, idempotency, or version behavior.
    - No source text, environment value, timestamp, UUID, random value, or sample business goal enters a default.
  </acceptance_criteria>
  <done>The package and locked test environment are healthy, defaults are exact, and the end-to-end walking-skeleton contract is demonstrably RED.</done>
</task>

</tasks>

## Artifacts this phase produces

- `packages/ba-tools/pyproject.toml` — project `ba-tools`, exact `project.version = "0.1.0"`, Python `>=3.11`, approved dependency groups, `ba_tools.__main__:entrypoint`, package-resource inclusion, pytest and Ruff configuration.
- `packages/ba-tools/requirements.lock` — universal runtime/bootstrap exact pins, target markers, and approved wheel hashes.
- `packages/ba-tools/requirements-dev.lock` — universal development exact pins, target markers, and approved wheel hashes.
- `packages/ba-tools/src/ba_tools/__init__.py` — exported constant `__version__ = "0.1.0"`.
- `packages/ba-tools/src/ba_tools/__main__.py` — importable `entrypoint(argv: list[str] | None = None) -> int` process seam consumed by Plan 01-03.
- `packages/ba-tools/src/ba_tools/state/resources/defaults/config.json` — fields `profile`, `schema_version`.
- `packages/ba-tools/src/ba_tools/state/resources/defaults/coverage-policy.json` — fields `per_req`, `profile`, `required_artifact_kinds`, `schema_version`, `waivers`.
- `packages/ba-tools/src/ba_tools/state/resources/defaults/business-goals.json` — fields `business_goals`, `schema_version`.
- `scripts/verify_dependency_locks.py` — `parse_approval_summary`, `parse_lock`, `evaluate_target_markers`, `verify_exact_lock_sets`, `main`.
- `packages/ba-tools/tests/e2e/test_walking_skeleton.py` — `test_repo_root_init_walking_skeleton`, `test_repo_root_init_is_idempotent`, `test_cli_version_is_exactly_0_1_0`.
- Generated development path `.ba-tools-runtime/dev/Scripts/python.exe` on Windows or `.ba-tools-runtime/dev/bin/python` on POSIX; this is the canonical later-plan test interpreter.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Approved summary → package metadata and locks | Human-approved package identities become executable dependency declarations. |
| Lock artifacts → development environment | Exact package wheels enter the project-local test interpreter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-BAOPS-01-SC | Tampering | Universal runtime/development locks | high | mitigate | Compare every target set and union to the approved record before installation; require exact pins, approved hashes, binary-only artifacts, and local install without dependency resolution. |
| T-BAOPS-01-02-VERSION | Tampering / Repudiation | Package identity surfaces | medium | mitigate | Fix version 0.1.0 in metadata and `__version__`, test it through CLI in the walking skeleton, and carry it into installer identity in Plan 01-07. |
| T-BAOPS-01-02-ENV | Elevation of Privilege | Development environment | high | mitigate | Create it under the repository, install only after exact-set verification, and use it for all subsequent tests instead of ambient packages. |
</threat_model>

<verification>
- [ ] Lock comparator proves exact target sets and unions with no extras.
- [ ] Both version surfaces are exactly 0.1.0.
- [ ] The locked development interpreter imports pytest and the local package.
- [ ] The three walking-skeleton tests are RED for intended behavior.
</verification>

<success_criteria>
- Package approval, metadata, locks, and installed development packages form one exact data contract.
- Later plans have one platform-aware locked interpreter.
- The first behavior test is fail-first and exercises the intended terminal-to-state path.
</success_criteria>

<output>
Create `.planning/phases/BAOPS-01-harness-foundation/01-02-SUMMARY.md` when done.
</output>
