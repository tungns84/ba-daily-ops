---
phase: BAOPS-01-harness-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - packages/ba-tools/src/ba_tools/__main__.py
  - packages/ba-tools/src/ba_tools/cli.py
  - packages/ba-tools/src/ba_tools/contracts.py
  - packages/ba-tools/src/ba_tools/errors.py
  - packages/ba-tools/tests/conftest.py
  - packages/ba-tools/tests/contract/test_cli_io.py
  - packages/ba-tools/tests/contract/test_dependency_locks.py
  - packages/ba-tools/tests/security/test_zero_network.py
  - packages/ba-tools/tests/portability/test_utf8.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - NFR-03
  - NFR-04
  - NFR-05
must_haves:
  truths:
    - "[EDGE FOUND-01/empty] No-argument, help, and version calls produce one non-empty success envelope; every failure produces one non-empty error envelope."
    - "[EDGE FOUND-01/encoding] Output is one UTF-8 JSON document without BOM, ANSI, ASCII escaping, or prefix and with exactly one trailing LF."
    - "[EDGE FOUND-01/concurrency] Concurrent processes each emit one complete object on one designated stream without interleaving."
    - "[EDGE NFR-03/concurrency] Every runtime command remains zero-network under parallel execution; socket and DNS guards fail attempted access."
    - "[EDGE NFR-04/empty] Empty strings and empty Unicode-bearing collections round-trip without substitution."
    - "[EDGE NFR-04/encoding] Vietnamese content survives redirected streams as exact bytes."
    - "[EDGE NFR-04/concurrency] Parallel processes preserve complete multibyte sequences and independent streams."
    - "[EDGE NFR-05/idempotency] Identical mechanical inputs produce byte-identical output without inferred business meaning."
    - "[EDGE NFR-05/concurrency] Concurrent execution cannot introduce environment-derived meaning, inferred defaults, or order-dependent business content."
    - "Permanent tests prove each final lock's target sets and union exactly equal the package approval record with zero extras."
  artifacts:
    - path: "packages/ba-tools/tests/contract/test_cli_io.py"
      provides: "Complete process, stream, version, and deterministic-envelope matrix"
      contains: "test_every_process_path_emits_once"
    - path: "packages/ba-tools/tests/contract/test_dependency_locks.py"
      provides: "Permanent target closure and exact-set regression tests"
      contains: "test_lock_sets_equal_approved_target_closures"
    - path: "packages/ba-tools/tests/security/test_zero_network.py"
      provides: "Runtime import scan and per-process socket/DNS denial"
      contains: "test_every_runtime_command_is_zero_network"
    - path: "packages/ba-tools/tests/portability/test_utf8.py"
      provides: "Redirected, concurrent, path, file, and error UTF-8 byte evidence"
      contains: "test_vietnamese_bytes_round_trip"
  key_links:
    - from: "packages/ba-tools/tests/contract/test_dependency_locks.py"
      to: ".planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md"
      via: "the committed lock comparator is exercised against the authoritative approved target closures"
      pattern: "verify_exact_lock_sets"
    - from: "packages/ba-tools/tests/contract/test_cli_io.py"
      to: "packages/ba-tools/src/ba_tools/__main__.py"
      via: "real subprocesses exercise all parser, command, interruption, and fault paths through the sole emitter"
      pattern: "entrypoint"
    - from: "packages/ba-tools/tests/security/test_zero_network.py"
      to: "packages/ba-tools/src/ba_tools"
      via: "AST/import scan plus guarded command execution covers every runtime module and command"
      pattern: "socket|dns"
  prohibitions:
    - statement: "The runtime zero-network boundary MUST NOT permit telemetry, update checks, index probes, HTTP, DNS, or LLM access."
      status: resolved
      verification: test
---

## Phase Goal

**As a** Business Analyst, **I want to** bootstrap a portable, healthy harness workspace, **so that** I can use the same deterministic workflow on every supported operating system.

<objective>
Harden the walking skeleton's package, process, network, and UTF-8 contracts with real subprocess and concurrency evidence.

Purpose: Ensure every success/failure path and every approved dependency set obeys the same deterministic boundary before state-recovery and installer work proceed.
Output: Shared fixtures, permanent dependency equality tests, complete CLI contract tests, runtime network denial, UTF-8 portability tests, and any gateway corrections they require.
</objective>

<execution_context>
@C:/Users/TungNS/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/TungNS/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md
@.planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md
@.planning/phases/BAOPS-01-harness-foundation/01-PATTERNS.md
@.planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md
@.planning/phases/BAOPS-01-harness-foundation/01-UI-SPEC.md
@.planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md
@.planning/phases/BAOPS-01-harness-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED package, process, zero-network, and UTF-8 matrices</name>
  <files>packages/ba-tools/tests/conftest.py, packages/ba-tools/tests/contract/test_cli_io.py, packages/ba-tools/tests/contract/test_dependency_locks.py, packages/ba-tools/tests/security/test_zero_network.py, packages/ba-tools/tests/portability/test_utf8.py</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-01-SUMMARY.md, .planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md, .planning/phases/BAOPS-01-harness-foundation/01-UI-SPEC.md, scripts/verify_dependency_locks.py, packages/ba-tools/src/ba_tools/__main__.py, packages/ba-tools/src/ba_tools/contracts.py, packages/ba-tools/tests/e2e/test_walking_skeleton.py</read_first>
  <behavior>
    - Real subprocesses cover no-args, help, exact version, init, malformed options, unknown commands, expected errors, keyboard interruption, and injected unexpected failure.
    - Target-evaluated lock sets and normalized unions equal the approved closure record, with exact hashes and no source-only or extra package.
    - Every runtime command executes with socket construction and DNS lookup denied.
    - Vietnamese and empty values survive redirected and concurrent byte streams.
  </behavior>
  <action>Create fixtures `temp_repo`, `unicode_repo_with_spaces`, `dev_python`, `run_cli_bytes`, `inject_cli_fault`, `process_barrier`, and `deny_network`. In `test_cli_io.py`, parse raw bytes and assert one physical JSON line, one trailing LF, deterministic fields/lists, exact stream/exit policy, exact version 0.1.0, and safe errors. In `test_dependency_locks.py`, invoke the committed comparator and independently assert per-target set equality, union equality, exact pins/hashes, approved substitutions, and no source artifact. In `test_zero_network.py`, scan imports below `src/ba_tools` while excluding `installer/`, then invoke every current command under per-process socket and DNS guards. In `test_utf8.py`, exercise Vietnamese success/error text, empty and populated collections, redirected streams, Unicode repository/file names, and synchronized parallel invocations; compare bytes. Run RED only for missing hardening behavior, with fixtures and collection healthy.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/contract packages/ba-tools/tests/security/test_zero_network.py packages/ba-tools/tests/portability/test_utf8.py -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/contract packages/ba-tools/tests/security/test_zero_network.py packages/ba-tools/tests/portability/test_utf8.py -q</automated>
    <expected>RED: missing edge behavior fails named assertions; dependency equality tests pass before any product hardening proceeds.</expected>
  </verify>
  <acceptance_criteria>
    - Dependency tests pass and prove exact equality rather than subset inclusion.
    - Process tests cover success, usage, expected, interrupted, and unexpected paths.
    - Network tests execute every registered command and fail any socket or DNS call.
    - UTF-8 tests compare literal bytes, include `Tiếng Việt — mục tiêu trống`, and use synchronized parallel processes.
    - All subprocesses use the locked development interpreter.
  </acceptance_criteria>
  <done>The complete contract matrix exists; dependency assertions are green and behavior gaps are RED with deterministic diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make every process and encoding contract GREEN</name>
  <files>packages/ba-tools/src/ba_tools/__main__.py, packages/ba-tools/src/ba_tools/cli.py, packages/ba-tools/src/ba_tools/contracts.py, packages/ba-tools/src/ba_tools/errors.py</files>
  <read_first>packages/ba-tools/tests/contract/test_cli_io.py, packages/ba-tools/tests/security/test_zero_network.py, packages/ba-tools/tests/portability/test_utf8.py, packages/ba-tools/src/ba_tools/__main__.py, packages/ba-tools/src/ba_tools/cli.py, packages/ba-tools/src/ba_tools/contracts.py, packages/ba-tools/src/ba_tools/errors.py</read_first>
  <behavior>
    - Framework parsing and every exception type return through the one application serializer.
    - Raw bytes stay deterministic and UTF-8 under redirection and concurrency.
    - Runtime code performs no network operation or network-capable import.
  </behavior>
  <action>Correct the process gateway until every RED assertion passes without bypassing the central serializer. Convert Click parse/help/version behavior into returned data under `standalone_mode=False`; map `BaToolsError`, Click exceptions, keyboard interruption, and unexpected exceptions to stable safe codes with exit 2. Write final bytes through one explicit UTF-8 path and flush once. Keep handlers data-returning and runtime modules local-only. Ensure exact version remains 0.1.0 across metadata, `__version__`, and `--version`. Preserve full arrays, Vietnamese text, and repo-relative paths without truncation or ASCII escaping.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/e2e/test_walking_skeleton.py packages/ba-tools/tests/contract packages/ba-tools/tests/security/test_zero_network.py packages/ba-tools/tests/portability/test_utf8.py -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/e2e/test_walking_skeleton.py packages/ba-tools/tests/contract packages/ba-tools/tests/security/test_zero_network.py packages/ba-tools/tests/portability/test_utf8.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Every process path emits exactly one JSON document on exactly one stream and returns exit 0 or 2.
    - Concurrent outputs parse independently with no partial JSON or multibyte sequence.
    - Runtime import and guarded-command tests prove zero network.
    - Exact version, dependency equality, walking skeleton, and UTF-8 tests all pass together.
  </acceptance_criteria>
  <done>The walking skeleton is contract-hardened across package identity, all process paths, zero-network execution, UTF-8 redirection, and concurrency.</done>
</task>

</tasks>

## Artifacts this phase produces

- `packages/ba-tools/tests/conftest.py` — fixtures `temp_repo`, `unicode_repo_with_spaces`, `dev_python`, `run_cli_bytes`, `inject_cli_fault`, `process_barrier`, `deny_network`.
- `packages/ba-tools/tests/contract/test_cli_io.py` — `test_every_process_path_emits_once`, `test_success_uses_stdout_and_exit_zero`, `test_failure_uses_stderr_and_exit_two`, `test_exact_version_surfaces_match`, `test_json_bytes_are_canonical_utf8`, `test_concurrent_process_outputs_are_independent`, `test_unexpected_error_is_redacted`.
- `packages/ba-tools/tests/contract/test_dependency_locks.py` — `test_lock_sets_equal_approved_target_closures`, `test_lock_unions_equal_approved_unions`, `test_no_unapproved_or_source_packages`, `test_every_lock_entry_is_exact_and_hashed`, `test_approved_substitutions_are_consumed`.
- `packages/ba-tools/tests/security/test_zero_network.py` — `test_runtime_imports_exclude_network_clients`, `test_every_runtime_command_is_zero_network`, `test_parallel_runtime_commands_are_zero_network`.
- `packages/ba-tools/tests/portability/test_utf8.py` — `test_vietnamese_bytes_round_trip`, `test_empty_unicode_values_round_trip`, `test_redirected_streams_are_utf8`, `test_unicode_repo_and_files_round_trip`, `test_concurrent_utf8_outputs_are_complete`.
- Hardened production symbols: `entrypoint`, `cli`, `canonical_json_bytes`, `emit_json`, `success_envelope`, `error_envelope`, and `BaToolsError`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Framework/process failures → terminal streams | Untrusted argv and exceptions must become one redacted document. |
| Runtime process → network APIs | Product commands must remain local even under faults and concurrency. |
| Lock approval record → committed locks | Package closure drift must be mechanically detected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-BAOPS-01-04-ERR | Information Disclosure | Process gateway | high | mitigate | Allowlist fields, centralize emission, and test every exception path for traceback/path/environment leakage. |
| T-BAOPS-01-04-NET | Information Disclosure | `src/ba_tools` | high | mitigate | Static import inspection and per-process socket/DNS denial for every command and concurrent execution. |
| T-BAOPS-01-04-LOCKSET | Tampering | Final dependency locks | high | mitigate | Permanent tests compare every target closure and union to the approved record with zero extras. |
| T-BAOPS-01-SC | Tampering | Test/runtime packages | high | mitigate | Run only through the exact Plan 01-02 development interpreter and recheck the approval contract. |
</threat_model>

<verification>
- [ ] Quick contract, dependency, security, and portability suites pass through `DEV_PYTHON`.
- [ ] Version surfaces are all exactly 0.1.0.
- [ ] Lock target sets and unions match approval exactly.
- [ ] All runtime commands remain zero-network under parallel execution.
</verification>

<success_criteria>
- The package and terminal boundary cannot drift silently.
- All assigned process/network/UTF-8 edge assertions are mechanically exercised.
- Later plans can rely on stable fixtures and deterministic output.
</success_criteria>

<output>
Create `.planning/phases/BAOPS-01-harness-foundation/01-04-SUMMARY.md` when done.
</output>
