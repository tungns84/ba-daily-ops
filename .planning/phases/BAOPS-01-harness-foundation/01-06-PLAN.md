---
phase: BAOPS-01-harness-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - "01-04"
  - "01-05"
files_modified:
  - packages/ba-tools/src/ba_tools/cli.py
  - packages/ba-tools/src/ba_tools/init_command.py
  - packages/ba-tools/src/ba_tools/state/validation.py
  - packages/ba-tools/src/ba_tools/state/resources/schemas/config.schema.json
  - packages/ba-tools/src/ba_tools/state/resources/schemas/coverage-policy.schema.json
  - packages/ba-tools/src/ba_tools/state/resources/schemas/business-goals.schema.json
  - packages/ba-tools/tests/integration/test_init.py
  - packages/ba-tools/tests/unit/test_defaults.py
autonomous: true
requirements:
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - NFR-04
  - NFR-05
must_haves:
  truths:
    - "[EDGE FOUND-04/adjacency] Duplicate required target paths are a configuration error and never collide, merge, or overwrite."
    - "[EDGE FOUND-04/empty] Fresh init creates exactly three required files; an empty `business_goals` array is valid and contains no inferred sample."
    - "[EDGE FOUND-04/ordering] Created paths and validation diagnostics use declared deterministic order, independent of filesystem enumeration."
    - "[EDGE FOUND-04/idempotency] Complete valid state returns `changed:false` and preserves every byte and mtime."
    - "[EDGE FOUND-04/concurrency] Concurrent init and repair are serialized by one workspace lock; timeout produces zero mutation."
    - "Plain partial init, explicit repair, malformed JSON, schema-invalid JSON, and unsupported schema versions follow D-12 through D-14 exactly."
  artifacts:
    - path: "packages/ba-tools/src/ba_tools/state/validation.py"
      provides: "Bundled schema loading, schema self-check, complete stable diagnostics, and state classification"
      contains: "def classify_workspace_state("
    - path: "packages/ba-tools/src/ba_tools/state/resources/schemas/config.schema.json"
      provides: "Closed schema for the versioned profile configuration"
      contains: "\"profile\""
    - path: "packages/ba-tools/tests/integration/test_init.py"
      provides: "Fresh, complete, partial, repair, invalid, duplicate, timeout, and concurrent transition matrix"
      contains: "test_repair_creates_only_missing_files"
  key_links:
    - from: "packages/ba-tools/src/ba_tools/init_command.py"
      to: "packages/ba-tools/src/ba_tools/state/validation.py"
      via: "all existing required files are parsed and validated before transition selection or mutation"
      pattern: "classify_workspace_state"
    - from: "packages/ba-tools/src/ba_tools/init_command.py"
      to: "packages/ba-tools/src/ba_tools/state/locking.py"
      via: "quarantine, classify, validate, transition, publication, and read-back share the workspace lock"
      pattern: "WorkspaceLock"
    - from: "packages/ba-tools/src/ba_tools/state/validation.py"
      to: "packages/ba-tools/src/ba_tools/state/resources/schemas"
      via: "package resources load explicit local Draft 2020-12 schemas with no remote resolution"
      pattern: "Draft202012Validator"
  prohibitions:
    - statement: "Initialization MUST NOT invent business goals, reset invalid state, replace an existing canonical file during repair, or mutate before all existing files validate."
      status: resolved
      verification: test
---

## Phase Goal

**As a** Business Analyst, **I want to** bootstrap a portable, healthy harness workspace, **so that** I can use the same deterministic workflow on every supported operating system.

<objective>
Complete schema-backed initialization, explicit repair, invalid-state preservation, and deterministic diagnostics on the hardened filesystem boundary.

Purpose: Implement D-11 through D-15 as one complete state machine while reusing Plan 01-05 containment, lock, atomic, and quarantine primitives.
Output: Three bundled schemas, state classifier, `init --repair`, complete transition tests, and exact default/schema tests.
</objective>

<execution_context>
@C:/Users/TungNS/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/TungNS/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md
@.planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md
@.planning/phases/BAOPS-01-harness-foundation/01-PATTERNS.md
@.planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md
@.planning/phases/BAOPS-01-harness-foundation/01-UI-SPEC.md
@.planning/phases/BAOPS-01-harness-foundation/01-04-SUMMARY.md
@.planning/phases/BAOPS-01-harness-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the RED schema-backed state-transition matrix</name>
  <files>packages/ba-tools/tests/integration/test_init.py, packages/ba-tools/tests/unit/test_defaults.py</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md, .planning/phases/BAOPS-01-harness-foundation/01-UI-SPEC.md, packages/ba-tools/tests/conftest.py, packages/ba-tools/src/ba_tools/init_command.py, packages/ba-tools/src/ba_tools/state/validation.py, packages/ba-tools/src/ba_tools/state/resources/defaults/config.json, packages/ba-tools/src/ba_tools/state/resources/defaults/coverage-policy.json, packages/ba-tools/src/ba_tools/state/resources/defaults/business-goals.json</read_first>
  <behavior>
    - State matrix covers absent directory, complete valid, partial plain init, partial repair, malformed JSON, schema-invalid JSON, unsupported version, duplicate targets, lock timeout, and concurrent init/repair.
    - Every no-op/failure preserves existing bytes, hashes, and mtimes; repair creates only missing targets in declared order.
    - Default resources validate against their matching schemas and contain no inferred business data.
  </behavior>
  <action>Author tests for D-11 through D-15 before production changes. Snapshot every existing target and recovery artifact before no-op/failure cases. Require plain partial init to return `STATE_INCOMPLETE` with the exact UI-SPEC repair guidance and zero mutation. Require repair to create missing files only. Require malformed, schema-invalid, or unsupported-version input to return every stable field-addressed diagnostic in one `STATE_SCHEMA_INVALID` envelope and preserve all bytes. Test duplicate targets before filesystem access, lock timeout with zero writes, deterministic ordering independent of directory order, and synchronized concurrent commands. Add exact resource byte and schema-validation tests. Run RED for missing classification/schema/repair behavior.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/integration/test_init.py packages/ba-tools/tests/unit/test_defaults.py -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/integration/test_init.py packages/ba-tools/tests/unit/test_defaults.py -q</automated>
    <expected>RED: repair, invalid-state, schema, and duplicate-target assertions fail while walking-skeleton fresh/no-op cases remain green.</expected>
  </verify>
  <acceptance_criteria>
    - Named tests cite D-11, D-12, D-13, D-14, and D-15 in docstrings or case IDs.
    - No-op/failure assertions compare bytes, hashes, mtimes, envelopes, and complete diagnostics.
    - Concurrent tests use barriers/events and bounded joins.
    - Exact default resource bytes are asserted, including valid empty business goals.
  </acceptance_criteria>
  <done>The full state-transition matrix is collected and RED only where schema-backed behavior is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement closed local schemas and deterministic classification</name>
  <files>packages/ba-tools/src/ba_tools/state/validation.py, packages/ba-tools/src/ba_tools/state/resources/schemas/config.schema.json, packages/ba-tools/src/ba_tools/state/resources/schemas/coverage-policy.schema.json, packages/ba-tools/src/ba_tools/state/resources/schemas/business-goals.schema.json</files>
  <read_first>packages/ba-tools/tests/integration/test_init.py, packages/ba-tools/tests/unit/test_defaults.py, packages/ba-tools/src/ba_tools/state/validation.py, packages/ba-tools/src/ba_tools/state/resources/defaults/config.json, packages/ba-tools/src/ba_tools/state/resources/defaults/coverage-policy.json, packages/ba-tools/src/ba_tools/state/resources/defaults/business-goals.json</read_first>
  <behavior>
    - All three schemas are closed, local Draft 2020-12 resources with schema version const 1.
    - Every present file is parsed and validated; diagnostics are complete and deterministically sorted.
    - Classification distinguishes fresh, complete, partial, and invalid without mutation.
  </behavior>
  <action>Add closed local Draft 2020-12 schemas with `schema_version` const 1. Config requires `profile` in `light|standard|strict`; the Phase 1 default is light. Coverage policy requires all fixed structural fields and accepts the exact light resource. Business goals requires an array and accepts empty. Load only through package resources, run `check_schema`, reject remote references, and sort diagnostics by required-file order, JSON pointer, validator, then message. Implement `WorkspaceStateKind`, `ValidationDiagnostic`, `WorkspaceState`, `load_schema`, `validate_state_file`, and `classify_workspace_state`. Detect duplicate target paths before target filesystem access. Classification returns all present/missing/diagnostic data and performs no write.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/unit/test_defaults.py packages/ba-tools/tests/integration/test_init.py -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/unit/test_defaults.py packages/ba-tools/tests/integration/test_init.py -q</automated>
    <expected>Schema/default/classification assertions pass; transition assertions remain RED until Task 3.</expected>
  </verify>
  <acceptance_criteria>
    - Every packaged default validates byte-identically against exactly one local schema.
    - Remote references and unsupported schema versions fail locally.
    - Invalid input returns all stable field-addressed diagnostics.
    - Duplicate targets fail before target state inspection.
    - Classification performs zero canonical, temp, lock, or quarantine mutation.
  </acceptance_criteria>
  <done>Local schemas, complete diagnostics, and mutation-free state classification pass their tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete locked init and explicit repair transitions</name>
  <files>packages/ba-tools/src/ba_tools/cli.py, packages/ba-tools/src/ba_tools/init_command.py</files>
  <read_first>packages/ba-tools/tests/integration/test_init.py, packages/ba-tools/src/ba_tools/cli.py, packages/ba-tools/src/ba_tools/init_command.py, packages/ba-tools/src/ba_tools/state/validation.py, packages/ba-tools/src/ba_tools/state/atomic.py, packages/ba-tools/src/ba_tools/state/locking.py</read_first>
  <behavior>
    - Under one lock, quarantine recognized temps, classify and validate all present files, then select one allowed transition.
    - Plain init mutates fresh state only; repair creates missing targets in otherwise valid partial state only.
    - Invalid existing JSON produces complete sorted diagnostics and no mutation.
  </behavior>
  <action>Under the Plan 01-05 workspace lock, quarantine recognized temps, validate every present file, and classify before any write. Per D-12, partial plain init fails with exact repair guidance. Per D-13, `init --repair` uses create-only publication for missing targets only. Per D-14, any parse/schema/version error preserves every byte and creates nothing. Per D-11, complete state is a no-op; per D-15, fresh/repair read only packaged defaults. Wire `init --repair` with no overwrite route or prompt and return deterministic `changed`, `created`, `quarantined`, and diagnostic data through the sole process gateway.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/integration/test_init.py packages/ba-tools/tests/unit/test_defaults.py packages/ba-tools/tests/contract/test_cli_io.py packages/ba-tools/tests/security/test_path_containment.py packages/ba-tools/tests/reliability -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/integration/test_init.py packages/ba-tools/tests/unit/test_defaults.py packages/ba-tools/tests/contract/test_cli_io.py packages/ba-tools/tests/security/test_path_containment.py packages/ba-tools/tests/reliability -q</automated>
  </verify>
  <acceptance_criteria>
    - Fresh and complete behavior remains unchanged and deterministic.
    - Partial plain init writes nothing and returns exact repair guidance; repair creates missing targets only.
    - Invalid input returns all sorted field diagnostics and preserves every existing byte.
    - Every transition shares one lock with quarantine, validation, publication, and read-back.
    - Contract, path, reliability, init, and default suites pass together.
  </acceptance_criteria>
  <done>The complete schema-backed init/repair matrix and all underlying contract/reliability suites pass.</done>
</task>

</tasks>

## Artifacts this phase produces

- `packages/ba-tools/src/ba_tools/state/validation.py` — `WorkspaceStateKind(fresh, complete, partial, invalid)`, `ValidationDiagnostic(path, pointer, validator, message)`, `WorkspaceState(kind, present, missing, diagnostics)`, `load_schema`, `validate_state_file`, `classify_workspace_state`.
- `packages/ba-tools/src/ba_tools/state/resources/schemas/config.schema.json` — fields `$schema`, `$id`, `type`, `additionalProperties`, `required`, `properties.profile`, `properties.schema_version`.
- `packages/ba-tools/src/ba_tools/state/resources/schemas/coverage-policy.schema.json` — closed definitions for `per_req`, `profile`, `required_artifact_kinds`, `schema_version`, `waivers`.
- `packages/ba-tools/src/ba_tools/state/resources/schemas/business-goals.schema.json` — closed definitions for `business_goals`, `schema_version`.
- `packages/ba-tools/src/ba_tools/init_command.py` additions — `StateTransition`, `classify_then_transition`, expanded `InitResult(changed, created, quarantined)`.
- `packages/ba-tools/src/ba_tools/cli.py` addition — flag `init --repair`.
- Error codes `STATE_INCOMPLETE`, `STATE_SCHEMA_INVALID`, `UNSUPPORTED_SCHEMA_VERSION`, `DUPLICATE_STATE_TARGET`, `WORKSPACE_LOCK_TIMEOUT`.
- Generated canonical paths `.ba-ops/config.json`, `.ba-ops/coverage-policy.json`, `.ba-ops/business-goals.json`; generated recovery paths `.ba-ops/quarantine/atomic/*`.
- `packages/ba-tools/tests/integration/test_init.py` — `test_fresh_init_creates_exact_files`, `test_complete_init_is_byte_and_mtime_noop`, `test_partial_plain_init_writes_nothing`, `test_repair_creates_only_missing_files`, `test_invalid_json_is_preserved`, `test_schema_invalid_state_reports_all_diagnostics`, `test_unsupported_schema_version_is_preserved`, `test_duplicate_targets_fail_before_io`, `test_lock_timeout_writes_nothing`, `test_concurrent_init_and_repair_are_serialized`.
- `packages/ba-tools/tests/unit/test_defaults.py` — `test_exact_default_payload_bytes`, `test_defaults_validate_against_local_schemas`, `test_empty_business_goals_are_valid`, `test_defaults_contain_no_business_inference`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Existing `.ba-ops/` bytes → validator/state machine | User-editable malformed or adversarial JSON must be diagnosed before mutation. |
| State classifier → filesystem publication | Only an explicitly allowed transition may invoke create-only storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-BAOPS-01-06-OVERWRITE | Tampering | `init --repair` | high | mitigate | Preflight every file, classify before mutation, use create-only publication, and assert preserved bytes under races. |
| T-BAOPS-01-06-SCHEMA | Tampering | Local JSON validation | high | mitigate | Bundle self-checked closed schemas, reject remote references, collect all sorted diagnostics, and preserve invalid state. |
| T-BAOPS-01-06-RACE | Tampering / Denial of Service | State transition | high | mitigate | Reuse one workspace lock across quarantine, validation, transition, publication, and read-back; run concurrent tests. |
| T-BAOPS-01-SC | Tampering | `jsonschema` dependency | high | mitigate | Execute the exact approved version from the locked development interpreter and perform no remote schema retrieval. |
</threat_model>

<verification>
- [ ] Init/default quick suite passes through `DEV_PYTHON`.
- [ ] Contract, path, and reliability suites remain green.
- [ ] D-11 through D-15 each have named behavior evidence.
</verification>

<success_criteria>
- All five assigned FOUND-04 edge assertions pass.
- Plain init, repair, invalid preservation, fixed defaults, and diagnostics match locked decisions.
- State-transition logic and filesystem reliability remain separate, connected modules.
</success_criteria>

<output>
Create `.planning/phases/BAOPS-01-harness-foundation/01-06-SUMMARY.md` when done.
</output>
