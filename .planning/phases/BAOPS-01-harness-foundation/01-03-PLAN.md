---
phase: BAOPS-01-harness-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - packages/ba-tools/src/ba_tools/__main__.py
  - packages/ba-tools/src/ba_tools/cli.py
  - packages/ba-tools/src/ba_tools/contracts.py
  - packages/ba-tools/src/ba_tools/errors.py
  - packages/ba-tools/src/ba_tools/paths.py
  - packages/ba-tools/src/ba_tools/init_command.py
  - packages/ba-tools/src/ba_tools/state/__init__.py
  - packages/ba-tools/src/ba_tools/state/atomic.py
  - packages/ba-tools/src/ba_tools/state/locking.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - NFR-03
  - NFR-04
  - NFR-05
must_haves:
  truths:
    - "No arguments, help, version, and init each emit exactly one compact UTF-8 JSON document on the designated stream."
    - "The CLI version is exactly 0.1.0 and equals both project metadata and `ba_tools.__version__`."
    - "Fresh init creates exactly `.ba-ops/config.json`, `.ba-ops/coverage-policy.json`, and `.ba-ops/business-goals.json` through one contained, bounded-lock, create-only path."
    - "A valid repeated init returns `changed:false`, preserves bytes and mtimes, and creates no inferred business content."
  artifacts:
    - path: "packages/ba-tools/src/ba_tools/__main__.py"
      provides: "Only process exit and single-emission boundary"
      contains: "def entrypoint("
    - path: "packages/ba-tools/src/ba_tools/contracts.py"
      provides: "Canonical UTF-8 serializer and envelope constructors"
      contains: "def emit_json("
    - path: "packages/ba-tools/src/ba_tools/init_command.py"
      provides: "Walking-skeleton fresh and valid-rerun orchestration"
      contains: "def run_init("
    - path: "packages/ba-tools/src/ba_tools/state/atomic.py"
      provides: "Same-directory synced create-only publication"
      contains: "def atomic_create("
    - path: "packages/ba-tools/src/ba_tools/state/locking.py"
      provides: "Five-second native workspace writer lock"
      contains: "class WorkspaceLock"
  key_links:
    - from: "packages/ba-tools/src/ba_tools/__main__.py"
      to: "packages/ba-tools/src/ba_tools/cli.py"
      via: "Click is invoked with standalone mode disabled and returns data to the sole emitter"
      pattern: "standalone_mode=False"
    - from: "packages/ba-tools/src/ba_tools/cli.py"
      to: "packages/ba-tools/src/ba_tools/init_command.py"
      via: "the init callback passes a resolved root capability and returns InitResult data"
      pattern: "run_init"
    - from: "packages/ba-tools/src/ba_tools/init_command.py"
      to: "packages/ba-tools/src/ba_tools/state/locking.py"
      via: "one workspace lock encloses all required target checks, publications, and read-back"
      pattern: "WorkspaceLock"
    - from: "packages/ba-tools/src/ba_tools/init_command.py"
      to: "packages/ba-tools/src/ba_tools/state/atomic.py"
      via: "each absent canonical target is published create-only from exact packaged bytes"
      pattern: "atomic_create"
  prohibitions:
    - statement: "A mechanical CLI success MUST NOT be represented as semantic or business correctness, and initialization MUST NOT invent goals, priorities, or stakeholder meaning."
      status: resolved
      verification: test
---

## Phase Goal

**As a** Business Analyst, **I want to** bootstrap a portable, healthy harness workspace, **so that** I can use the same deterministic workflow on every supported operating system.

<objective>
Make the RED walking-skeleton test green through a real CLI → contained path → native lock → atomic file → read-back flow.

Purpose: Prove the repository-root terminal and durable-state stack before contract, recovery, and schema matrices expand it.
Output: Executable JSON gateway, exact version/help behavior, safe fresh init, and idempotent read-back through the locked development interpreter.
</objective>

<execution_context>
@C:/Users/TungNS/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/TungNS/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/BAOPS-01-harness-foundation/SKELETON.md
@.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md
@.planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md
@.planning/phases/BAOPS-01-harness-foundation/01-PATTERNS.md
@.planning/phases/BAOPS-01-harness-foundation/01-UI-SPEC.md
@.planning/phases/BAOPS-01-harness-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the exact-version single-emission CLI boundary</name>
  <files>packages/ba-tools/src/ba_tools/__main__.py, packages/ba-tools/src/ba_tools/cli.py, packages/ba-tools/src/ba_tools/contracts.py, packages/ba-tools/src/ba_tools/errors.py</files>
  <read_first>packages/ba-tools/pyproject.toml, packages/ba-tools/src/ba_tools/__init__.py, packages/ba-tools/src/ba_tools/__main__.py, packages/ba-tools/tests/e2e/test_walking_skeleton.py, .planning/phases/BAOPS-01-harness-foundation/01-UI-SPEC.md</read_first>
  <behavior>
    - No arguments and `--help` return one success envelope on stdout with exit 0.
    - `--version` returns one success envelope whose version is the exact string `0.1.0`.
    - Parser, expected, interrupted, and unexpected failures return one safe error envelope on stderr with exit 2.
  </behavior>
  <action>Implement one non-bypassable process boundary. Invoke Click with `standalone_mode=False`; command callbacks return dictionaries or raise `BaToolsError`; only `entrypoint()` selects stdout/stderr, calls `emit_json()`, and returns 0 or 2. Use success fields `schema_version`, `ok`, `command`, `data`, `warnings`; use error fields `code`, `message`, `details`, `remediation`. Implement canonical bytes with `ensure_ascii=False`, `allow_nan=False`, compact separators, deterministic ordering, no BOM/ANSI/log prefix, and one trailing LF. Source version output from the synchronized literal `ba_tools.__version__` and assert it remains `0.1.0`. Add `--repo-root`, JSON no-args/help/version, and the `init` route without command-level printing, logging, or direct exits.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/e2e/test_walking_skeleton.py::test_cli_version_is_exactly_0_1_0 -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/e2e/test_walking_skeleton.py::test_cli_version_is_exactly_0_1_0 -q</automated>
  </verify>
  <acceptance_criteria>
    - Version invocation reports exactly 0.1.0 and matches `project.version` plus `ba_tools.__version__`.
    - Every implemented process path writes one JSON document to one stream and returns only exit 0 or 2.
    - Unexpected exceptions expose no traceback, environment value, interpreter path, home path, or absolute repository path.
    - Command callbacks contain no product-output side effect.
  </acceptance_criteria>
  <done>The exact-version CLI process boundary passes its end-to-end assertion and is ready to route init data through the same emitter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make contained locked initialization GREEN</name>
  <files>packages/ba-tools/src/ba_tools/paths.py, packages/ba-tools/src/ba_tools/init_command.py, packages/ba-tools/src/ba_tools/state/__init__.py, packages/ba-tools/src/ba_tools/state/atomic.py, packages/ba-tools/src/ba_tools/state/locking.py</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md, .planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md, packages/ba-tools/src/ba_tools/state/resources/defaults/config.json, packages/ba-tools/src/ba_tools/state/resources/defaults/coverage-policy.json, packages/ba-tools/src/ba_tools/state/resources/defaults/business-goals.json, packages/ba-tools/tests/e2e/test_walking_skeleton.py</read_first>
  <behavior>
    - Explicit repo-root text becomes a capability only after resolved containment and canonical business-path validation.
    - Fresh init publishes the three exact resources in declared order while one five-second native lock is held.
    - Read-back validates exact bytes; a complete identical rerun reports no change and performs no write.
  </behavior>
  <action>Implement `ResolvedRepoRoot` and `ResolvedBusinessPath`; reject empty, absolute, drive, UNC, backslash, NUL, dot-segment, traversal, symlink, and reparse escapes before target access. Resolve the root once and pass capabilities into state functions. Implement `WorkspaceLock` with native `filelock.FileLock` and a fixed 5.0-second production timeout per D-16 and D-19. Implement same-directory secure temp creation, binary write, flush, fsync, close, create-only publication, and parent sync where supported. In `run_init`, hold one lock across target inspection, three publications, and read-back. Per D-15, use only the packaged resources and return `.ba-ops/config.json`, `.ba-ops/coverage-policy.json`, `.ba-ops/business-goals.json` in that order. Per D-11, exact complete state returns `changed:false`, `created:[]`, and zero writes. Return only repo-relative paths and mechanical facts.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/e2e/test_walking_skeleton.py -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/e2e/test_walking_skeleton.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The original RED assertions pass without weakening.
    - Fresh init exits 0, leaves stderr empty, emits one stdout object, reports `changed:true`, and creates exactly three canonical files in fixed order.
    - Repeated init reports `changed:false`, has an empty created array, and preserves all bytes and mtimes.
    - Every mutation occurs while one bounded native workspace lock is held and uses create-only publication.
    - Invalid root/path text fails before candidate filesystem access with one safe error envelope.
  </acceptance_criteria>
  <done>The walking skeleton is green through the real package, process, path, lock, atomic publication, file read-back, and idempotent rerun path.</done>
</task>

</tasks>

## Artifacts this phase produces

- `packages/ba-tools/src/ba_tools/__main__.py` — `entrypoint(argv: list[str] | None = None) -> int`; sole process emission/exit owner.
- `packages/ba-tools/src/ba_tools/cli.py` — Click `cli`, `help_command`, `version_command`, `init_command`; options `--repo-root`, `--help`, `--version`.
- `packages/ba-tools/src/ba_tools/contracts.py` — `canonical_json_bytes`, `success_envelope`, `error_envelope`, `emit_json`.
- `packages/ba-tools/src/ba_tools/errors.py` — `BaToolsError` with fields `code`, `message`, `details`, `remediation`.
- `packages/ba-tools/src/ba_tools/paths.py` — `ResolvedRepoRoot`, `ResolvedBusinessPath`, `resolve_repo_root`, `parse_business_path`, `resolve_business_path`, `reject_symlink_or_reparse_components`.
- `packages/ba-tools/src/ba_tools/init_command.py` — `InitResult(changed, created)`, `REQUIRED_STATE_FILES`, `run_init`.
- `packages/ba-tools/src/ba_tools/state/__init__.py` — explicit exports `WorkspaceLock`, `atomic_create`.
- `packages/ba-tools/src/ba_tools/state/atomic.py` — `canonical_file_bytes`, `prepare_temp`, `atomic_create`, `sync_parent_directory`.
- `packages/ba-tools/src/ba_tools/state/locking.py` — `WorkspaceLock`, `acquire_workspace_lock`; timeout 5.0 seconds.
- Generated canonical paths `.ba-ops/config.json`, `.ba-ops/coverage-policy.json`, `.ba-ops/business-goals.json`; generated lock path `.ba-ops.lock`.
- Existing tests made green: `test_repo_root_init_walking_skeleton`, `test_repo_root_init_is_idempotent`, `test_cli_version_is_exactly_0_1_0`.
- JSON fields `schema_version`, `ok`, `command`, `data`, `warnings`, `error.code`, `error.message`, `error.details`, `error.remediation`, `data.changed`, `data.created`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Terminal arguments → CLI process | Untrusted argv and root text enter parsing and safe error mapping. |
| CLI process → repository filesystem | Untrusted path values and concurrent processes reach state publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-BAOPS-01-03-PATH | Tampering / Information Disclosure | `paths.py` and state calls | high | mitigate | Reject unsafe forms before I/O, resolve beneath one root, reject symlink/reparse components, and pass capabilities rather than raw strings. |
| T-BAOPS-01-03-RACE | Tampering / Denial of Service | Fresh init | high | mitigate | Hold one five-second native lock across all target checks, create-only writes, and read-back. |
| T-BAOPS-01-03-ERR | Information Disclosure | `entrypoint()` | medium | mitigate | Emit allowlisted fields once; map unexpected exceptions to generic safe output. |
| T-BAOPS-01-SC | Tampering | Python dependencies | high | mitigate | Execute only with the Plan 01-02 exact hash-locked development interpreter. |
</threat_model>

<verification>
- [ ] Exact version test passes through the locked interpreter.
- [ ] Walking-skeleton fresh and repeat tests pass from an external CWD and Unicode/space path.
- [ ] Generated files have exact canonical bytes and repo-relative identities.
- [ ] No source dependency or ambient interpreter is used.
</verification>

<success_criteria>
- A BA-facing terminal path now reaches durable state end to end.
- D-11, D-15, D-16, and D-19 are observable in the walking skeleton.
- Package version, CLI version, and state behavior are deterministic.
</success_criteria>

<output>
Create `.planning/phases/BAOPS-01-harness-foundation/01-03-SUMMARY.md` when done.
</output>
