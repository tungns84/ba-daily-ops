---
phase: BAOPS-01-harness-foundation
plan: 11
type: execute
wave: 9
depends_on:
  - "01-10"
files_modified: []
autonomous: false
requirements:
  - FOUND-03
  - NFR-02
  - NFR-04
must_haves:
  truths:
    - "A real Windows 10 host using Windows PowerShell Desktop 5.1 and a real macOS 12 host each complete the exact approved offline installer, generated-launcher, version, UTF-8, init, and doctor smoke."
    - "Evidence records concrete OS build/minor, architecture, Python version, PowerShell edition/version where applicable, exact commit/lock hashes, and raw exit/stream classifications."
    - "No exact-minimum support claim is verified from parser tests, emulation labels, PowerShell 7-only execution, or newer hosted runners."
  artifacts:
    - path: ".planning/phases/BAOPS-01-harness-foundation/01-11-SUMMARY.md"
      provides: "Exact Minimum OS Evidence with Windows 10/PowerShell 5.1 and macOS 12 smoke bundles"
      contains: "Exact Minimum OS Evidence"
  key_links:
    - from: "scripts/smoke_foundation.py"
      to: ".planning/phases/BAOPS-01-harness-foundation/01-11-SUMMARY.md"
      via: "the reusable offline smoke emits redacted evidence on each exact-minimum host"
      pattern: "Windows 10|PowerShell 5.1|macOS 12"
  prohibitions:
    - statement: "The project MUST NOT claim exact minimum-OS compatibility from unit tests, emulation labels, newer runners, or a `pwsh`-only Windows path."
      status: resolved
      verification: judgment
---

## Phase Goal

**As a** Business Analyst, **I want to** bootstrap a portable, healthy harness workspace, **so that** I can use the same deterministic workflow on every supported operating system.

<objective>
Obtain exact Windows 10/Windows PowerShell 5.1 and macOS 12 evidence required to close NFR-02.

Purpose: Distinguish implemented version policy and ordinary CI compatibility from verified minimum-host support.
Output: Two human-reviewed, redacted, commit-bound offline smoke evidence bundles.
</objective>

<execution_context>
@C:/Users/TungNS/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/TungNS/.cursor/gsd-core/templates/summary.md
@C:/Users/TungNS/.cursor/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md
@.planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md
@.planning/phases/BAOPS-01-harness-foundation/01-UI-SPEC.md
@.planning/phases/BAOPS-01-harness-foundation/01-10-SUMMARY.md
@scripts/smoke_foundation.py
@.github/workflows/foundation.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the complete offline path on exact minimum hosts</name>
  <files>None — execute existing smoke artifacts and record evidence in 01-11-SUMMARY.md</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md, .planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md, .planning/phases/BAOPS-01-harness-foundation/01-10-SUMMARY.md, scripts/smoke_foundation.py, install.ps1, install.sh</read_first>
  <action>Use a real Windows 10 machine or self-hosted runner and a real macOS 12 machine or self-hosted runner; labels on newer/emulated hosts do not qualify. On Windows, launch through stock `powershell.exe`, record `$PSVersionTable.PSEdition` and `$PSVersionTable.PSVersion`, and require Desktop edition 5.1 or newer; a `pwsh` run may supplement but cannot replace this evidence. On each host, record concrete OS build/minor and architecture, require Python 3.11 or newer, copy the exact Phase 1 commit into a Vietnamese path containing spaces, provide the Plan 01-01-approved offline wheelhouse, and run `scripts/smoke_foundation.py` with evidence output. Require exact package/CLI version 0.1.0, installer success, generated launcher no-args/help/version, fresh/no-op/partial/repair init, pre/post-init doctor, `doctor --all`, UTF-8 byte checks, unchanged PATH/profile/policy snapshots, and runtime network denial. Bind evidence to commit, workflow, runtime lock, and wheelhouse hashes. Store only normalized host versions, architecture, hashes, repo-relative command identities, exit codes, and stream classifications. If either host is unavailable or an assertion fails, keep the checkpoint blocked and record the precise missing evidence.</action>
  <verify>
    <automated>Windows local prerequisite: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/portability/test_supported_versions.py packages/ba-tools/tests/portability/test_terminal_states.py -q; POSIX local prerequisite: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/portability/test_supported_versions.py packages/ba-tools/tests/portability/test_terminal_states.py -q</automated>
    <human-check>Confirm one real Windows 10/Windows PowerShell Desktop 5.1 bundle and one real macOS 12 bundle, both bound to the exact commit and showing the complete offline smoke passed.</human-check>
  </verify>
  <acceptance_criteria>
    - Windows evidence identifies Windows 10 build, architecture, `PSEdition=Desktop`, PowerShell 5.1-or-newer version, and Python 3.11+.
    - macOS evidence identifies major 12, concrete minor/patch, architecture, and Python 3.11+.
    - Both use the exact commit, runtime lock, package version 0.1.0, and approved wheelhouse hashes.
    - Every generated-launcher command exits as expected and records one valid UTF-8 JSON document on the designated stream.
    - PATH/profile/policy snapshots remain unchanged and runtime network denial stays active.
    - Missing or failed evidence prevents approval; no support claim is inferred.
  </acceptance_criteria>
  <done>Both exact minimum hosts pass the complete offline bootstrap/health path, including the stock Windows PowerShell 5.1 route, and redacted evidence is recorded.</done>
  <what-built>The complete foundation with a verified ordinary six-job CI run; this checkpoint supplies exact minimum-host evidence.</what-built>
  <how-to-verify>Inspect both evidence bundles for host identity, commit/lock/wheelhouse hashes, exact version, complete smoke steps, and policy/network invariants.</how-to-verify>
  <resume-signal>Type "approved" with both evidence locations, or describe the unavailable host or failing assertion.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/BAOPS-01-harness-foundation/01-11-SUMMARY.md` — section `Exact Minimum OS Evidence`; Windows fields `os_build`, `architecture`, `powershell_edition`, `powershell_version`, `python_version`; macOS fields `os_version`, `architecture`, `python_version`; shared fields `commit_sha`, `workflow_hash`, `runtime_lock_hash`, `wheelhouse_hashes`, `package_version`, command IDs, exit codes, stream classifications, invariant results, and approval.
- No product source, class, function, flag, field, schema, launcher, workflow source, generated state path, or test function is created by this evidence-only plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External minimum-version host → project support claim | Human-supplied host identity and smoke output become release evidence. |
| Approved wheelhouse → minimum-version host | Exact executable dependency bytes enter the isolated environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-BAOPS-01-11-EVIDENCE | Spoofing / Repudiation | Exact-minimum host evidence | high | mitigate | Require concrete host/PowerShell versions, architecture, commit/lock/wheelhouse hashes, complete smoke records, and human approval. |
| T-BAOPS-01-11-LEAK | Information Disclosure | Captured evidence | medium | mitigate | Store normalized versions, hashes, repo-relative command IDs, and stream classifications; omit home/interpreter/absolute repository paths. |
| T-BAOPS-01-SC | Tampering | Offline wheelhouse | high | mitigate | Use only approved packages matching exact lock hashes and reject any version/hash mismatch before activation. |
</threat_model>

<verification>
- [ ] Local portability/UI prerequisites pass through the locked development interpreter.
- [ ] A real Windows 10/PowerShell Desktop 5.1 evidence bundle passes.
- [ ] A real macOS 12 evidence bundle passes.
- [ ] Both bundles identify exact commit, version, lock, workflow, and wheelhouse hashes.
- [ ] Summary redaction contains no absolute repository, home, runtime, or interpreter path.
</verification>

<success_criteria>
- Exact Windows 10 and macOS 12 support is evidenced rather than inferred.
- Windows support explicitly includes the stock PowerShell 5.1 path.
- The same approved offline installer/launcher/runtime path passes on both minimum hosts.
</success_criteria>

<output>
Create `.planning/phases/BAOPS-01-harness-foundation/01-11-SUMMARY.md` when done.
</output>
