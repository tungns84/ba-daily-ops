---
phase: BAOPS-01-harness-foundation
plan: 09
type: execute
wave: 7
depends_on:
  - "01-04"
  - "01-05"
  - "01-06"
  - "01-07"
  - "01-08"
files_modified:
  - scripts/smoke_foundation.py
  - packages/ba-tools/tests/portability/test_supported_versions.py
  - packages/ba-tools/tests/portability/test_terminal_states.py
  - packages/ba-tools/tests/contract/test_foundation_workflow.py
  - .github/workflows/foundation.yml
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - NFR-02
  - NFR-03
  - NFR-04
  - NFR-05
must_haves:
  truths:
    - "[EDGE NFR-02/boundary] Windows 10, macOS 12, and Python 3.11 pass at the exact floor; one version step below fails with actionable guidance; newer integer-tuple versions pass."
    - "[UI empty] Empty arrays remain arrays; empty goals use the approved copy; empty/EOF consent means No; mandatory doctor checks remain present."
    - "[UI loading] The installer shows four ordered stages while launchers/runtime commands emit no progress or partial JSON."
    - "[UI error] Failures use stable safe code/message/remediation with no traceback or absolute path; installer failure has one `Next:` action."
    - "[UI populated] Complete doctor/init/warning/detail/remediation collections preserve deterministic order without count-only summaries."
    - "[UI partial] Partial init writes nothing and points to repair; skipped doctor checks name failed prerequisites."
    - "[UI overflow] Output is not truncated, ellipsized, paginated, or hidden; JSON remains one physical line."
    - "[UI zero-one-many] Collection fields stay arrays at every cardinality."
    - "[UI long-text] Full Vietnamese text and repo-relative paths remain byte-exact."
    - "Workflow source has explicit read-only permissions, full-SHA actions, six OS/Python jobs, locked-environment commands, Windows PowerShell 5.1, and offline product smoke."
    - "Creating and statically validating workflow YAML does not claim that any remote job ran or passed."
  artifacts:
    - path: "scripts/smoke_foundation.py"
      provides: "Reusable offline install → launcher → version/init/doctor smoke and evidence bundle"
      contains: "def run_foundation_smoke("
    - path: "packages/ba-tools/tests/contract/test_foundation_workflow.py"
      provides: "Fast deterministic workflow permissions, pinning, matrix, host, and command checks"
      contains: "test_workflow_declares_read_only_permissions"
    - path: ".github/workflows/foundation.yml"
      provides: "Ordinary Windows/macOS/Linux × Python 3.11/3.14 test and offline-smoke matrix"
      contains: "permissions:"
  key_links:
    - from: ".github/workflows/foundation.yml"
      to: "scripts/smoke_foundation.py"
      via: "every matrix job creates the locked development environment and runs the same offline smoke from a Unicode path with spaces"
      pattern: "smoke_foundation.py"
    - from: ".github/workflows/foundation.yml"
      to: "packages/ba-tools/tests/contract/test_foundation_workflow.py"
      via: "fast source tests enforce read-only permissions, immutable actions, matrix coverage, PowerShell 5.1, and locked commands"
      pattern: "contents: read"
    - from: "scripts/smoke_foundation.py"
      to: "install.ps1|install.sh"
      via: "platform selection invokes one installer and then only generated root launchers"
      pattern: "install.ps1|install.sh"
  prohibitions: []
---

## Phase Goal

**As a** Business Analyst, **I want to** bootstrap a portable, healthy harness workspace, **so that** I can use the same deterministic workflow on every supported operating system.

<objective>
Create the reusable offline foundation smoke, exact boundary/UI tests, and a least-privilege ordinary three-OS CI workflow without asserting remote success.

Purpose: Provide fast workflow-source feedback and a repeatable six-job matrix whose actual conclusions are separately required by Plan 01-10.
Output: Smoke driver, portability/UI tests, workflow contract tests, and `foundation.yml`.
</objective>

<execution_context>
@C:/Users/TungNS/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/TungNS/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/BAOPS-01-harness-foundation/SKELETON.md
@.planning/phases/BAOPS-01-harness-foundation/01-CONTEXT.md
@.planning/phases/BAOPS-01-harness-foundation/01-RESEARCH.md
@.planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md
@.planning/phases/BAOPS-01-harness-foundation/01-UI-SPEC.md
@.planning/phases/BAOPS-01-harness-foundation/01-04-SUMMARY.md
@.planning/phases/BAOPS-01-harness-foundation/01-05-SUMMARY.md
@.planning/phases/BAOPS-01-harness-foundation/01-06-SUMMARY.md
@.planning/phases/BAOPS-01-harness-foundation/01-07-SUMMARY.md
@.planning/phases/BAOPS-01-harness-foundation/01-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove version boundaries and terminal states in one offline smoke</name>
  <files>scripts/smoke_foundation.py, packages/ba-tools/tests/portability/test_supported_versions.py, packages/ba-tools/tests/portability/test_terminal_states.py</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md, .planning/phases/BAOPS-01-harness-foundation/01-UI-SPEC.md, install.ps1, install.sh, installer/bootstrap.py, packages/ba-tools/src/ba_tools/doctor.py, packages/ba-tools/src/ba_tools/init_command.py, packages/ba-tools/tests/conftest.py</read_first>
  <behavior>
    - Integer-tuple predicates accept Windows 10/macOS 12/Python 3.11 and newer, and reject Windows 9/macOS 11/Python 3.10.
    - Offline smoke copies to a Vietnamese path with spaces, installs, invokes generated launchers, checks exact version 0.1.0, runs fresh/no-op/partial/repair init, then doctor/default and `--all`.
    - Integrated terminal cases cover all eight UI categories without partial output or semantic truncation.
  </behavior>
  <action>Create injected version tests for exact floors, one major below, newer major/minor, multi-digit components, and malformed text; use integer tuples only. Build `smoke_foundation.py` with options `--repo-source`, `--work-dir`, `--wheelhouse`, `--expected-platform`, and `--evidence-out`. Copy the source under a Vietnamese path containing spaces, invoke `install.ps1 --offline` or `install.sh --offline`, then invoke only generated launchers. On Windows, record and require the actual PowerShell host; the stock path uses `powershell.exe` Desktop 5.1 or newer and cannot be satisfied by a `pwsh`-only record. Parse raw bytes and validate exact version 0.1.0, no-args/help, fresh/no-op/partial/repair init, pre/post-init doctor, and `doctor --all`. Record redacted OS, architecture, Python, PowerShell, commit/lock hashes, command identities, exit codes, and stream classifications. In terminal-state tests, encode empty, loading, error, populated, partial, overflow, zero-one-many, and long-text cases, including non-TTY and `NO_COLOR`.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/portability/test_supported_versions.py packages/ba-tools/tests/portability/test_terminal_states.py -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/portability/test_supported_versions.py packages/ba-tools/tests/portability/test_terminal_states.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Exact minimum and below-minimum version cases produce the expected pass/fail and guidance.
    - Smoke runs offline and uses only generated launchers after installation.
    - Windows evidence distinguishes `powershell.exe` Desktop 5.1 from PowerShell 7.
    - All eight UI categories have named integrated tests with complete byte/data assertions.
    - Evidence contains no absolute repository, home, runtime, or interpreter path.
  </acceptance_criteria>
  <done>The local offline smoke and exact boundary/UI tests pass on the current host through the locked interpreter.</done>
</task>

<task type="auto">
  <name>Task 2: Author and statically verify the least-privilege six-job workflow</name>
  <files>packages/ba-tools/tests/contract/test_foundation_workflow.py, .github/workflows/foundation.yml</files>
  <read_first>.planning/phases/BAOPS-01-harness-foundation/01-VALIDATION.md, packages/ba-tools/requirements.lock, packages/ba-tools/requirements-dev.lock, scripts/smoke_foundation.py, .github/workflows/foundation.yml</read_first>
  <action>Create a pull-request/push `foundation` workflow with top-level `permissions: { contents: read }` and no write-capable permission. Define exactly six ordinary jobs from Windows/macOS/Linux × Python 3.11/3.14. Pin every third-party action to a full commit SHA. Each job creates `.ba-tools-runtime/dev` from `requirements-dev.lock` with hash and binary-only enforcement, installs the local project with `--no-deps --no-build-isolation`, and uses the platform-specific development interpreter for Ruff, tests, and support scripts. Build an approved wheelhouse only in the dependency setup segment; product execution remains offline and guarded. Every job runs the complete non-online suite and `smoke_foundation.py` from a Unicode path with spaces. The Windows job must invoke a dedicated `shell: powershell` step, assert Desktop edition and version at least 5.1, and run the installer through `powershell.exe`; a separate `pwsh` step may check compatibility but cannot replace it. POSIX jobs verify LF/executable behavior. Create fast source tests that parse the workflow text and assert read-only permissions, no write permission keys, six matrix combinations, Python versions, full-SHA actions, locked interpreter paths, offline smoke, and explicit stock PowerShell host. Do not state remote success in workflow creation output.</action>
  <verify>
    <automated>Windows: & ".\.ba-tools-runtime\dev\Scripts\python.exe" -m pytest packages/ba-tools/tests/contract/test_foundation_workflow.py -q; POSIX: ./.ba-tools-runtime/dev/bin/python -m pytest packages/ba-tools/tests/contract/test_foundation_workflow.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Fast workflow-source tests pass independently of the approximately 180-second full suite.
    - Top-level permissions are explicitly read-only and no job elevates them.
    - Six OS/Python combinations are present and every job uses the locked environment.
    - Third-party actions are full-SHA pinned.
    - Windows uses an explicit Windows PowerShell 5.1 path; ordinary hosted jobs do not claim exact Windows 10/macOS 12 evidence.
    - Plan completion states only that workflow source is present and statically valid; actual job results remain blocked on Plan 01-10.
  </acceptance_criteria>
  <done>The least-privilege workflow source passes fast deterministic checks and is ready to produce remote run evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/smoke_foundation.py` — `SmokeOptions(repo_source, work_dir, wheelhouse, expected_platform, evidence_out)`, `CommandEvidence(command_id, exit_code, stdout_kind, stderr_kind)`, `HostEvidence(os_version, architecture, python_version, powershell_edition, powershell_version)`, `run_command_bytes`, `assert_single_json`, `collect_host_evidence`, `run_foundation_smoke`, `write_evidence`; flags `--repo-source`, `--work-dir`, `--wheelhouse`, `--expected-platform`, `--evidence-out`.
- `packages/ba-tools/tests/portability/test_supported_versions.py` — `test_exact_minimum_versions_are_supported`, `test_below_minimum_versions_are_rejected`, `test_newer_multi_digit_versions_are_supported`, `test_malformed_versions_are_rejected`, `test_version_comparison_uses_integer_tuples`.
- `packages/ba-tools/tests/portability/test_terminal_states.py` — `test_empty_state_contract`, `test_loading_state_contract`, `test_error_state_contract`, `test_populated_state_contract`, `test_partial_state_contract`, `test_overflow_state_contract`, `test_zero_one_many_state_contract`, `test_long_text_state_contract`.
- `packages/ba-tools/tests/contract/test_foundation_workflow.py` — `test_workflow_declares_read_only_permissions`, `test_no_job_elevates_permissions`, `test_workflow_matrix_has_six_required_jobs`, `test_actions_are_full_sha_pinned`, `test_jobs_use_locked_development_interpreter`, `test_jobs_run_offline_smoke`, `test_windows_job_uses_powershell_51`.
- `.github/workflows/foundation.yml` — workflow `foundation`; top-level `contents: read`; six Windows/macOS/Linux × Python 3.11/3.14 jobs; exact locked setup, full suite, and offline smoke.
- CI path: approved dependency/wheelhouse setup → locked Ruff/tests → network-denied generated-launcher smoke → redacted evidence artifact.

## Edge and UI probe audit

- Explicit edge assertions: 26.
- Authored edge truths: 26 — 9 in 01-04, 5 in 01-05, 5 in 01-06, 2 in 01-07, 4 in 01-08, and 1 in 01-09.
- Backstop edge assertions: 0. Unresolved edge assertions: 0. Silent drops: 0.
- UI categories supplied by `01-UI-SPEC.md`: 8; all 8 are authored and tested in this plan.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | BA bootstraps a portable healthy harness on supported OSes | 01-02–01-11 | COVERED | Package, skeleton, hardening, installer, doctor, ordinary CI, and exact-minimum evidence |
| REQ | FOUND-01 | One JSON success stream / structured error stream | 01-03, 01-04, 01-09 | COVERED | Gateway, full process matrix, smoke |
| REQ | FOUND-02 | Ordered actionable doctor checks | 01-08, 01-09 | COVERED | Doctor DAG plus smoke |
| REQ | FOUND-03 | Portable one-command project-local install | 01-01, 01-02, 01-07, 01-09–01-11 | COVERED | Approved locks, exact version, installers, evidence |
| REQ | FOUND-04 | Three-file `.ba-ops/` initialization | 01-02, 01-03, 01-06, 01-09 | COVERED | Defaults, skeleton, state matrix, smoke |
| REQ | FOUND-05 | Repo-relative contained business paths | 01-03, 01-05, 01-09 | COVERED | Capability parser and adversarial evidence |
| REQ | FOUND-06 | Locking and crash-safe atomic writes | 01-03, 01-05, 01-06, 01-09 | COVERED | Skeleton plus fault/recovery hardening |
| REQ | NFR-02 | Windows 10+, macOS 12+, Linux, Python 3.11+ | 01-07–01-11 | COVERED | PowerShell floor, version predicates, ordinary CI, exact hosts |
| REQ | NFR-03 | Runtime zero-network | 01-01, 01-04, 01-07–01-09 | COVERED | Approval, guards, installer exception, offline smoke |
| REQ | NFR-04 | Full Vietnamese UTF-8 | 01-02–01-11 | COVERED | Resources, bytes, paths, installers, doctor, CI/host evidence |
| REQ | NFR-05 | No business inference | 01-02–01-04, 01-06, 01-09 | COVERED | Fixed defaults and deterministic tests |
| RESEARCH | R-01 | One application-owned JSON gateway | 01-03, 01-04 | COVERED | Sole emitter and full process matrix |
| RESEARCH | R-02 | Repo-root path capability | 01-03, 01-05 | COVERED | Grammar, resolution, ancestry, reparse checks |
| RESEARCH | R-03 | Explicit init state machine | 01-03, 01-06 | COVERED | Skeleton and complete transitions |
| RESEARCH | R-04 | Native lock, atomic publication, quarantine | 01-03, 01-05 | COVERED | One writer, create/replace split, recovery evidence |
| RESEARCH | R-05 | Versioned environment generations | 01-07 | COVERED | Build, verify exact version, atomic pointer |
| RESEARCH | R-06 | Doctor as ordered check DAG | 01-08 | COVERED | Selection, dependencies, statuses, continuation |
| RESEARCH | R-07 | Package legitimacy and hash locks | 01-01, 01-02, 01-04 | COVERED | Target approval, exact lock equality, permanent tests |
| RESEARCH | R-08 | Installer/runtime zero-network boundary | 01-04, 01-07–01-09 | COVERED | Runtime guard and installer-only exception |
| RESEARCH | R-09 | Canonical UTF-8 deterministic JSON | 01-02–01-04, 01-09 | COVERED | Resources, serializer, byte tests, smoke |
| RESEARCH | R-10 | Nyquist tests and OS/Python matrix | 01-02–01-11 | COVERED | Locked test environment, CI source/run, exact hosts |
| RESEARCH | R-11 | Bundled schemas and fixed defaults | 01-02, 01-06 | COVERED | Three resources and three local schemas |
| RESEARCH | R-12 | Prerequisite guidance and explicit consent | 01-07 | COVERED | Thin wrappers and exact terminal contract |
| RESEARCH | R-13 | Safe redacted actionable errors | 01-03, 01-04, 01-06–01-09 | COVERED | Typed envelopes and no-leak tests |
| CONTEXT | D-01 | Isolated project-local environment | 01-07 | COVERED | `.ba-tools-runtime/envs/` |
| CONTEXT | D-02 | Generated Windows/POSIX root launchers | 01-07 | COVERED | `ba-tools.ps1` and `ba-tools` |
| CONTEXT | D-03 | Root launcher, not persistent PATH | 01-07, 01-09 | COVERED | No PATH mutation; launcher smoke |
| CONTEXT | D-04 | No automatic Python/Git installation | 01-07 | COVERED | Early actionable prerequisite failure |
| CONTEXT | D-05 | Explicit dependency-download consent | 01-01, 01-07 | COVERED | Approval plus invocation consent |
| CONTEXT | D-06 | Exact repository pins and explicit upgrade | 01-01, 01-02, 01-03, 01-07 | COVERED | Approved locks and synchronized version 0.1.0 |
| CONTEXT | D-07 | Core fail versus optional warning | 01-08 | COVERED | Severity and exit policy |
| CONTEXT | D-08 | Default scope plus `doctor --all` | 01-08 | COVERED | Registry selection |
| CONTEXT | D-09 | Doctor pre- and post-init | 01-08 | COVERED | State-dependent checks |
| CONTEXT | D-10 | Continue independent checks / skip dependents | 01-08 | COVERED | DAG execution |
| CONTEXT | D-11 | Valid rerun returns `changed:false` | 01-03, 01-06 | COVERED | Byte/mtime no-op |
| CONTEXT | D-12 | Plain init rejects partial state | 01-06 | COVERED | `STATE_INCOMPLETE` |
| CONTEXT | D-13 | Repair creates missing only | 01-06 | COVERED | Create-only publication |
| CONTEXT | D-14 | Preserve invalid existing JSON | 01-06 | COVERED | Complete diagnostics, zero mutation |
| CONTEXT | D-15 | Light fixed defaults / empty goals | 01-02, 01-03, 01-06 | COVERED | Exact resources and schemas |
| CONTEXT | D-16 | Bounded lock wait | 01-03, 01-05 | COVERED | Five-second timeout |
| CONTEXT | D-17 | Safe stale-owner recovery | 01-05 | COVERED | Native lock plus dead-process proof |
| CONTEXT | D-18 | Quarantine abandoned temp; no promotion | 01-05 | COVERED | Preserved recovery evidence |
| CONTEXT | D-19 | One workspace-wide write lock | 01-03, 01-05, 01-06 | COVERED | All `.ba-ops/` mutations serialized |

Excluded by phase boundary: REQ-ID/SRS authoring, workflow runner, artifact generation, trace/INDEX semantics, plugins, profile behavior beyond the fixed light defaults, team mode, SaaS/backend, and renderer implementation.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub Actions dependencies/setup → tested source | Hosted runner actions and package setup execute before product tests. |
| Workflow permissions → repository resources | A workflow token must remain explicitly read-only. |
| Hosted platform → support claim | Ordinary runners provide compatibility evidence, not exact minimum-OS proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-BAOPS-01-09-CI | Tampering / Elevation of Privilege | GitHub Actions workflow | high | mitigate | Declare `permissions: contents: read`, forbid job elevation, pin full SHAs, use exact locks, and enforce all properties in fast source tests. |
| T-BAOPS-01-09-NET | Information Disclosure | CI product smoke | high | mitigate | Permit network only for approved wheelhouse setup and execute product commands offline under runtime guards. |
| T-BAOPS-01-09-CLAIM | Repudiation | Platform evidence | medium | mitigate | Separate workflow-source completion from actual run evidence and reserve exact-minimum claims for Plan 01-11. |
| T-BAOPS-01-SC | Tampering | CI dependencies | high | mitigate | Use only approved exact locks and local package installation without dependency resolution. |
</threat_model>

<verification>
- [ ] Fast workflow contract test passes in seconds and checks read-only permissions.
- [ ] Portability/UI tests pass through `DEV_PYTHON`.
- [ ] Full local gate: Windows uses the locked development interpreter to run `-m pytest packages/ba-tools/tests -q -m "not online_install"` and `-m ruff check packages/ba-tools/src packages/ba-tools/tests installer scripts`; POSIX runs the same modules through `.ba-tools-runtime/dev/bin/python`.
- [ ] No statement claims remote matrix success; Plan 01-10 remains required.
- [ ] Source audit covers GOAL, all ten requirement IDs, R-01 through R-13, and D-01 through D-19 with no missing row.
</verification>

<success_criteria>
- Workflow source is least-privilege, immutable-action-pinned, target-complete, and statically verified.
- The complete local suite remains the final local gate while fast feedback is available for workflow edits.
- All 26 edge assertions and eight UI categories are authored with no silent drop.
- Actual remote CI conclusions are delegated only to the blocking evidence plan.
</success_criteria>

<output>
Create `.planning/phases/BAOPS-01-harness-foundation/01-09-SUMMARY.md` when done.
</output>
