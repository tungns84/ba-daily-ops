---
phase: 02-ba-srs-analyze-quality-gate-traceability-core
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - .agents/skills/ba-srs-analyze/SKILL.md
  - .agents/skills/ba-srs-analyze/agents/openai.yaml
  - .agents/ba-daily-operators/ba-core/workflows/ba-srs-analyze.md
  - .agents/ba-daily-operators/ba-core/agents/ba-srs-writer.md
  - .agents/ba-daily-operators/ba-core/agents/ba-critic.md
  - .agents/ba-daily-operators/ba-core/references/gates.md
  - .agents/ba-daily-operators/ba-tools/tests/test_skill_schema.py
  - .agents/ba-daily-operators/ba-tools/tests/test_workflow_contract.py
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/srs/critic-independence/requirements.json
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/srs/critic-independence/analysis.md
autonomous: true
requirements: [SRS-02, SRS-05, SRS-06, GATE-01, CDX-01, CDX-02, CDX-03]
must_haves:
  truths:
    - "ba-srs-analyze ships as flat .agents/skills/ba-srs-analyze/SKILL.md with frontmatter keys exactly {name, description} (CDX-01)"
    - "agents/openai.yaml carries interface.{display_name,short_description,default_prompt} and policy.allow_implicit_invocation: false (CDX-02); the default_prompt is skill-native wording (e.g. 'Use the ba-srs-analyze workflow, route full, on <path>'), NOT a nonexistent shell command like `ba-srs-analyze --route full` (review: Codex HIGH — no fake CLI)"
    - "the operator is runnable/testable, not just documented: `ba-tools resolve-route ba-srs-analyze` returns `full` (already registered in Phase-1 resolve_route.py DEFAULT_ROUTES + init_cmd.py OPERATOR_ROUTES) and the workflow's route table matches that registration — verified by test_resolve_route_full + test_workflow_routes_match_registration (review: Codex/OpenCode HIGH — route plumbing)"
    - "the workflow file format is pinned: Markdown with a YAML frontmatter block (keys: operator, default_route, routes) followed by a structured per-route step list — test_workflow_contract.py parses the frontmatter and asserts the schema (review: OpenCode HIGH — workflow format unspecified)"
    - "the thin workflow resolves route → workflow step → follows it for extract/draft/lint/verify/full/iterate (default full) (CDX-03, SRS-06, D-15/D-16)"
    - "the `extract` route accepts arbitrary `.md`/`.txt` source docs and splits them via the `ba-tools extract-uc` command when the source is UC-shaped, else the agent performs generic section extraction over the markdown_sections boundaries the workflow names (D-17) — the workflow makes explicit that generic non-UC extraction is agent-owned (no generic `ba-tools extract` command exists in v1), gating extract-uc reuse on a UC-shape check (review: Codex MEDIUM — generic extract has no CLI)"
    - "Codex v1 executes the writer→critic loop as a SINGLE sequential agent loop within the operator (no true subagent spawn); the workflow phrases each agent step as a sequential workflow section with enforced context discipline, not a parallel 'spawn' (review: Codex+OpenCode MEDIUM — no autonomous spawn in v1)"
    - "the full route runs the Quality gate (ba-tools verify then ba-critic CoVe loop ≤3) and only on convergence runs trace write + index update (GATE-01, D-10)"
    - "the ba-critic payload is {source_path, requirements.json path} ONLY — analysis.md is never passed (D-21, CoVe independence); a static test asserts the workflow's critic step manifest excludes analysis.md (review: Codex/OpenCode MEDIUM — static critic-payload assertion)"
    - "ba-critic emits findings as a JSON array of {req_id, severity (fail|warn), question, answer, verdict} plus a {converged} signal, so the workflow can count FAIL-severity findings deterministically (review: Codex/OpenCode MEDIUM — critic output schema)"
    - "after 3 loops still failing the workflow escalates to a Confirm checkpoint and never silent-passes; convergence is logged 'passed early' vs 'passed after N' (D-10/D-11)"
  prohibitions:
    - "no frontmatter field beyond name+description in SKILL.md (Codex silently truncates extras)"
    - "the openai.yaml default_prompt never instructs the user to run a nonexistent `ba-srs-analyze` shell command (it is a Codex skill invocation, skill-native wording only)"
    - "allow_implicit_invocation is never true and never placed at YAML top level (must be nested under policy)"
    - "the workflow never passes raw artifact content between agents — paths + a small manifest only (DESIGN §4)"
    - "the critic step manifest never includes analysis.md or the writer's rationale path"
    - "the workflow never describes the writer/critic loop as a real parallel subagent spawn (Codex v1 = one sequential agent loop)"
    - "no eager-loaded doc (SKILL.md / workflow / referenced eager files) is ≥ 32,768 B; DEFAULT workflow < 38,000 B"
  artifacts:
    - path: ".agents/skills/ba-srs-analyze/SKILL.md"
      provides: "Codex skill discovery index"
      contains: "name"
    - path: ".agents/skills/ba-srs-analyze/agents/openai.yaml"
      provides: "interface + policy contract"
      contains: "allow_implicit_invocation"
    - path: ".agents/ba-daily-operators/ba-core/workflows/ba-srs-analyze.md"
      provides: "thin route-dispatch + CoVe loop control"
      contains: "full"
    - path: ".agents/ba-daily-operators/ba-core/agents/ba-srs-writer.md"
      provides: "writer agent prompt"
    - path: ".agents/ba-daily-operators/ba-core/agents/ba-critic.md"
      provides: "critic agent prompt (read-only, source-only, JSON findings schema)"
    - path: ".agents/ba-daily-operators/ba-tools/tests/test_workflow_contract.py"
      provides: "static workflow-format + route-registration + critic-payload tests"
  key_links:
    - from: ".agents/skills/ba-srs-analyze/SKILL.md + openai.yaml"
      to: ".agents/ba-daily-operators/ba-core/workflows/ba-srs-analyze.md"
      via: "skill points at the workflow file; default_prompt is skill-native 'use the workflow, route full' (not a fake CLI)"
      pattern: "ba-srs-analyze"
    - from: "ba-tools resolve-route ba-srs-analyze (Phase-1 DEFAULT_ROUTES)"
      to: "workflow route table default_route"
      via: "the registered default 'full' matches the workflow frontmatter default_route — operator is resolvable/testable"
      pattern: "resolve-route|default_route"
    - from: "workflow full route"
      to: "ba-tools verify / render / trace write / index update"
      via: "deterministic gate + render + trace + index invoked by paths, after CoVe convergence"
      pattern: "ba-tools (verify|render|trace|index)"
    - from: "workflow critic step"
      to: "ba-critic.md"
      via: "sequential agent step given {source_path, requirements.json path} only — analysis.md excluded (static test asserts)"
      pattern: "ba-critic"
---

<objective>
Create the first operator skill in this repo and wire the Quality gate
orchestration. This establishes the SKILL.md / openai.yaml / workflow /
agent-prompt layout every later operator copies. Four greenfield surfaces:

1. Codex skill — flat `.agents/skills/ba-srs-analyze/SKILL.md` (frontmatter
   `name`+`description` ONLY) + `agents/openai.yaml`
   (`interface.*` + `policy.allow_implicit_invocation: false`) (CDX-01/CDX-02).
2. Thin workflow — `ba-core/workflows/ba-srs-analyze.md` resolving the six
   routes (extract/draft/lint/verify/full/iterate, default full) to steps,
   passing paths-not-content, driving the CoVe loop (CDX-03, SRS-06, D-15/D-16).
3. Agent prompts — `ba-srs-writer.md` (emits requirements.json + analysis.md,
   verbatim spans, FR/NFR/BR) and `ba-critic.md` (source-only re-derivation,
   read-only, never reads analysis.md) (SRS-02, SRS-05, D-21).
4. Quality gate contract — `ba-core/references/gates.md` documenting
   verify→critic→trace+index, ≤3 loops, escalate-to-Confirm (GATE-01, D-10/D-11).

Purpose: Bind the deterministic CLI (plans 02-03) to the agent CoVe loop so the
end-to-end differentiator works: source → grounded requirements → gated artifact.
Output: 5 skill/workflow/agent/reference files; the live skill-schema test; the
new `test_workflow_contract.py`; the F11 critic-independence fixture.

Review feedback incorporated (REVIEWS.md, this plan):
- **OpenCode HIGH — workflow file format pinned.** The workflow is Markdown with a
  YAML frontmatter block (`operator`, `default_route`, `routes`) + a structured
  per-route step list. `test_workflow_contract.py` parses and asserts the schema.
- **Codex/OpenCode HIGH — operator is runnable, not just documented.** Route
  registration ALREADY EXISTS from Phase 1: `resolve_route.py` DEFAULT_ROUTES has
  `ba-srs-analyze → full` and `init_cmd.py` OPERATOR_ROUTES lists all six routes.
  This plan adds tests asserting `ba-tools resolve-route ba-srs-analyze` returns
  `full` and that the workflow's route table matches that registration — no new
  registration code needed, the prior Codex concern is resolved by Phase-1 fact.
- **Codex HIGH — no fake CLI in the default prompt.** `openai.yaml`'s
  `default_prompt` uses skill-native wording ("Use the ba-srs-analyze workflow,
  route full, on <path>"), never a nonexistent `ba-srs-analyze --route full` shell
  command.
- **Codex/OpenCode MEDIUM — critic output schema.** `ba-critic.md` emits a JSON
  array of `{req_id, severity (fail|warn), question, answer, verdict}` + a
  `{converged}` signal so the workflow counts FAILs deterministically.
- **Codex/OpenCode MEDIUM — Codex v1 is sequential.** The workflow phrases the
  writer→critic loop as ONE sequential agent loop with context discipline, not a
  parallel subagent spawn.
- **Codex/OpenCode MEDIUM — static critic-payload assertion.** A static test
  asserts the workflow's critic step manifest excludes `analysis.md`.
- **Codex MEDIUM — generic extract is agent-owned.** The workflow makes explicit
  that non-UC generic section extraction is agent-owned (there is no generic
  `ba-tools extract` command in v1; `extract-uc` is reused only for UC-shaped
  sources).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/02-ba-srs-analyze-quality-gate-traceability-core/02-CONTEXT.md
@.planning/phases/02-ba-srs-analyze-quality-gate-traceability-core/02-AI-SPEC.md
@.planning/phases/02-ba-srs-analyze-quality-gate-traceability-core/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces (others in plans 01/02/03):
- **New skill dir (greenfield):** `.agents/skills/ba-srs-analyze/SKILL.md`,
  `.agents/skills/ba-srs-analyze/agents/openai.yaml`.
- **New ba-core dir (greenfield):**
  `.agents/ba-daily-operators/ba-core/workflows/ba-srs-analyze.md`,
  `.agents/ba-daily-operators/ba-core/agents/ba-srs-writer.md`,
  `.agents/ba-daily-operators/ba-core/agents/ba-critic.md`,
  `.agents/ba-daily-operators/ba-core/references/gates.md`.
- **Workflow file format (pinned):** Markdown + YAML frontmatter (`operator`,
  `default_route`, `routes`) + a structured per-route step list.
- **Critic output schema:** JSON array of `{req_id, severity, question, answer,
  verdict}` + `{converged}` signal.
- **Live test:** `tests/test_skill_schema.py` skip markers removed; validators
  run against the now-existing skill files.
- **New test:** `tests/test_workflow_contract.py` — parses the workflow
  frontmatter (format schema), asserts the route table matches the Phase-1
  `resolve-route ba-srs-analyze → full` registration, and statically asserts the
  critic step manifest excludes `analysis.md`.
- **Fixture:** F11 `critic-independence` (`requirements.json` + a planted
  `analysis.md` the critic must not consult).
- **Routes referenced (registered in Phase-1):** `extract`, `draft`, `lint`,
  `verify`, `full` (default), `iterate`. **Payload shapes:** writer
  `{source_path, sections_dir, slug, route}`; critic
  `{source_path, requirements_json_path}`.
- **Convergence log vocabulary:** `passed early`, `passed after N`,
  `non-convergence-escalation`.
- **Existing symbols this plan binds to (NOT created here — Phase 1):**
  `resolve_route.py::DEFAULT_ROUTES["ba-srs-analyze"]="full"`,
  `init_cmd.py::OPERATOR_ROUTES["ba-srs-analyze"]`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Codex skill (SKILL.md + openai.yaml) + live skill-schema test + byte-check</name>
  <files>.agents/skills/ba-srs-analyze/SKILL.md, .agents/skills/ba-srs-analyze/agents/openai.yaml, .agents/ba-daily-operators/ba-tools/tests/test_skill_schema.py</files>
  <read_first>
    - DESIGN.md §3 (skill / openai.yaml contract, flat layout)
    - .planning/phases/02-ba-srs-analyze-quality-gate-traceability-core/02-PATTERNS.md:292-334 (exact SKILL.md + openai.yaml structures, the policy-nesting constraint)
    - CLAUDE.md (verified Codex skill contract: frontmatter name+description only; interface.*/policy.* nesting; 32,768 B truncation)
    - .agents/ba-daily-operators/ba-tools/tests/test_skill_schema.py (the skipped validators from plan 01 to flip live)
    - .agents/ba-daily-operators/ba-tools/ba_tools/commands/byte_check.py (the 32768 B limit semantics — strict less-than)
  </read_first>
  <action>
    Create `.agents/skills/ba-srs-analyze/SKILL.md` with YAML frontmatter containing EXACTLY two keys — `name: ba-srs-analyze` and a keyword-dense `description` naming the routes (extract|draft|lint|verify|full|iterate) and the trigger phrases — and NO other frontmatter field. Add a short HTML-comment body pointing at the workflow file path; the body is a discovery index, not logic. Create `.agents/skills/ba-srs-analyze/agents/openai.yaml` with `interface.display_name`, `interface.short_description`, `interface.default_prompt`, and `policy.allow_implicit_invocation: false` — allow_implicit_invocation nested under `policy`, never at top level, never true. CRITICAL (review Codex HIGH): `default_prompt` is SKILL-NATIVE wording that instructs the agent to follow the workflow, e.g. "Use the ba-srs-analyze workflow on the given source document, route `full` (resolve via `ba-tools resolve-route ba-srs-analyze`)." — it must NOT read like a runnable shell command (`ba-srs-analyze --route full ...` is a fake CLI; no such binary exists). It MAY reference the real `ba-tools resolve-route ba-srs-analyze` command as the route source. In `tests/test_skill_schema.py` remove the plan-01 skip markers so the frontmatter-keys validator asserts the SKILL.md key set equals `{"name","description"}` and the openai.yaml validator asserts `interface` has the three keys and `policy.allow_implicit_invocation is False`; add an assertion that `default_prompt` does NOT contain the substring `ba-srs-analyze --route` (no fake CLI). Keep every eager-loaded file under 32,768 B. Do not edit the JSON envelope or any ba_tools code.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && python -m pytest tests/test_skill_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_skill_schema.py` passes (no longer skipped): SKILL.md frontmatter key set is exactly `{name, description}`; openai.yaml has `interface.display_name`/`short_description`/`default_prompt` and `policy.allow_implicit_invocation` is `false`.
    - `.agents/skills/ba-srs-analyze/SKILL.md` and `agents/openai.yaml` exist at the flat paths.
    - `default_prompt` uses skill-native wording and does NOT contain the literal `ba-srs-analyze --route` (asserted by the test — no fake CLI per Codex HIGH); it may reference `ba-tools resolve-route ba-srs-analyze`.
    - `ba-tools byte-check --file .agents/skills/ba-srs-analyze/SKILL.md` reports under the 32,768 B limit (exit 0).
    - No frontmatter key other than name/description appears in SKILL.md.
  </acceptance_criteria>
  <done>The first Codex skill exists with the verified contract and a skill-native (non-fake-CLI) default prompt; the skill-schema test enforces it.</done>
</task>

<task type="auto">
  <name>Task 2: Thin workflow (pinned format) + ba-srs-writer + ba-critic prompts + gates.md + workflow-contract tests + F11 fixture</name>
  <files>.agents/ba-daily-operators/ba-core/workflows/ba-srs-analyze.md, .agents/ba-daily-operators/ba-core/agents/ba-srs-writer.md, .agents/ba-daily-operators/ba-core/agents/ba-critic.md, .agents/ba-daily-operators/ba-core/references/gates.md, .agents/ba-daily-operators/ba-tools/tests/test_workflow_contract.py, .agents/ba-daily-operators/ba-tools/tests/fixtures/srs/critic-independence/requirements.json, .agents/ba-daily-operators/ba-tools/tests/fixtures/srs/critic-independence/analysis.md</files>
  <read_first>
    - DESIGN.md §4 (thin orchestrator, route table, "pass paths not content"), §6 (three gates, ba-critic CoVe definition)
    - .planning/phases/02-ba-srs-analyze-quality-gate-traceability-core/02-AI-SPEC.md (§3 Entry Point Pattern workflow skeleton, §4 CoVe loop pseudocode, §4b prompt discipline, §6 guardrails G1-G4)
    - .planning/phases/02-ba-srs-analyze-quality-gate-traceability-core/02-CONTEXT.md (D-14 IEEE-830, D-15/D-16 routes, D-21 payloads, D-09 prefixes, D-10/D-11 convergence)
    - .agents/ba-daily-operators/ba-tools/ba_tools/commands/resolve_route.py:13 (DEFAULT_ROUTES — `ba-srs-analyze: full` already registered in Phase 1; the workflow default_route MUST match this)
    - .agents/ba-daily-operators/ba-tools/ba_tools/commands/init_cmd.py:26 (OPERATOR_ROUTES — the six routes already registered; the workflow route table MUST match this list)
    - .agents/ba-daily-operators/ba-tools/tests/fixtures/srs/non-convergence-escalate/requirements.json (F12 skeleton from plan 03 — referenced by the escalation contract)
  </read_first>
  <behavior>
    - test_workflow_frontmatter_schema: the workflow file begins with a YAML frontmatter block whose keys are `operator`, `default_route`, `routes`; `operator == "ba-srs-analyze"`, `default_route == "full"`, `routes` is a list containing the six route names.
    - test_resolve_route_full: `ba-tools resolve-route ba-srs-analyze` returns `default_route: "full"` (asserts the Phase-1 registration the operator depends on actually resolves).
    - test_workflow_routes_match_registration: the workflow frontmatter `routes` list equals `init_cmd.OPERATOR_ROUTES["ba-srs-analyze"]` and `default_route` equals `resolve_route.DEFAULT_ROUTES["ba-srs-analyze"]`.
    - test_critic_payload_excludes_analysis (review MEDIUM): a static parse of the workflow's `full`-route critic step finds the critic manifest keys are exactly `{source_path, requirements_json_path}` and `analysis.md` does NOT appear in any critic step manifest.
    - test_no_fake_subagent_spawn: the workflow does not describe the loop as a true parallel subagent spawn (it is phrased as a sequential agent loop; assert the workflow text does not promise concurrent agents).
  </behavior>
  <action>
    Create `.agents/ba-daily-operators/ba-core/workflows/ba-srs-analyze.md` in a PINNED format (review OpenCode HIGH): a YAML frontmatter block at the top of the file with keys `operator: ba-srs-analyze`, `default_route: full`, `routes: [extract, draft, lint, verify, full, iterate]` (this list and default MUST match the Phase-1 `init_cmd.OPERATOR_ROUTES` / `resolve_route.DEFAULT_ROUTES` registration), followed by a Markdown body with one `## Route: <name>` section per route, each containing an ordered numbered step list. Keep the file < 38,000 B. Each step either names a real `ba-tools` command (`resolve-route`, `extract-uc` for UC-shaped sources, `verify --reqs-format json`, `render srs`, `render registry`, `trace write`, `index update`, `state` patch/advance, `confirm`) or hands off to an agent with a paths-only manifest written as an explicit key list. Phrase the writer/critic handoffs as SEQUENTIAL workflow sections, not parallel spawns (review MEDIUM: Codex v1 runs one sequential agent loop — say "run the writer step, then the critic step" and never imply concurrent subagents). For the `extract` route, state that UC-shaped sources use `ba-tools extract-uc` and that generic (non-UC) section extraction is AGENT-OWNED over the document's markdown_sections boundaries — there is no generic `ba-tools extract` command in v1 (review Codex MEDIUM). Encode the `full` route per AI-SPEC §3/§4: resolve route+slug → extract → run ba-srs-writer with `{source_path, sections_dir, slug, route}` → `ba-tools render srs` → `ba-tools verify` (exit 2 folds failures back to a re-draft) → CoVe loop n in 1..3 running ba-critic with `{source_path, requirements_json_path}` ONLY, converged = zero new FAIL findings (log "passed early"/"passed after N"), re-draft via ba-srs-writer between loops → on loop-3-still-failing call the Confirm gate (log "non-convergence-escalation", never silent pass) → on convergence `ba-tools trace write` + `ba-tools index update` + state advance. Encode `iterate` per D-16. Create `ba-srs-writer.md`: role = emit canonical `requirements.json` (`{id,statement,status,source_trace}` with verbatim ≥12-char spans, FR/NFR/BR prefixes per D-09, atomic single-obligation statements, derived reqs carry no source_trace) + `analysis.md` rationale; include the three inline exemplars (one stated+span, one derived, one rejected paraphrase). Create `ba-critic.md`: role = independent CoVe re-derivation from the source ONLY; explicitly forbid reading analysis.md or any writer rationale; generate one verification question per requirement, answer from source alone, and emit findings as a JSON array of objects `{req_id, severity: "fail"|"warn", question, answer, verdict}` plus a top-level `{converged: bool}` signal (review MEDIUM: pin the critic output schema so the workflow counts FAILs deterministically); read-only (never edits). Create `ba-core/references/gates.md` documenting the Quality gate contract (verify deterministic hard block → critic CoVe ≤3 → trace+index; escalate-to-Confirm via `ba-tools confirm` on non-convergence; WARN non-blocking). Create `tests/test_workflow_contract.py` with the five tests in the behavior block, parsing the workflow frontmatter with stdlib and importing `resolve_route.DEFAULT_ROUTES` / `init_cmd.OPERATOR_ROUTES` for the match assertions. Build the F11 `critic-independence` fixture: a `requirements.json` plus an `analysis.md` containing a planted writer-only rationale that, if the critic read it, would change the verdict. Keep all eager files under the byte budgets.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && python -m pytest tests/test_skill_schema.py tests/test_workflow_contract.py -q && python -m ba_tools byte-check --file ../ba-core/workflows/ba-srs-analyze.md</automated>
  </verify>
  <acceptance_criteria>
    - All files exist at their flat paths: workflow, ba-srs-writer.md, ba-critic.md, gates.md, test_workflow_contract.py, and the F11 fixture pair.
    - `test_workflow_frontmatter_schema` passes: the workflow has a YAML frontmatter block with `operator`/`default_route`/`routes`, `default_route == "full"`, and the six route names present.
    - `test_resolve_route_full` passes: `ba-tools resolve-route ba-srs-analyze` returns `full`.
    - `test_workflow_routes_match_registration` passes: workflow `routes` == `init_cmd.OPERATOR_ROUTES["ba-srs-analyze"]` and `default_route` == `resolve_route.DEFAULT_ROUTES["ba-srs-analyze"]`.
    - `test_critic_payload_excludes_analysis` passes: the critic step manifest is exactly `{source_path, requirements_json_path}` and `analysis.md` appears in no critic step manifest.
    - The `full` route's step list includes `ba-tools verify`, a `ba-critic` CoVe loop with a ≤3 bound, a Confirm-escalation branch, and `ba-tools trace write` + `index update` only after convergence.
    - `ba-critic.md` text states the payload is `{source_path, requirements_json_path}` only, forbids reading `analysis.md`/writer rationale, and specifies the JSON findings schema `{req_id, severity, question, answer, verdict}` + `{converged}`.
    - `ba-srs-writer.md` requires verbatim ≥12-char spans, FR/NFR/BR prefixes (D-09), atomicity, and that `derived` requirements carry no `source_trace`.
    - The workflow phrases the writer/critic loop sequentially (no parallel-spawn promise); `extract` names `extract-uc` for UC sources and marks generic extraction agent-owned.
    - `gates.md` documents verify→critic→trace+index, ≤3 loops, escalate-to-Confirm, WARN non-blocking.
    - `python -m ba_tools byte-check --file ../ba-core/workflows/ba-srs-analyze.md` exits 0 (workflow < 32,768 B eager limit, also < 38,000 B DEFAULT tier).
    - F11 `critic-independence/` contains `requirements.json` + `analysis.md` with a planted writer-only rationale.
  </acceptance_criteria>
  <done>The workflow is pinned (frontmatter format), its routes match the Phase-1 registration (operator is resolvable/testable), the critic emits a JSON findings schema and never receives analysis.md (static test), the loop is sequential (Codex v1), and the writer/critic/gates contracts are encoded; F11 + workflow-contract tests exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source document content → ba-srs-writer / ba-critic | external/untrusted source text crosses into agent prompts; verbatim spans are gated downstream by ba-tools verify |
| eager-loaded skill/workflow/refs → Codex loader | files ≥ 32,768 B are silently truncated, corrupting the contract |
| writer rationale (analysis.md) → critic | passing it collapses CoVe independence (Failure mode #2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | external source doc fed to the writer (prompt-injection in source text) | accept (advisory v1) | WARN_INJECTION scan is advisory in v1 (Open Decision #2); `ba-tools scan` flags but does not block; promote to hard gate for external `stated` reqs in a later milestone |
| T-02-13 | Repudiation / Info Disclosure | CoVe independence collapse via analysis.md leak | mitigate | critic payload is paths-only `{source_path, requirements_json_path}`; prompt forbids reading analysis.md; F11 fixture validates; G3 guardrail |
| T-02-14 | Elevation of Privilege | skill auto-firing without explicit invocation | mitigate | `policy.allow_implicit_invocation: false` (tested in Task 1); default route is the static `full`, never inferred from free text |
| T-02-15 | Denial of Service | eager doc ≥ 32,768 B silently truncated → broken contract | mitigate | byte-check the SKILL.md + workflow in acceptance criteria; keep workflow < 38,000 B DEFAULT tier |
| T-02-16 | Tampering | silent auto-pass on loop 3 with open FAILs | mitigate | workflow escalates to Confirm checkpoint and logs `non-convergence-escalation`; never emits converged with open FAILs (D-10, G2); F12 skeleton validates |
| T-02-17 | Tampering | unrunnable operator — workflow route table drifts from the resolve-route registration (review HIGH) | mitigate | test_workflow_routes_match_registration asserts the workflow routes/default equal the Phase-1 DEFAULT_ROUTES/OPERATOR_ROUTES; test_resolve_route_full asserts `resolve-route ba-srs-analyze` returns `full` |
| T-02-18 | Repudiation | default_prompt naming a fake `ba-srs-analyze` CLI misleads the operator into running a nonexistent binary (review HIGH) | mitigate | skill-native default_prompt; test asserts it lacks the `ba-srs-analyze --route` literal and references the real `ba-tools resolve-route` |
| T-02-SC | Tampering | npm/pip installs | mitigate | zero new dependencies this phase (D-18); nothing to install |
</threat_model>

<verification>
- `cd .agents/ba-daily-operators/ba-tools && python -m pytest tests/test_skill_schema.py tests/test_workflow_contract.py -q` passes (live, no skips).
- SKILL.md frontmatter has only `name`+`description`; openai.yaml nests `interface.*` + `policy.allow_implicit_invocation: false`; default_prompt is skill-native (no fake CLI literal).
- The workflow has a pinned YAML frontmatter format; its `routes`/`default_route` match the Phase-1 registration; `ba-tools resolve-route ba-srs-analyze` returns `full`.
- The full route runs verify → ba-critic CoVe ≤3 → escalate-to-Confirm → trace write + index update, phrased as a sequential agent loop.
- `ba-critic.md` payload is source + requirements.json only (static test asserts analysis.md excluded); critic findings use the `{req_id, severity, question, answer, verdict}` + `{converged}` schema.
- `python -m ba_tools byte-check` reports SKILL.md and the workflow under the byte limits.
</verification>

<success_criteria>
- ROADMAP SC-5: the skill ships flat with the verified frontmatter/openai.yaml contract and a thin pinned-format workflow whose route table matches the resolvable Phase-1 registration (operator is runnable/testable, not just documented).
- ROADMAP SC-2 (the orchestration half): the Quality gate runs `ba-tools verify` then `ba-critic` (JSON findings schema), ≤3 loops, early-exit-on-convergence logged, escalate-to-Confirm on non-convergence, sequential agent loop (Codex v1).
- SRS-02/SRS-05/SRS-06: writer drafts grounded requirements; critic re-derives independently with analysis.md statically excluded; six routes exposed (default full).
- CDX-01/02/03 satisfied; the operator-skill convention later operators copy is established.
</success_criteria>

<output>
Create `.planning/phases/02-ba-srs-analyze-quality-gate-traceability-core/02-04-SUMMARY.md` when done
</output>