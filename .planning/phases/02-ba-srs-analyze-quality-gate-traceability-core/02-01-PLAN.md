---
phase: 02-ba-srs-analyze-quality-gate-traceability-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/ba-daily-operators/ba-tools/ba_tools/lint.py
  - .agents/ba-daily-operators/ba-tools/ba_tools/scaffold.py
  - .agents/ba-daily-operators/ba-tools/tests/test_lint_reqs.py
  - .agents/ba-daily-operators/ba-tools/tests/test_smoke.py
  - .agents/ba-daily-operators/ba-tools/tests/test_skill_schema.py
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/srs/renumbered-reqid/source.md
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/srs/renumbered-reqid/requirements.json
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/srs/renumbered-reqid/requirements_v2.json
autonomous: true
requirements: [SRS-03, TRACE-03]
must_haves:
  truths:
    - "check_grounding accepts a dict source_trace without raising AttributeError (grounded iff dict has non-empty 'doc')"
    - "ba-tools init scaffolds a .ba-ops/traces/ subdirectory"
    - "statement_hash drift on the same REQ-ID is flagged by the stability lint (F9 renumbered-reqid fixture)"
  prohibitions:
    - "lint.py / scaffold.py contain no LLM/judgement call and no model-client import (determinism boundary)"
    - "No new runtime dependency is added this phase; the spine ingests `.md`/`.txt` only and `.docx` stays plugin-only (D-18) — verified: pyproject.toml runtime deps unchanged (stdlib + existing filelock) and `grep -rn 'import docx\\|python-docx' ba_tools/` over spine modules returns no matches"
  artifacts:
    - path: ".agents/ba-daily-operators/ba-tools/ba_tools/lint.py"
      provides: "dict-aware check_grounding"
      contains: "isinstance"
    - path: ".agents/ba-daily-operators/ba-tools/ba_tools/scaffold.py"
      provides: "traces subdir in _SUBDIRS"
      contains: "traces"
    - path: ".agents/ba-daily-operators/ba-tools/tests/test_smoke.py"
      provides: "command-registration smoke test"
    - path: ".agents/ba-daily-operators/ba-tools/tests/test_skill_schema.py"
      provides: "SKILL.md/openai.yaml schema validator test"
  key_links:
    - from: "verify_cmd.py JSON path (plan 02)"
      to: "lint.py::check_grounding"
      via: "dict source_trace passed to check_grounding without .strip() crash"
      pattern: "check_grounding"
    - from: "scaffold.py::_SUBDIRS"
      to: ".ba-ops/traces/"
      via: "init creates the traces dir that trace write (plan 03) populates"
      pattern: "traces"
---

<objective>
Wave-0 prerequisites + test scaffolding. Land the two one-shot fixes that every
later Phase-2 plan depends on, and create the empty test files + the first lint
fixture so subsequent plans extend rather than create test infrastructure.

This is the sequencing gate identified in RESEARCH: the `lint.py:234`
`check_grounding` dict-bug and the `scaffold.py:157` `_SUBDIRS` patch MUST land
before the JSON verify path (plan 02) or `index update` / `trace write`
(plan 03) are wired — otherwise the JSON `source_trace` gate raises
`AttributeError` and the `.ba-ops/traces/` dir is never scaffolded by `init`.

Purpose: Remove the two blockers and stand up the pytest scaffolds so Wave-2
plans add test cases into existing files (RESEARCH "extend, don't replace").
Output: Patched `lint.py` + `scaffold.py`; new `test_smoke.py`,
`test_skill_schema.py`; extended `test_lint_reqs.py`; the F9 `renumbered-reqid`
fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ba-srs-analyze-quality-gate-traceability-core/02-PATTERNS.md
@.planning/phases/02-ba-srs-analyze-quality-gate-traceability-core/02-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan produces (others in plans 02-04):
- **Modified module:** `ba_tools/lint.py` — `check_grounding` made dict-aware.
- **Modified config:** `ba_tools/scaffold.py` — `_SUBDIRS` gains `"traces"`.
- **New test files:** `tests/test_smoke.py`, `tests/test_skill_schema.py`.
- **Extended test file:** `tests/test_lint_reqs.py` — `test_stability_drift`.
- **New fixture dir:** `tests/fixtures/srs/renumbered-reqid/` (F9) with
  `source.md`, `requirements.json`, `requirements_v2.json`.
- **New file path created at runtime by init:** `.ba-ops/traces/` (via scaffold).

Symbols/fields introduced for downstream drift-exclusion: `_SUBDIRS` value
`"traces"`; test names `test_grounding_dict_compat`, `test_stability_drift`,
`test_commands_registered`, plus the skill-schema validator helpers.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Patch check_grounding for dict source_trace + scaffold traces subdir</name>
  <files>.agents/ba-daily-operators/ba-tools/ba_tools/lint.py, .agents/ba-daily-operators/ba-tools/ba_tools/scaffold.py, .agents/ba-daily-operators/ba-tools/tests/test_lint_reqs.py</files>
  <read_first>
    - .agents/ba-daily-operators/ba-tools/ba_tools/lint.py:223-247 (check_grounding — the buggy `.strip()` at line 234)
    - .agents/ba-daily-operators/ba-tools/ba_tools/lint.py:250-270 (check_citation_present — already dict-aware; mirror its isinstance guard)
    - .agents/ba-daily-operators/ba-tools/ba_tools/scaffold.py:157 (_SUBDIRS list lacking "traces")
    - .agents/ba-daily-operators/ba-tools/tests/test_lint_reqs.py (existing lint tests — extend, do not replace)
  </read_first>
  <behavior>
    - Test test_grounding_dict_compat: a row whose `source_trace` is a dict with a non-empty `doc` returns no GROUNDING_MISSING finding; a dict with empty/absent `doc` returns a GROUNDING_MISSING finding; a row whose `source_trace` is a string (Phase-1 Markdown path) still behaves as before.
    - Test (scaffold): after running the scaffold seed routine, `.ba-ops/traces/` exists as a directory.
  </behavior>
  <action>
    In `lint.py::check_grounding` replace the single line that calls `.strip()` on `row.get("source_trace", "")` with a dict-aware normalization: bind `_st = row.get("source_trace", "")`, then set `source_trace = _st.get("doc", "").strip() if isinstance(_st, dict) else _st.strip()`. Mirror the `isinstance(..., dict)` guard already used in `check_citation_present`. Leave the `source` line and the `GROUNDING_MISSING` finding shape unchanged. In `scaffold.py` line 157 append `"traces"` to the `_SUBDIRS` list so it reads srs/mermaid/mockup/backlog/plugins/traces. Add `test_grounding_dict_compat` to `tests/test_lint_reqs.py` covering the three dict/string cases above, plus a scaffold assertion that the seed routine creates `.ba-ops/traces/`. Do NOT introduce any model-client import; this is pure stdlib.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && python -m pytest tests/test_lint_reqs.py::test_grounding_dict_compat -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `lint.py::check_grounding` contains an `isinstance(_st, dict)` branch; calling it with `{"source_trace": {"doc": "a.md", "span": "x"}}` returns None (grounded) and with `{"source_trace": {}}` returns a `GROUNDING_MISSING` finding — no `AttributeError`.
    - `scaffold.py` `_SUBDIRS` list contains the string `"traces"`.
    - `python -m pytest tests/test_lint_reqs.py::test_grounding_dict_compat -x` exits 0.
    - `grep -rn "import openai\|import anthropic" ba_tools/lint.py ba_tools/scaffold.py` returns no matches.
  </acceptance_criteria>
  <done>check_grounding handles a dict source_trace without crashing; init scaffolds .ba-ops/traces/; the dict-compat test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F9 stability-drift fixture + REQ-ID stability test + smoke/skill-schema scaffolds</name>
  <files>.agents/ba-daily-operators/ba-tools/tests/fixtures/srs/renumbered-reqid/source.md, .agents/ba-daily-operators/ba-tools/tests/fixtures/srs/renumbered-reqid/requirements.json, .agents/ba-daily-operators/ba-tools/tests/fixtures/srs/renumbered-reqid/requirements_v2.json, .agents/ba-daily-operators/ba-tools/tests/test_lint_reqs.py, .agents/ba-daily-operators/ba-tools/tests/test_smoke.py, .agents/ba-daily-operators/ba-tools/tests/test_skill_schema.py</files>
  <read_first>
    - .agents/ba-daily-operators/ba-tools/ba_tools/lint.py:278-340 (detect_reqid_issues — the two-pass stability detector to exercise)
    - .agents/ba-daily-operators/ba-tools/tests/test_lint_reqs.py (test style + how reqs rows are built)
    - .agents/ba-daily-operators/ba-tools/ba_tools/__main__.py:17-45 (_COMMAND_MODULES — the registration list test_smoke asserts against)
  </read_first>
  <behavior>
    - Test test_stability_drift (F9): feeding the same REQ-ID `FR-001` with a materially changed `statement` between requirements.json and requirements_v2.json yields a stability/drift finding from detect_reqid_issues (not a silent renumber).
    - Test test_commands_registered: every module name expected in `_COMMAND_MODULES` is reachable; placeholder asserts the Phase-1 commands are present and is structured so plan 03 can add `trace`/`index` assertions.
    - test_skill_schema: helper that parses a SKILL.md frontmatter block and asserts its key set is exactly {name, description}; helper that parses openai.yaml and asserts `interface.*` keys + `policy.allow_implicit_invocation` nesting. Marked xfail/skipped until the skill files exist (plan 04 flips them live).
  </behavior>
  <action>
    Create `tests/fixtures/srs/renumbered-reqid/` with `source.md` (a short doc containing a verbatim sentence the requirement cites), `requirements.json` (one `FR-001` whose statement matches v1), and `requirements_v2.json` (same `FR-001` id with a materially different statement). Add `test_stability_drift` to `tests/test_lint_reqs.py` that loads both JSON files, builds the old/new id->statement maps, calls `detect_reqid_issues`, and asserts a drift finding is returned for `FR-001`. Create `tests/test_smoke.py` with `test_commands_registered` importing `ba_tools.__main__` and asserting the `_COMMAND_MODULES` length and presence of the Phase-1 module objects; leave a clearly-marked spot for plan 03 to add `trace_cmd`/`index_cmd`. Create `tests/test_skill_schema.py` with two pytest helpers (frontmatter-keys validator, openai.yaml nesting validator) and tests over the (not-yet-existing) skill paths marked `pytest.mark.skip(reason="skill files land in plan 04")`. Use only stdlib + pytest.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && python -m pytest tests/test_lint_reqs.py::test_stability_drift tests/test_smoke.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_lint_reqs.py::test_stability_drift -x` exits 0 and the test asserts a non-empty drift finding for `FR-001`.
    - `tests/fixtures/srs/renumbered-reqid/` contains `source.md`, `requirements.json`, `requirements_v2.json`; both JSON files parse and share id `FR-001` with differing statements.
    - `python -m pytest tests/test_smoke.py::test_commands_registered -x` exits 0.
    - `tests/test_skill_schema.py` collects without error (skill-path tests skipped, not failing).
  </acceptance_criteria>
  <done>F9 fixture exists; stability drift on a reused REQ-ID is flagged; smoke + skill-schema test scaffolds collect and are ready for plans 03/04 to extend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture files → lint/scaffold code | Fixture JSON is developer-authored, low trust surface; still parsed with stdlib json |
| scaffold → filesystem | `_SUBDIRS` directory names are static literals, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | check_grounding dict handling | mitigate | dict-aware normalization prevents AttributeError → INTERNAL_ERROR masking a real FAIL; test_grounding_dict_compat asserts behavior |
| T-02-02 | Denial of Service | scaffold dir creation | accept | static literal subdir names; no user-controlled path |
| T-02-SC | Tampering | npm/pip installs | mitigate | zero new dependencies this phase (D-18); nothing to install — no [ASSUMED]/[SUS] package introduced |
</threat_model>

<verification>
- `cd .agents/ba-daily-operators/ba-tools && python -m pytest tests/test_lint_reqs.py tests/test_smoke.py -q` passes.
- `tests/test_skill_schema.py` collects (skipped tests visible).
- `grep -rn "import openai\|import anthropic" ba_tools/` returns empty (determinism boundary).
- `_SUBDIRS` includes `"traces"`.
</verification>

<success_criteria>
- check_grounding no longer crashes on a dict source_trace; the Wave-2 JSON verify path is unblocked.
- init scaffolds `.ba-ops/traces/`; the Wave-2 trace write path has a home.
- F9 stability-drift fixture + test exist and pass.
- Smoke + skill-schema scaffolds exist for plans 03/04 to extend.
</success_criteria>

<output>
Create `.planning/phases/02-ba-srs-analyze-quality-gate-traceability-core/02-01-SUMMARY.md` when done
</output>
