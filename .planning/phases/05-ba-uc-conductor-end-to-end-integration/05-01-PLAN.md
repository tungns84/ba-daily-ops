---
phase: 05-ba-uc-conductor-end-to-end-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .agents/ba-daily-operators/ba-tools/tests/test_uc_conductor_state.py
  - .agents/ba-daily-operators/ba-tools/tests/test_index_gate_predicate.py
  - .agents/ba-daily-operators/ba-tools/tests/test_scaffold_all_four_rows.py
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/uc-001-test.md
autonomous: true
requirements: [UC-03, GATE-03]
must_haves:
  truths:
    - "uc-status next_step lands on a failed step (failed status is non-complete)"
    - "uc-status next_step lands on a killed mid-step (in_progress is non-complete)"
    - "A gate-fail leaves earlier complete steps intact and later steps pending (recoverable state)"
    - "state patch of a pipeline_step round-trips: uc-status reflects the new next_step"
    - "Two concurrent pipeline-step patches do not clobber each other (FileLock serializes)"
    - "index update after a mermaid/mockup trace with valid req_ids reports orphans=[] and the step's req_ids NOT in gaps (D-G2 pass)"
    - "index update after a trace citing an unknown REQ-ID reports that id in orphans (D-G2 fail condition 1)"
    - "ensure_scaffold seeds all four pipeline rows including index (WR-02 regression guard, per RESEARCH correction)"
  artifacts:
    - path: ".agents/ba-daily-operators/ba-tools/tests/test_uc_conductor_state.py"
      provides: "Pipeline state-machine tests: next_step over failed/in_progress, no-clobber, concurrent-write, patch round-trip"
      min_lines: 120
    - path: ".agents/ba-daily-operators/ba-tools/tests/test_index_gate_predicate.py"
      provides: "D-G2 orphan + self-coverage predicate tests from index update output"
      min_lines: 50
    - path: ".agents/ba-daily-operators/ba-tools/tests/test_scaffold_all_four_rows.py"
      provides: "Regression guard: scaffold seeds srs-analyze/mermaid/mockup/index rows"
      min_lines: 25
    - path: ".agents/ba-daily-operators/ba-tools/tests/fixtures/uc-001-test.md"
      provides: "Minimal fixture UC for the agent-run E2E UAT (UC-001, 2-3 simple requirements)"
      min_lines: 10
  key_links:
    - from: ".agents/ba-daily-operators/ba-tools/tests/test_uc_conductor_state.py"
      to: ".agents/ba-daily-operators/ba-tools/ba_tools/commands/uc_status.py"
      via: "subprocess ba-tools uc-status reads STATE.md and computes next_step"
      pattern: "uc-status"
    - from: ".agents/ba-daily-operators/ba-tools/tests/test_index_gate_predicate.py"
      to: ".agents/ba-daily-operators/ba-tools/ba_tools/commands/index_cmd.py"
      via: "subprocess ba-tools index update returns orphans/gaps JSON"
      pattern: "index.*update"
---

<objective>
Build the deterministic integration test suite that proves the ba-uc conductor's
state-machine contract WITHOUT invoking the agent — pure ba-tools spine behavior:
uc-status next_step over failed/killed steps, gate-reject recoverability,
concurrent-write no-clobber, the D-G2 index-integrity predicate, and the
scaffold-seeds-all-four-rows regression guard. Also ship the minimal fixture UC
that the agent-run E2E UAT consumes.

This is the integration test half of "this phase doubles as the spine's
integration test." It is fully automatable and gates UC-03 + the GATE-03
no-synthetic-render guard at the machine level. The conductor authoring (Plan 03)
must make these tests pass without changing any ba-tools command.

Purpose: Lock the resumability + index-integrity contract as executable spec so
the conductor in Plan 03 is held to it.
Output: 3 new test files + 1 fixture UC.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ba-uc-conductor-end-to-end-integration/05-RESEARCH.md
@.planning/phases/05-ba-uc-conductor-end-to-end-integration/05-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates (the rest of the phase is Plans 02 + 03):
- Test file `tests/test_uc_conductor_state.py` with functions:
  `test_failed_step_is_next_step`, `test_in_progress_step_is_next_step`,
  `test_gate_fail_state_not_clobbered`, `test_resume_entry_point`,
  `test_pipeline_patch_round_trip`, `test_concurrent_pipeline_patch_no_clobber`.
- Test file `tests/test_index_gate_predicate.py` with functions:
  `test_index_gate_no_orphans_after_mermaid_trace`,
  `test_index_gate_orphan_detected`,
  `test_index_gate_self_coverage_predicate`.
- Test file `tests/test_scaffold_all_four_rows.py` with function:
  `test_scaffold_seeds_all_four_rows`.
- Fixture `tests/fixtures/uc-001-test.md` — minimal UC-001 for E2E UAT (Plan 03).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Pipeline state-machine tests (resume / kill / no-clobber / concurrent-write)</name>
  <files>.agents/ba-daily-operators/ba-tools/tests/test_uc_conductor_state.py</files>
  <read_first>
    - .agents/ba-daily-operators/ba-tools/tests/test_uc_status.py (analog — copy `_make_state_md` lines 11-57 and `_run_uc_status` lines 60-65 verbatim)
    - .agents/ba-daily-operators/ba-tools/tests/test_state.py (analog — copy `_run_state` CLI helper lines 29-40; copy the `multiprocessing.Process` concurrent-write pattern at lines ~288-300)
    - .agents/ba-daily-operators/ba-tools/ba_tools/commands/uc_status.py (the SUT — `_compute_next_step`, `_COMPLETE_STATUSES`)
    - .agents/ba-daily-operators/ba-tools/ba_tools/state_store.py (PIPELINE_STEPS tuple line 126; pipeline_step/pipeline_status directive keys ~248-264; acquire_state_lock)
  </read_first>
  <behavior>
    - test_failed_step_is_next_step: STATE.md with srs-analyze=failed, rest pending → uc-status next_step == "srs-analyze" (failed is non-complete).
    - test_in_progress_step_is_next_step: STATE.md with srs-analyze=complete, mermaid=in_progress → uc-status next_step == "mermaid" (kill-mid-step recovery).
    - test_gate_fail_state_not_clobbered: STATE.md with srs-analyze=complete, mermaid=failed, mockup=pending, index=pending → uc-status steps dict shows exactly those statuses (earlier complete preserved, later still pending) AND next_step == "mermaid".
    - test_resume_entry_point: after mermaid=failed, uc-status next_step == "mermaid" — the deterministic re-entry point the resume route consumes.
    - test_pipeline_patch_round_trip: scaffold a repo, run `state patch --data {"pipeline_step":"mermaid","pipeline_status":"complete"}` (srs-analyze also patched complete first), then uc-status next_step == "mockup".
    - test_concurrent_pipeline_patch_no_clobber: two multiprocessing.Process workers each `state patch` a DIFFERENT pipeline_step (e.g. mermaid, mockup); after join, STATE.md retains BOTH writes (uc-status shows both statuses) — FileLock serialized, no clobber.
  </behavior>
  <action>
    Create tests/test_uc_conductor_state.py. Reuse `_make_state_md` and `_run_uc_status` from test_uc_status.py and `_run_state` from test_state.py (copy the helpers into this file — do not import across test modules). For the patch round-trip and concurrent tests, scaffold a real .ba-ops/ via `ba-tools init ba-uc` (or `ensure_scaffold`) so the Pipeline Steps table pre-exists with all four rows; then drive `state patch` with `--data` JSON carrying pipeline_step + pipeline_status. Valid non-complete statuses per RESEARCH: pending, in_progress, failed. Valid complete: complete/completed/done. For the concurrent test, mirror test_state.py's multiprocessing.Process + Queue pattern: each worker patches a distinct step, parent joins both, then reads STATE.md via uc-status and asserts both step statuses survived. Assert all subprocess returncodes are 0 (or document the LOCK_TIMEOUT exit-2 path per test_state.py's existing concurrent semantics). Do NOT modify any ba_tools source — these tests describe existing behavior the conductor relies on.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest tests/test_uc_conductor_state.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All six test functions present and passing.
    - test_concurrent_pipeline_patch_no_clobber uses multiprocessing.Process (not a single-process loop).
    - No edits to any file under ba_tools/ (tests only) — `git diff --name-only` shows only test files.
    - next_step assertions cover failed, in_progress, and the resume entry point.
  </acceptance_criteria>
  <done>uc-status next_step lands correctly on non-complete (failed/in_progress) steps; gate-fail leaves recoverable state; pipeline patch round-trips; concurrent patches do not clobber — all green via pytest, zero ba_tools source change.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: D-G2 index-integrity predicate tests + scaffold-four-rows guard + fixture UC</name>
  <files>.agents/ba-daily-operators/ba-tools/tests/test_index_gate_predicate.py, .agents/ba-daily-operators/ba-tools/tests/test_scaffold_all_four_rows.py, .agents/ba-daily-operators/ba-tools/tests/fixtures/uc-001-test.md</files>
  <read_first>
    - .agents/ba-daily-operators/ba-tools/tests/test_index.py (analog — fixture/scaffold + trace-record setup; subprocess index update + JSON parse)
    - .agents/ba-daily-operators/ba-tools/tests/test_trace.py (analog — how trace records are written programmatically)
    - .agents/ba-daily-operators/ba-tools/ba_tools/commands/index_cmd.py (the SUT — ok_json emits orphans/gaps/req_ids/stale; covered_by computed internally and NOT emitted, per RESEARCH)
    - .agents/ba-daily-operators/ba-tools/ba_tools/scaffold.py (the SUT for the four-rows guard — _STATE_MD seeds all four rows incl. index at ~line 133; ensure_scaffold is idempotent, never overwrites)
    - .planning/phases/05-ba-uc-conductor-end-to-end-integration/05-RESEARCH.md (Q2 D-G2 predicate, Test 6/Test 7 specs, WR-02 correction)
  </read_first>
  <behavior>
    - test_index_gate_no_orphans_after_mermaid_trace: seed srs trace (req_ids FR-001, FR-002) + mermaid trace (req_ids FR-001) → index update → orphans == [] AND "FR-001" not in gaps.
    - test_index_gate_orphan_detected: seed a mermaid trace citing XX-999 (absent from srs traces) → index update → "XX-999" in orphans.
    - test_index_gate_self_coverage_predicate: implement the D-G2 fail predicate in-test as `len(orphans)==0 and all(rid not in gaps for rid in step_req_ids)`; assert it evaluates PASS for the covered case and FAIL for the orphan/uncovered case.
    - test_scaffold_seeds_all_four_rows: ensure_scaffold(tmp_path) then uc-status returns steps dict with exactly srs-analyze/mermaid/mockup/index all == "pending" and next_step == "srs-analyze".
  </behavior>
  <action>
    Create tests/test_index_gate_predicate.py and tests/test_scaffold_all_four_rows.py. For the index tests: scaffold a repo, write srs + mermaid trace records the same way test_trace.py/test_index.py do (programmatic trace files under the traces subdir, or via `ba-tools trace write`), then `ba-tools index update` and parse stdout JSON. Read the D-G2 predicate from index update's existing output ONLY — orphans list + gaps list; do NOT assume a covered_by field exists (RESEARCH confirms it is not emitted). Encode the exact predicate from RESEARCH Q2: FAIL iff orphans non-empty OR any step req_id appears in gaps. For the scaffold guard: call ensure_scaffold (import from ba_tools.scaffold) on tmp_path, then run uc-status against it and assert all four canonical rows are present and pending — this is the WR-02 regression guard (RESEARCH: scaffold ALREADY seeds the index row; this test pins that and prevents regression; do NOT add a scaffold-patch task anywhere in the phase). Also create tests/fixtures/uc-001-test.md: a minimal UC ("## UC-001. <name>") describing 2-3 simple, atomic, groundable requirements for the Plan-03 E2E UAT. Tests only — no ba_tools source change.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest tests/test_index_gate_predicate.py tests/test_scaffold_all_four_rows.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - D-G2 predicate test reads ONLY orphans+gaps from index update JSON (no reliance on a covered_by field).
    - Orphan negative case asserts the unknown REQ-ID appears in orphans.
    - test_scaffold_seeds_all_four_rows asserts all four canonical steps present and pending.
    - tests/fixtures/uc-001-test.md exists with a "## UC-001." heading and >=2 requirement statements.
    - `git diff --name-only` under ba_tools/ shows only test + fixture files (no source change, no scaffold.py edit).
  </acceptance_criteria>
  <done>D-G2 orphan + self-coverage predicate is proven from existing index update output; scaffold-four-rows regression guard green; fixture UC ready for the E2E UAT — all via pytest, zero ba_tools source change, scaffold.py NOT modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → ba-tools subprocess | Tests invoke ba-tools via `sys.executable -m ba_tools`; untrusted only insofar as tmp_path inputs |
| concurrent writers → STATE.md | Two processes contend for the STATE.md lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | concurrent STATE.md writes in test_concurrent_pipeline_patch_no_clobber | mitigate | Test asserts FileLock serialization (acquire_state_lock, timeout=10) preserves both writes — proves the no-clobber contract rather than introducing a race |
| T-05-02 | Denial of Service | multiprocessing concurrent test hangs on lock | accept | Phase 1 lockfile has a 10s stale-reclaim window; test mirrors the existing test_state.py timeout-bounded pattern |
| T-05-03 | Information Disclosure | fixture UC content | accept | uc-001-test.md is synthetic non-sensitive sample data committed to the repo |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies — stdlib pytest + multiprocessing only; no install task in this plan |
</threat_model>

<verification>
- `pytest tests/test_uc_conductor_state.py tests/test_index_gate_predicate.py tests/test_scaffold_all_four_rows.py -q` all green.
- `git diff --name-only` confirms ONLY test files + the fixture changed (zero ba_tools source change; scaffold.py untouched).
- Full suite still green: `pytest tests/ -q` (no regression introduced).
</verification>

<success_criteria>
The resumability + index-integrity + scaffold-seed contract the conductor depends
on is captured as passing, automatable pytest with zero new ba-tools command and
no scaffold.py modification. The fixture UC for the E2E UAT exists.
</success_criteria>

<output>
Create `.planning/phases/05-ba-uc-conductor-end-to-end-integration/05-01-SUMMARY.md` when done
</output>
