---
phase: 05-ba-uc-conductor-end-to-end-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/ba-daily-operators/ba-core/references/gates.md
autonomous: true
requirements: [GATE-03]
must_haves:
  truths:
    - "gates.md contains a Safety Gate Contract section scoped plugin-enforced / spine-exempt"
    - "The contract states the spine invokes no render CLI and the conductor never fires the Safety gate"
    - "The contract lists render-CLI-only, path-traversal+injection scan, and .png/.svg extension clauses"
    - "gates.md remains under the AGENTS.md/eager-ref byte budget (< 32,768 B)"
  artifacts:
    - path: ".agents/ba-daily-operators/ba-core/references/gates.md"
      provides: "GATE-03 Safety-gate contract appended as a new section (Safety Gate Contract + clauses + prohibition table)"
      contains: "Safety Gate Contract"
  key_links: []
---

<objective>
Define the GATE-03 Safety-gate contract by extending the existing
ba-core/references/gates.md with a Safety Gate Contract section: render CLI only,
path-traversal + injection scan on .ba-ops/ writes, .png/.svg extension check, no
synthetic render — explicitly scoped "enforced by the deferred plugins; the spine
invokes no render, so the conductor never fires the Safety gate." This is the
contract-only deliverable for GATE-03 (D-SAFE); actual enforcement lives in the
deferred plugins and is out of scope this phase.

Per RESEARCH Q5 and PATTERNS, extend gates.md (single canonical gate reference)
rather than create a sibling file, keeping all gate contracts co-located.

Purpose: Satisfy ROADMAP criterion 4 — the Safety gate contract is DEFINED and
documented as plugin-enforced with no synthetic spine render.
Output: gates.md extended with the Safety Gate Contract section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ba-uc-conductor-end-to-end-integration/05-CONTEXT.md
@.planning/phases/05-ba-uc-conductor-end-to-end-integration/05-RESEARCH.md
@.planning/phases/05-ba-uc-conductor-end-to-end-integration/05-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan adds to gates.md:
- A Safety Gate Contract section (heading depth ##) with a Scope marker and a Phase-5 status marker.
- Clause 1 — Render CLI only (### depth)
- Clause 2 — Path-traversal and injection scan (### depth)
- Clause 3 — Media extension check (### depth)
- Clause 4 — Hash manifest (PLUG-04, deferred) (### depth)
- A Safety Gate — Prohibition summary table (## depth)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Append the Safety Gate Contract section to gates.md</name>
  <files>.agents/ba-daily-operators/ba-core/references/gates.md</files>
  <read_first>
    - .agents/ba-daily-operators/ba-core/references/gates.md (the file being extended — match the existing Gate 1/2/3 heading depth, command-block fencing, and the existing Prohibition summary table format at the bottom)
    - .planning/phases/05-ba-uc-conductor-end-to-end-integration/05-RESEARCH.md (Q5 — exact clause wording for the four clauses + scope marker)
    - .planning/phases/05-ba-uc-conductor-end-to-end-integration/05-PATTERNS.md (gates.md extend pattern — section heading, clause format, prohibition-table format to mirror)
    - DESIGN.md (repo root — section 6 the three gates incl. Safety gate row; section 11 non-negotiables: real render CLIs only, no synthetic image; cite these in the clauses)
  </read_first>
  <action>
    Append (do NOT rewrite the existing Quality-gate content) a Safety Gate Contract section (## heading) after the existing Prohibition summary table. Open with a Scope paragraph: enforced by the deferred plugins ba-make-diagram and ba-uc-delivery; the spine (ba-srs-analyze, ba-mermaid, ba-mockup, ba-uc) invokes no render CLI, so the conductor never fires the Safety gate. Add a Phase-5 status line: contract defined, enforcement deferred to plugin implementation. Then four ### clause subsections mirroring the Gate 1/2/3 prose style. Clause 1 (render CLI only): any render/export step must invoke a real CLI such as draw.io export or mmdc; forbid Pillow, SVG-converter, screenshot, hand-pasted image; cite DESIGN sections 6 and 11. Clause 2 (path-traversal + injection scan): validate via resolve_under_root + is_within_root from repo.py; any path passed to a render CLI must resolve under --repo-root; advisory injection scan via the ba-tools scan command. Clause 3 (media extension check): rendered media must carry extension .png or .svg (.pdf for export-only); reject other extensions at write time. Clause 4 (hash manifest, PLUG-04, deferred): render step writes a manifest with rendered and embedded sha256; pass iff equal; enforced only when PLUG-04 ships. Finish with a Safety Gate — Prohibition summary table (## heading) mirroring the existing prohibition table with rows: never use Pillow/SVG-converter/screenshot for render (DESIGN 6/11); never write media outside .png/.svg/.pdf (Clause 3); never pass paths outside --repo-root to a render CLI (Clause 2); never fire the Safety gate from the spine (Scope marker). Keep prose; use only the short CLI-invocation fences the existing gate sections already use. Watch the byte budget — gates.md is an eager-ref read by spine operators, so it must stay under 32,768 B.
  </action>
  <verify>
    <automated>cd "D:/projects/harness/ba-daily-ops" && grep -c -i "Safety Gate Contract" .agents/ba-daily-operators/ba-core/references/gates.md && python -c "import os; b=os.path.getsize('.agents/ba-daily-operators/ba-core/references/gates.md'); print('bytes', b); assert b < 32768"</automated>
  </verify>
  <acceptance_criteria>
    - gates.md contains a "## Safety Gate Contract" heading and the Scope + Phase-5 status markers.
    - All four clauses present (render CLI only; path-traversal + injection scan; media extension check; hash manifest deferred).
    - A "Safety Gate — Prohibition summary" table is present with the four prohibition rows.
    - The Scope text states the spine fires no render and the conductor never fires the Safety gate.
    - `os.path.getsize` of gates.md is < 32768 bytes (eager-ref budget held).
    - The existing Quality-gate sections (Gate 1/2/3, escalation, WARN) are unchanged — append-only.
  </acceptance_criteria>
  <done>gates.md carries the GATE-03 Safety-gate contract (4 clauses + prohibition table) scoped plugin-enforced / spine-exempt, under the 32,768 B eager-ref budget, with the existing Quality-gate content untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future plugin implementers | The contract is the authoritative source the deferred plugins must enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | render path inputs (documented for plugins) | mitigate (contract) | Clause 2 mandates resolve_under_root + is_within_root for any render-CLI path; documented now, enforced by plugins |
| T-05-05 | Spoofing | synthetic render passed off as real | mitigate (contract) | Clause 1 forbids Pillow/SVG-convert/screenshot; Clause 4 manifest hash-equality (deferred) detects substitution |
| T-05-06 | Elevation of Privilege | subprocess injection on render CLI args | mitigate (contract) | Clause 2 injection scan + extension allowlist (Clause 3) documented for plugin enforcement |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Documentation-only plan; zero installs, zero new dependencies |
</threat_model>

<verification>
- `grep -c -i "Safety Gate Contract" gates.md` >= 1.
- gates.md size < 32768 B (eager-ref budget).
- `git diff` shows append-only change (existing Gate 1/2/3 + escalation + WARN sections intact).
</verification>

<success_criteria>
GATE-03's Safety-gate contract is defined and documented in gates.md as
plugin-enforced and spine-exempt, with render-CLI-only, path-traversal+injection,
and extension clauses, while staying within the eager-ref byte budget.
</success_criteria>

<output>
Create `.planning/phases/05-ba-uc-conductor-end-to-end-integration/05-02-SUMMARY.md` when done
</output>
