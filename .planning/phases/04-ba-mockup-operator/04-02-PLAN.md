---
phase: 04-ba-mockup-operator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/ba-daily-operators/ba-core/workflows/ba-mockup.md
  - .agents/ba-daily-operators/ba-core/agents/ba-mockup-author.md
  - .agents/skills/ba-mockup/SKILL.md
  - .agents/skills/ba-mockup/agents/openai.yaml
autonomous: true
requirements: [MOCK-01, MOCK-02, MOCK-03]
user_setup: []

must_haves:
  truths:
    - "D-01: one screen per invocation — the workflow authors exactly one artifact carrying exactly one `req_ids` set per run (no multi-screen loop; that is the Phase-5 conductor's job)"
    - "D-01a: input is an existing SRS `--slug`; the workflow hands the agent `.ba-ops/srs/<slug>/requirements.json` (paths only) and the agent picks the REQ-ID subset; artifacts land under `.ba-ops/mockup/<slug>/`"
    - "D-02: req_ids carried in the artifact (YAML frontmatter for `.md`, HTML-comment first line for `.html`) → workflow reads them → calls `trace write --kind mockup --req-ids <list>` (no new ba-tools parser)"
    - "D-03: html fidelity = single self-contained static `.html`, inline CSS in one `<style>`, zero JS/CDN/framework/external src — enforced by the ba-mockup-author.md HTML-scaffold rules"
    - "D-04: wireframe fidelity = markdown-structural blocks (headings + lists + tables) in a `.md`, NOT ASCII box-drawing — enforced by the ba-mockup-author.md wireframe rules"
    - "D-05: routes are `screen` (author-only, no CLI/no trace) and `full` (author→trace→index, default); there is NO render route and NO synthetic render path (DESIGN §11)"
    - "D-05a: `--fidelity` is required and workflow-enforced (hard-rejects missing/invalid, must be html|wireframe) BEFORE authoring; zero new ba-tools commands"
    - "D-06: orphan validation is downstream via `index update`; the workflow's `trace write` records the req_ids it is given without validating them"
  artifacts:
    - path: ".agents/ba-daily-operators/ba-core/workflows/ba-mockup.md"
      provides: "thin orchestrator — frontmatter (operator: ba-mockup, default_route: full, routes screen/full), fidelity gate, screen+full route bodies, no render route"
      contains: "operator: ba-mockup"
      min_lines: 60
    - path: ".agents/ba-daily-operators/ba-core/agents/ba-mockup-author.md"
      provides: "author role contract — fidelity-branched output schema (html vs wireframe), req_ids discipline, no-invent rule, no-script/no-box-drawing rules"
      contains: "ba-mockup-author"
      min_lines: 50
    - path: ".agents/skills/ba-mockup/SKILL.md"
      provides: "CDX skill discovery index — frontmatter name+description ONLY"
      contains: "name: ba-mockup"
    - path: ".agents/skills/ba-mockup/agents/openai.yaml"
      provides: "Codex skill metadata — interface.* + policy.allow_implicit_invocation: false"
      contains: "allow_implicit_invocation: false"
  key_links:
    - from: ".agents/skills/ba-mockup/agents/openai.yaml"
      to: ".agents/ba-daily-operators/ba-core/workflows/ba-mockup.md"
      via: "default_prompt points the agent at the ba-mockup workflow file"
      pattern: "ba-mockup.md"
    - from: ".agents/ba-daily-operators/ba-core/workflows/ba-mockup.md"
      to: ".agents/ba-daily-operators/ba-core/agents/ba-mockup-author.md"
      via: "screen route Step 4 instructs the agent to open + follow the author role"
      pattern: "ba-mockup-author.md"
    - from: ".agents/ba-daily-operators/ba-core/workflows/ba-mockup.md"
      to: ".agents/ba-daily-operators/ba-tools/ba_tools/commands/trace_cmd.py"
      via: "full route trace write --kind mockup (existing CLI, consumed as-is)"
      pattern: "trace write"
---

<objective>
Build the ba-mockup Codex skill operator by copying the Phase-3 ba-mermaid layout
wholesale and adapting it for fidelity-branched authoring with no render route.
Three pieces: the thin workflow (`ba-mockup.md`), the author role contract
(`ba-mockup-author.md`), and the skill discovery files (SKILL.md + openai.yaml).

The deterministic surface is fully covered by the existing `trace write --kind
mockup` + `index update` CLI (verified registered in Phase 1/2) — this plan adds
ZERO ba-tools commands. `--fidelity` enforcement, req_ids extraction, and artifact
authoring are all workflow/agent-layer concerns.

Purpose: Deliver the operator a BA invokes to turn an SRS slug into a UI mockup.
Output: 1 workflow + 1 agent prompt + 1 SKILL.md + 1 openai.yaml.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04-ba-mockup-operator/04-CONTEXT.md
@.planning/phases/04-ba-mockup-operator/04-RESEARCH.md
@.planning/phases/04-ba-mockup-operator/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates (NEW):
- `.agents/ba-daily-operators/ba-core/workflows/ba-mockup.md` — thin workflow (routes screen/full, fidelity gate, no render route)
- `.agents/ba-daily-operators/ba-core/agents/ba-mockup-author.md` — author role contract (fidelity-branched output)
- `.agents/skills/ba-mockup/SKILL.md` — CDX skill discovery index
- `.agents/skills/ba-mockup/agents/openai.yaml` — Codex skill metadata
- The workflow-level `--fidelity` argument concept (html|wireframe), enforced in the workflow — NOT a new ba-tools flag.

(NO new ba-tools symbols — this phase adds none. NO ba_tools/ file is touched.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the ba-mockup thin workflow (fidelity gate, screen + full routes, no render route)</name>
  <files>.agents/ba-daily-operators/ba-core/workflows/ba-mockup.md</files>
  <read_first>
    - .agents/ba-daily-operators/ba-core/workflows/ba-mermaid.md (the EXACT analog — frontmatter shape, preamble (determinism boundary / sequential execution / pass-paths-not-content), Route: author body, Route: full Steps, the `--source-doc` = requirements.json note)
    - .planning/phases/04-ba-mockup-operator/04-PATTERNS.md (§ "ba-mockup.md" — the frontmatter change, the Route: screen template, the Route: full Step bodies, the two regexes, "What is NOT in this workflow")
    - .planning/phases/04-ba-mockup-operator/04-RESEARCH.md (§ Code Examples → "Workflow: ba-mockup.md Structure" canonical, §3 regexes, §4 trace write invocation)
  </read_first>
  <action>
    Copy `ba-mermaid.md` and adapt. Frontmatter: `operator: ba-mockup`, `default_route: full`, `routes: [screen, full]`. Copy the preamble verbatim, changing the operator name and the closing sentence ("writes the mockup artifact" instead of "diagram .md").

    Route: screen — author-only, no CLI, no trace. Steps in order: (1) `ba-tools resolve-route ba-mockup` to confirm default route = full; (2) the FIDELITY GATE — validate `--fidelity` must be `html` or `wireframe`, and if absent or invalid, emit an error and STOP before authoring (this is the D-05a / MOCK-01 criterion-1 hard-reject, workflow-level, no ba-tools call); (3) `ba-tools init ba-mockup` for scaffold context; (4) open `.agents/ba-daily-operators/ba-core/agents/ba-mockup-author.md` and follow the author role; (5) pass the paths-only payload (requirements_json, slug, fidelity, screen_name, route=screen); (6) the agent writes `.ba-ops/mockup/<slug>/<screen-name>.html` for html or `<screen-name>.md` for wireframe. Output line: `.ba-ops/mockup/<slug>/<screen-name>.<ext>`.

    Route: full — author → trace write → index update. Step 1 references the screen route steps. Step 2 (Extract req_ids) describes BOTH carriers: for html fidelity scan the FIRST LINE for the `<!-- req_ids: [...] -->` comment and extract the bracketed list (split on comma, strip); for wireframe fidelity scan the YAML frontmatter block for the `req_ids: [...]` inline list and extract it. Step 3 runs the exact `trace write --kind mockup --slug <slug> --artifact .ba-ops/mockup/<slug>/<screen-name>.<ext> --source-doc .ba-ops/srs/<slug>/requirements.json --requirements .ba-ops/srs/<slug>/requirements.json --req-ids <extracted list>` invocation, followed by the verbatim note that `--source-doc` is the SRS requirements.json (NOT the mockup artifact) pinning source_hash for drift detection. Step 4 runs `ba-tools index update`.

    HARD CONSTRAINTS: there is NO `## Route: render` section; the file contains NO `mmdc`, `mermaid-render`, `drawio`, screenshot, or any synthetic-render reference anywhere (D-05, DESIGN §11). The `## Route: screen` section in particular must contain none of those tokens (test_mockup_author.py::test_screen_route_invokes_no_render_cli asserts this). Reproduce the trace-write block and regexes from the analog/RESEARCH — do not invent new flags.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest tests/test_mockup_author.py -x -q -k "screen_route_invokes_no_render_cli or workflow_rejects_missing_fidelity"</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `pytest tests/test_mockup_author.py::test_screen_route_invokes_no_render_cli -x` passes — the `## Route: screen` section contains none of `render`, `mmdc`, `mermaid-render`, `drawio`.
    - Test command: `pytest tests/test_mockup_author.py::test_workflow_rejects_missing_fidelity -x` passes — the workflow text contains `fidelity`, `html`, `wireframe`.
    - Source assertion: frontmatter has `operator: ba-mockup`, `default_route: full`, `routes: [screen, full]`; the file contains NO `Route: render` heading and no `mmdc`/`mermaid-render`/`drawio` token anywhere.
    - Source assertion: the full-route trace-write invocation uses `--kind mockup` and `--source-doc .ba-ops/srs/<slug>/requirements.json`.
  </acceptance_criteria>
  <done>ba-mockup.md exists with the fidelity gate, screen + full routes, the correct trace-write invocation, and zero render references; the two workflow-inspection tests from Plan 01 go green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the ba-mockup-author agent role contract (fidelity-branched output, req_ids discipline)</name>
  <files>.agents/ba-daily-operators/ba-core/agents/ba-mockup-author.md</files>
  <read_first>
    - .agents/ba-daily-operators/ba-core/agents/ba-diagrammer.md (the EXACT analog — header/role, determinism-boundary paragraph, inputs payload block, output schema block, the req_ids discipline section to copy verbatim)
    - .planning/phases/04-ba-mockup-operator/04-PATTERNS.md (§ "ba-mockup-author.md" — header change, inputs payload change (fidelity + screen_name), the fidelity-branched output section, sections to DROP and ADD)
    - .planning/phases/04-ba-mockup-operator/04-RESEARCH.md (§1 HTML Scaffold Conventions + Rules, §2 Wireframe conventions + Rules, §5 agent filename/discovery, Pitfalls 1/3/5)
  </read_first>
  <action>
    Copy `ba-diagrammer.md` and adapt. Header: "# ba-mockup-author Agent Role"; role paragraph = mockup-authoring agent that reads `requirements.json`, picks the REQ-ID subset the SCREEN realizes, and writes a `.html` or wireframe `.md` per `--fidelity`. Determinism-boundary paragraph: author + judge only; ba-tools handles all hash/CLI-provable work; NEVER call an LLM sub-service or any external tool; READ source files, WRITE the mockup artifact.

    Inputs payload (paths only): `requirements_json`, `slug`, `fidelity: <html|wireframe>`, `screen_name: <agent-chosen from UC/requirement context>`, `route: <screen|full>`. State that the agent reads requirements_json itself; no content is forwarded.

    Output section — fidelity-branched:
    - If fidelity = html: write `.ba-ops/mockup/<slug>/<screen-name>.html`. The ABSOLUTE FIRST LINE MUST be the `<!-- req_ids: [...] -->` comment (before the doctype) — Pitfall 1. Then a self-contained HTML5 document with ALL CSS inline in one `<style>` block; semantic elements (`<header>`/`<nav>`/`<main>`/`<section>`/`<footer>`/`<form>`/`<table>` as fitting); NO `<script>` tag, NO external `src=`/`href=` URL, NO CDN/framework (D-03). Carry the req_ids reference into the document body or footer for human visibility.
    - If fidelity = wireframe: write `.ba-ops/mockup/<slug>/<screen-name>.md`. YAML frontmatter MUST include `req_ids`, `fidelity: wireframe`, `slug`, `screen`. Body uses headings + lists + tables to describe layout regions. NO ASCII box-drawing characters (D-04, Pitfall 5).

    req_ids discipline section — copy the ba-diagrammer.md section verbatim, adapting "diagram depicts" → "screen realizes": read all IDs from requirements.json, select only those the screen realizes, NEVER invent IDs not in requirements.json (any unknown ID surfaces as an orphan via `index update`, D-06, Pitfall 3), pick the focused subset, honor an explicit `--req-ids` subset exactly.

    DROP the mermaid-specific `## Diagram type selection` and `## Mermaid syntax guidelines` sections. Use directive prose; do not paste a full HTML document body — name the required structural elements and rules; the executor reproduces the skeleton from RESEARCH §1/§2 via read_first.

    NOTE on comment-text discipline: the negative-grep tests (no `<script>`, no `+--` box-drawing) target the AUTHORED ARTIFACTS, not this prompt file. This prompt legitimately names `<script>` and box-drawing in its "do NOT use" rules; that is correct and does not trip any artifact gate, since tests scan `authored_html.html`/`authored_wireframe.md`, not `ba-mockup-author.md`.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -c "import pathlib,re; t=pathlib.Path('../ba-core/agents/ba-mockup-author.md').read_text(encoding='utf-8'); assert 'ba-mockup-author' in t; assert 'fidelity' in t and 'html' in t and 'wireframe' in t; assert re.search(r'invent', t, re.I); assert 'req_ids' in t; print('author-prompt-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the file contains the header `# ba-mockup-author Agent Role`, an `html` output branch and a `wireframe` output branch, a `req_ids discipline` section with a never-invent rule, and the fidelity-branched output file paths under `.ba-ops/mockup/<slug>/`.
    - Source assertion: the html branch states the `<!-- req_ids: [...] -->` comment is the absolute first line and forbids `<script>`/external src; the wireframe branch forbids ASCII box-drawing.
    - Behavior assertion: the dropped mermaid sections (`Diagram type selection`, `Mermaid syntax guidelines`) are absent.
    - CLI output: the verify command prints `author-prompt-ok`.
  </acceptance_criteria>
  <done>ba-mockup-author.md exists with the fidelity-branched output schema, the no-invent req_ids discipline, the html no-script/first-line rules, and the wireframe no-box-drawing rule; mermaid-only sections dropped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create the ba-mockup skill (SKILL.md + openai.yaml)</name>
  <files>
    .agents/skills/ba-mockup/SKILL.md
    .agents/skills/ba-mockup/agents/openai.yaml
  </files>
  <read_first>
    - .agents/skills/ba-mermaid/SKILL.md (the EXACT analog — frontmatter is name + description ONLY, plus the two HTML-comment lines pointing at the workflow)
    - .agents/skills/ba-mermaid/agents/openai.yaml (the EXACT analog — interface.display_name/short_description/default_prompt + policy.allow_implicit_invocation, the mandatory nesting)
    - .planning/phases/04-ba-mockup-operator/04-PATTERNS.md (§ "SKILL.md" and "openai.yaml" — the field-by-field change tables + canonical blocks)
    - .planning/phases/04-ba-mockup-operator/04-RESEARCH.md (§ Code Examples → canonical SKILL.md + openai.yaml blocks)
    - CLAUDE.md (Codex skill contract: SKILL.md frontmatter = name + description ONLY; openai.yaml uses interface.* + policy.allow_implicit_invocation nested under policy:)
  </read_first>
  <action>
    Create `.agents/skills/ba-mockup/SKILL.md`: copy the ba-mermaid SKILL.md exactly; change ONLY `name: ba-mockup`, the `description` block (use the canonical mockup description from PATTERNS.md/RESEARCH — covers fidelity html|wireframe, self-contained .html / markdown-structural .md, req_ids traceability, routes screen|full default full, fidelity required, trigger phrases), and the workflow-path HTML comment to point at `ba-mockup.md`. The frontmatter carries ONLY `name` + `description` — no `routes:`/`default_route:`/any other field (Pitfall 6; test_skill_schema.py validates this).

    Create `.agents/skills/ba-mockup/agents/openai.yaml`: copy the ba-mermaid openai.yaml; change `interface.display_name: "BA Mockup"`; `interface.short_description` to the mockup one-liner ("Requirements → UI mockup .html or wireframe .md; req_ids traceability via trace write + index update."); rewrite `interface.default_prompt` for ba-mockup with route `full` (default), an explicit step instructing to validate `--fidelity` (required: html or wireframe), the workflow path `.agents/ba-daily-operators/ba-core/workflows/ba-mockup.md`, the agent path `.agents/ba-daily-operators/ba-core/agents/ba-mockup-author.md`, and the example `slug = order-management, --fidelity html`. Keep `policy.allow_implicit_invocation: false`. The `interface:` and `policy:` keys are top-level; `allow_implicit_invocation` is nested under `policy:` (mandatory nesting per CLAUDE.md / CDX-02).
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest tests/test_skill_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `pytest tests/test_skill_schema.py -x` passes — ba-mockup SKILL.md frontmatter is name+description only (no extra fields).
    - Source assertion: `.agents/skills/ba-mockup/SKILL.md` frontmatter contains `name: ba-mockup` and a `description:` and NO other frontmatter keys.
    - Source assertion: `.agents/skills/ba-mockup/agents/openai.yaml` has `interface.display_name: "BA Mockup"`, a `default_prompt` referencing `ba-mockup.md` and `ba-mockup-author.md`, and `policy.allow_implicit_invocation: false` (nested under `policy:`).
  </acceptance_criteria>
  <done>The ba-mockup skill discovery files exist; SKILL.md passes the schema test (name+description only); openai.yaml has the correct interface fields and nested policy.allow_implicit_invocation: false pointing at the ba-mockup workflow + author.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BA invocation → workflow | `--slug` + `--fidelity` enter the workflow; fidelity validated at workflow level before authoring |
| agent → artifact file | the author writes `.html`/`.md` under `.ba-ops/mockup/<slug>/` (path resolved under --repo-root by existing repo.py guards) |
| workflow → trace write | extracted req_ids passed to the existing CLI; CLI records, index validates downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering (V5 Input) | `--fidelity` argument | mitigate | Workflow hard-rejects missing/invalid `--fidelity` (must be html\|wireframe) BEFORE any authoring or CLI call (D-05a). Verification: `test_workflow_rejects_missing_fidelity` (Plan 01) asserts the gate text; the screen route stops on invalid fidelity. |
| T-4-02 | Spoofing | agent invents a REQ-ID | mitigate | `ba-mockup-author.md` carries an explicit never-invent-IDs rule; any slip is caught downstream by `index update` orphan detection (D-06). Verification: `test_invented_id_surfaces_as_orphan` (Plan 01). |
| T-4-04 | Tampering | `<script>`/JS injection in the `.html` artifact | mitigate | D-03 forbids JS/CDN/framework; `ba-mockup-author.md` HTML rules forbid `<script>` and external `src=`/`href=`. Verification: the `authored_html.html` fixture (Plan 01) is asserted to contain no `<script>`; the author rule mirrors that for runtime output. |
| T-4-05 | Information Disclosure / Tampering | implicit auto-invocation of the build path | mitigate | `policy.allow_implicit_invocation: false` in openai.yaml (CDX-02 / REQUIREMENTS Out-of-Scope). Verification: source assertion in Task 3. |
| T-4-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase; zero new dependencies. No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `pytest tests/test_mockup_author.py -x` fully green once this plan lands (the two workflow-inspection tests now pass alongside the four fixture tests).
- `pytest tests/test_skill_schema.py -x` green (ba-mockup SKILL.md schema valid).
- The workflow contains NO render route and no `mmdc`/`mermaid-render`/`drawio` token (D-05, DESIGN §11).
- `git status --porcelain` shows NO change under `ba_tools/` — zero new ba-tools commands.
</verification>

<success_criteria>
- ba-mockup.md, ba-mockup-author.md, SKILL.md, openai.yaml all exist with the correct contracts.
- Fidelity gate is workflow-level; routes are screen/full with default full; no render route.
- All eight locked decisions (D-01..D-06, D-01a, D-05a) realized.
- No file under `ba_tools/` modified.
</success_criteria>

<output>
Create `.planning/phases/04-ba-mockup-operator/04-02-SUMMARY.md` when done.
</output>