---
phase: 04-ba-mockup-operator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/mockup_requirements.json
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/authored_html.html
  - .agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/authored_wireframe.md
  - .agents/ba-daily-operators/ba-tools/tests/test_mockup_author.py
  - .agents/ba-daily-operators/ba-tools/tests/test_mockup_trace_index.py
autonomous: true
requirements: [MOCK-01, MOCK-02, MOCK-03]
user_setup: []

must_haves:
  truths:
    - "D-03: an html-fidelity artifact carries `<!-- req_ids: [...] -->` as its FIRST line and a `<!DOCTYPE html>` with inline `<style>`, no `<script>`/external src (proven by test_mockup_author.py fidelity-branch tests)"
    - "D-04: a wireframe-fidelity artifact carries `req_ids:` in YAML frontmatter and uses headings/lists/tables with NO ASCII box-drawing (proven by test_mockup_author.py wireframe tests)"
    - "D-02: cited req_ids written into a mockup artifact reach INDEX.md mockup column via the existing `trace write --kind mockup` + `index update` contract (proven by test_mockup_trace_index.py, no ba_tools edit)"
    - "D-06: a mockup citing a REQ-ID absent from requirements.json surfaces under INDEX.md `## Orphans` downstream (proven by test_invented_id_surfaces_as_orphan, trace records what it is given)"
  artifacts:
    - path: ".agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/mockup_requirements.json"
      provides: "FR-001/FR-002 requirements fixture (kind-agnostic; copy of mermaid index_requirements.json)"
      contains: "FR-001"
    - path: ".agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/authored_html.html"
      provides: "valid html-fidelity artifact fixture — req_ids comment first line + DOCTYPE + inline style"
      contains: "<!-- req_ids:"
    - path: ".agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/authored_wireframe.md"
      provides: "valid wireframe-fidelity artifact fixture — YAML frontmatter req_ids + headings/lists/tables"
      contains: "req_ids:"
    - path: ".agents/ba-daily-operators/ba-tools/tests/test_mockup_author.py"
      provides: "MOCK-01 + MOCK-03 fidelity-branch + workflow-inspection tests"
      min_lines: 60
    - path: ".agents/ba-daily-operators/ba-tools/tests/test_mockup_trace_index.py"
      provides: "MOCK-02 + criterion-3 subprocess trace/index integration tests"
      min_lines: 120
  key_links:
    - from: ".agents/ba-daily-operators/ba-tools/tests/test_mockup_trace_index.py"
      to: ".agents/ba-daily-operators/ba-tools/ba_tools/commands/trace_cmd.py"
      via: "subprocess `ba-tools trace write --kind mockup` (consumed as-is, zero edits)"
      pattern: "trace.*write.*--kind.*mockup"
    - from: ".agents/ba-daily-operators/ba-tools/tests/test_mockup_trace_index.py"
      to: ".agents/ba-daily-operators/ba-tools/ba_tools/commands/index_cmd.py"
      via: "subprocess `ba-tools index update` → INDEX Mockup column + Orphans (consumed as-is)"
      pattern: "index.*update"
    - from: ".agents/ba-daily-operators/ba-tools/tests/test_mockup_author.py"
      to: ".agents/ba-daily-operators/ba-core/workflows/ba-mockup.md"
      via: "workflow-inspection slice of `## Route: screen` (created in Plan 02; these tests pass once 02 lands)"
      pattern: "Route: screen"
---

<objective>
Lay the Nyquist test foundation for the ba-mockup operator: three fixtures and two
pytest files that pin the three phase success criteria. Mirrors the Phase-3
ba-mermaid test pair (`test_mermaid_author.py` + `test_mermaid_trace_index.py`)
file-for-file.

This is Wave 0 in Nyquist terms — tests exist before the operator they verify.
The integration tests (`test_mockup_trace_index.py`) pass immediately because they
consume the existing `trace write --kind mockup` + `index update` CLI as-is (zero
ba_tools changes). The workflow-inspection tests in `test_mockup_author.py` assert
against `ba-mockup.md`, which Plan 02 creates — those go green once Plan 02 lands.

Purpose: Make the three success criteria measurable before authoring the operator.
Output: 3 fixtures + 2 test files under `.agents/ba-daily-operators/ba-tools/tests/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ba-mockup-operator/04-RESEARCH.md
@.planning/phases/04-ba-mockup-operator/04-PATTERNS.md
@.planning/phases/04-ba-mockup-operator/04-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates (NEW):
- `tests/fixtures/mockup/mockup_requirements.json` — requirements fixture (FR-001/FR-002)
- `tests/fixtures/mockup/authored_html.html` — html-fidelity artifact fixture
- `tests/fixtures/mockup/authored_wireframe.md` — wireframe-fidelity artifact fixture
- `tests/test_mockup_author.py` — MOCK-01/MOCK-03 fidelity-branch + workflow-inspection tests
- `tests/test_mockup_trace_index.py` — MOCK-02 + criterion-3 integration tests

(All paths are under `.agents/ba-daily-operators/ba-tools/`. NO new ba-tools symbols
— this phase adds zero ba_tools code. NO ba_tools/ file is touched by this plan.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the three mockup test fixtures</name>
  <files>
    .agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/mockup_requirements.json
    .agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/authored_html.html
    .agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/authored_wireframe.md
  </files>
  <read_first>
    - .agents/ba-daily-operators/ba-tools/tests/fixtures/mermaid/index_requirements.json (copy VERBATIM → mockup_requirements.json; schema + FR-001/FR-002 are kind-agnostic — index update reads it regardless of trace kind)
    - .agents/ba-daily-operators/ba-tools/tests/fixtures/mermaid/authored_diagram.md (analog for the req_ids-frontmatter shape the wireframe fixture mirrors)
    - .planning/phases/04-ba-mockup-operator/04-PATTERNS.md (§ "tests/fixtures/mockup/authored_html.html" and "authored_wireframe.md" — the minimal valid skeletons to reproduce)
    - .planning/phases/04-ba-mockup-operator/04-RESEARCH.md (§1 HTML Scaffold Conventions, §2 Wireframe conventions — the canonical shapes)
  </read_first>
  <action>
    Create the directory `.agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/` and write three fixtures.

    (1) `mockup_requirements.json`: copy `tests/fixtures/mermaid/index_requirements.json` byte-for-byte. It already defines FR-001 and FR-002 with `status: stated` and a `source_trace` block; it is kind-agnostic.

    (2) `authored_html.html` (html-fidelity D-03 fixture): line 1 MUST be the literal HTML comment carrying req_ids `[FR-001, FR-002]` in the `<!-- req_ids: [...] -->` form; line 2 is the HTML5 doctype; include a single inline `<style>` block in `<head>`; use semantic `<header>`/`<main>`/`<footer>`; include a simple form (`<label>`, `<input>`, `<button>`). It MUST contain NO `<script>` tag and NO external `src=`/`href=` URL. Reproduce the minimal skeleton from PATTERNS.md § authored_html.html.

    (3) `authored_wireframe.md` (wireframe-fidelity D-04 fixture): YAML frontmatter at top with `req_ids: [FR-001, FR-002]`, `fidelity: wireframe`, `slug:`, `screen:`; body uses `#`/`##`/`###` headings, bullet lists, and at least one markdown table to describe layout regions. It MUST NOT contain ASCII box-drawing (no `+--` runs, no `─`/`│` characters). Reproduce the minimal skeleton from PATTERNS.md § authored_wireframe.md.

    Do NOT edit any file under `ba_tools/`. These are test inputs only.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -c "import json,pathlib; d=pathlib.Path('tests/fixtures/mockup'); assert json.loads((d/'mockup_requirements.json').read_text())['requirements'][0]['id']=='FR-001'; h=(d/'authored_html.html').read_text(encoding='utf-8'); assert h.splitlines()[0].strip().startswith('<!--') and 'req_ids' in h.splitlines()[0] and '<script' not in h.lower(); w=(d/'authored_wireframe.md').read_text(encoding='utf-8'); assert 'req_ids:' in w and '+--' not in w; print('fixtures-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/fixtures/mockup/mockup_requirements.json` is byte-identical to `tests/fixtures/mermaid/index_requirements.json` (diff is empty) and parses with `requirements[0].id == "FR-001"`.
    - Source assertion: line 1 of `authored_html.html` matches `<!--\s*req_ids:\s*\[([^\]]*)\]\s*-->` with a non-empty list; the file contains `<!DOCTYPE html>` and a `<style>` block and contains no `<script` substring (case-insensitive).
    - Source assertion: `authored_wireframe.md` has a YAML frontmatter block containing `req_ids:` with a non-empty inline list and contains no `+--` ASCII box-drawing run.
    - CLI output: the verify command above prints `fixtures-ok`.
  </acceptance_criteria>
  <done>Three fixtures exist; html fixture has the req_ids comment as line 1 + doctype + inline style + no script; wireframe fixture has frontmatter req_ids + no box-drawing; requirements fixture matches the mermaid analog.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create test_mockup_author.py (MOCK-01 + MOCK-03 fidelity-branch + workflow inspection)</name>
  <files>.agents/ba-daily-operators/ba-tools/tests/test_mockup_author.py</files>
  <read_first>
    - .agents/ba-daily-operators/ba-tools/tests/test_mermaid_author.py (the EXACT structural analog: path constants `_REPO_ROOT = Path(__file__).parent.parent.parent.parent.parent`, the frontmatter-scan loop, the `## Route:` section-slice loop)
    - .agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/authored_html.html (the fixture this file asserts against — created in Task 1)
    - .agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/authored_wireframe.md (the wireframe fixture asserted against)
    - .planning/phases/04-ba-mockup-operator/04-PATTERNS.md (§ "tests/test_mockup_author.py" — the six named tests and the path-constant changes)
    - .planning/phases/04-ba-mockup-operator/04-RESEARCH.md (§3 — the `_HTML_REQ_IDS_RE` and `_MD_REQ_IDS_RE` regexes that the assertions reference)
  </read_first>
  <action>
    Copy `test_mermaid_author.py` as the structural base and adapt path constants: keep `_REPO_ROOT` (5 parents = repo root), set `_FIXTURE_HTML = Path(__file__).parent / "fixtures" / "mockup" / "authored_html.html"`, `_FIXTURE_WF = Path(__file__).parent / "fixtures" / "mockup" / "authored_wireframe.md"`, and `_WORKFLOW_PATH = _REPO_ROOT / ".agents" / "ba-daily-operators" / "ba-core" / "workflows" / "ba-mockup.md"`.

    Implement six tests:
    (a) `test_html_artifact_has_req_ids_comment` — first line of `_FIXTURE_HTML` matches the html req_ids regex with a non-empty list (MOCK-02/MOCK-03 carrier).
    (b) `test_html_artifact_has_doctype` — `_FIXTURE_HTML` contains `<!DOCTYPE html>` (MOCK-03 html shape).
    (c) `test_wireframe_artifact_has_frontmatter` — `_FIXTURE_WF` YAML frontmatter has `req_ids:` non-empty inline list, reusing the frontmatter-scan loop from the analog (MOCK-03 wireframe shape).
    (d) `test_wireframe_has_no_ascii_box_drawing` — `_FIXTURE_WF` body contains no `+--` run (D-04).
    (e) `test_screen_route_invokes_no_render_cli` — slice the `## Route: screen` section of `_WORKFLOW_PATH` and assert it contains none of `render`, `mmdc`, `mermaid-render`, `drawio` (D-05 no-render).
    (f) `test_workflow_rejects_missing_fidelity` — assert `_WORKFLOW_PATH` text contains the words `fidelity`, `html`, and `wireframe` together in the workflow (D-05a fidelity gate text-presence).

    Tests (e) and (f) read `ba-mockup.md`, created in Plan 02. Guard the file-missing case so the test is RED (assertion failure or explicit skip-if-absent that flips to a hard assert once the file exists) rather than an import error — follow the analog's pattern. Do NOT mark these xfail permanently; they MUST go green after Plan 02.

    No fenced code in this prose — reproduce the literal regexes from the analog and RESEARCH §3.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest tests/test_mockup_author.py -x -q -k "req_ids_comment or has_doctype or frontmatter or box_drawing"</automated>
  </verify>
  <acceptance_criteria>
    - Test command: the four fixture-only tests (`test_html_artifact_has_req_ids_comment`, `test_html_artifact_has_doctype`, `test_wireframe_artifact_has_frontmatter`, `test_wireframe_has_no_ascii_box_drawing`) PASS against the Task-1 fixtures.
    - Behavior assertion: `test_screen_route_invokes_no_render_cli` and `test_workflow_rejects_missing_fidelity` are collected and currently RED/skip-pending (workflow not yet authored) — they will go green after Plan 02; they are NOT permanently xfailed.
    - Source assertion: `_WORKFLOW_PATH` resolves to `.agents/ba-daily-operators/ba-core/workflows/ba-mockup.md` (path-constant correctness).
  </acceptance_criteria>
  <done>The four fixture-based tests pass; the two workflow-inspection tests are wired to `ba-mockup.md` and pending Plan 02; path constants resolve correctly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create test_mockup_trace_index.py (MOCK-02 + criterion-3 orphan, integration)</name>
  <files>.agents/ba-daily-operators/ba-tools/tests/test_mockup_trace_index.py</files>
  <read_first>
    - .agents/ba-daily-operators/ba-tools/tests/test_mermaid_trace_index.py (the EXACT analog: imports/constants block, `_MATRIX_HEADER`, `_ORPHANS_SECTION`, `_run`/`_read_index`/`_orphans_body`/`_matrix_rows` helpers, the `trace_index_env` fixture, `_write_srs_trace`, `_write_mermaid_trace`, the three test classes + `TestIndexMdStructure`)
    - .agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/mockup_requirements.json (the requirements fixture this test copies into the tmp SRS dir — created in Task 1)
    - .planning/phases/04-ba-mockup-operator/04-PATTERNS.md (§ "tests/test_mockup_trace_index.py" — the constant changes, the mockup artifact write, `_write_mockup_trace`, and the renamed test classes)
    - .agents/ba-daily-operators/ba-tools/ba_tools/commands/trace_cmd.py (CONSUMED AS-IS — confirm `--kind mockup` + `--req-ids` + `--source-doc` + `--requirements` flags; do NOT edit)
    - .agents/ba-daily-operators/ba-tools/ba_tools/commands/index_cmd.py (CONSUMED AS-IS — confirm Mockup column header + Orphans section; do NOT edit)
  </read_first>
  <action>
    Copy `test_mermaid_trace_index.py` and adapt: set `_FIXTURE_DIR = Path(__file__).parent / "fixtures" / "mockup"` and `_INDEX_REQS_FIXTURE = _FIXTURE_DIR / "mockup_requirements.json"`. Keep `_MATRIX_HEADER = "| REQ-ID | SRS § | Mermaid | Mockup | Story | Status |"` and `_ORPHANS_SECTION = "## Orphans"` unchanged (identical to the analog — verified against index_cmd.py).

    Copy the helper functions `_run`, `_read_index`, `_orphans_body`, `_matrix_rows` verbatim (they use `PYTHON = sys.executable` and invoke `ba_tools` via `-m`). In the `trace_index_env` fixture, replace the mermaid artifact with a mockup `.html`: create `.ba-ops/mockup/<slug>/screen.html` whose first line is the `<!-- req_ids: [FR-001] -->` comment followed by a minimal valid HTML body; return `mockup_artifact` in the env dict.

    Replace `_write_mermaid_trace` with `_write_mockup_trace` invoking exactly: `trace write --kind mockup --slug <slug> --artifact <mockup_artifact> --source-doc <reqs_file> --requirements <reqs_file> --req-ids <list>` (add `--force` when requested). `--source-doc` is `requirements.json`, NOT the mockup artifact — this pins `source_hash` to the SRS for drift detection (per ba-mermaid.md Step 2 note).

    Implement three test classes mirroring the analog, swapping `_write_mermaid_trace` → `_write_mockup_trace`:
    - `TestReqIdsAppearInIndexMockupColumn`: SRS trace → mockup trace `--req-ids FR-001` → `index update` → assert the FR-001 matrix row has `ok` status and FR-001 is NOT in `## Orphans`.
    - `TestNoOrphansForRealIds`: mockup trace `FR-001,FR-002` → `index update` → assert `## Orphans` body is `(none)`.
    - `TestInventedIdSurfacesAsOrphan` / `test_invented_id_surfaces_as_orphan`: mockup trace `FR-001,FR-999` → `index update` → assert `## Orphans` contains `FR-999`, is NOT `(none)`, and does NOT contain `FR-001` (D-06 / criterion 3).

    Copy `TestIndexMdStructure` verbatim (it validates `_MATRIX_HEADER`/`_ORPHANS_SECTION` against the rendered INDEX — the same constants apply to mockup). These tests run the real CLI in `tmp_path`; no mocking, no ba_tools edits.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest tests/test_mockup_trace_index.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `pytest tests/test_mockup_trace_index.py -x` passes ALL tests (these consume the existing CLI as-is, so they are green on creation).
    - Test command: `pytest tests/test_mockup_trace_index.py::TestReqIdsAppearInIndexMockupColumn -x` passes — FR-001 reaches the INDEX Mockup column with `ok` and no orphan.
    - Test command: `pytest tests/test_mockup_trace_index.py::test_invented_id_surfaces_as_orphan -x` passes — FR-999 surfaces under `## Orphans`, FR-001 does not (D-06).
    - Behavior assertion: `git status --porcelain ba_tools/` is empty — no ba_tools file changed.
  </acceptance_criteria>
  <done>All trace/index integration tests pass against the unchanged CLI; FR-001 reaches the Mockup column; FR-999 surfaces as orphan; FR-001 does not; zero ba_tools edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → CLI subprocess | tests invoke `ba-tools` over a `tmp_path` repo root; no untrusted external input reaches production code |
| fixture file → repo | committed static fixtures; no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering (V5 Input) | `--fidelity` argument | accept (this plan) | Fidelity enforcement is workflow-level (Plan 02). This plan's `test_workflow_rejects_missing_fidelity` proves the gate text exists; no ba-tools flag added. |
| T-4-02 | Spoofing | agent-supplied `req_ids` | mitigate | `test_invented_id_surfaces_as_orphan` proves an invented FR-999 surfaces under INDEX `## Orphans` via the existing `index update` drift detection (D-06). Verification: the test asserts FR-999 in Orphans, FR-001 not. |
| T-4-03 | Tampering | path traversal in `--artifact`/`--source-doc` | accept | Existing `repo.py` `resolve_under_root`/`is_within_root` in `trace_cmd.py` guards all path inputs; consumed as-is, no new code. Verification: integration tests pass with paths under `tmp_path`. |
| T-4-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pytest already active since Phase 1; zero new dependencies. No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `pytest tests/test_mockup_trace_index.py -x` fully green (consumes existing CLI).
- The four fixture-only tests in `test_mockup_author.py` green; the two workflow-inspection tests collected and pending Plan 02.
- `git status --porcelain` shows NO change under `ba_tools/` — zero new ba-tools commands (hard constraint).
</verification>

<success_criteria>
- 3 fixtures + 2 test files exist under `.agents/ba-daily-operators/ba-tools/tests/`.
- Integration tests pass on creation; criterion-3 orphan test proves D-06.
- No file under `ba_tools/` modified.
</success_criteria>

<output>
Create `.planning/phases/04-ba-mockup-operator/04-01-SUMMARY.md` when done.
</output>
