---
phase: 04-ba-mockup-operator
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - .planning/phases/04-ba-mockup-operator/04-03-SUMMARY.md
autonomous: false
requirements: [MOCK-01, MOCK-02, MOCK-03]
user_setup: []

must_haves:
  truths:
    - "D-05a + MOCK-01 criterion-1: the full test suite is green, proving `--fidelity` is required (workflow rejects missing/invalid), html → `.html`, wireframe → inline `.md` blocks"
    - "D-02 + MOCK-02 criterion-2: a real-ID mockup trace reaches the INDEX.md mockup column with `ok` and no orphans, end-to-end through the unchanged `trace write` + `index update`"
    - "D-06 + MOCK-03 criterion-3: a mockup citing a non-existent REQ-ID surfaces under INDEX.md `## Orphans`"
    - "D-05: no render route exists and no synthetic-render token appears anywhere in the operator (DESIGN §11) — confirmed by the no-render-cli test plus a repo-wide grep over the new files"
    - "Zero ba-tools commands added this phase — `git diff --stat` shows no change under `ba_tools/` across the whole phase (hard constraint)"
  artifacts:
    - path: ".planning/phases/04-ba-mockup-operator/04-03-SUMMARY.md"
      provides: "phase verification record — full-suite result, criteria mapping, human-verify outcome"
      contains: "MOCK-01"
  key_links:
    - from: ".agents/ba-daily-operators/ba-core/workflows/ba-mockup.md"
      to: ".agents/ba-daily-operators/ba-tools/tests/test_mockup_author.py"
      via: "workflow-inspection tests now green (no-render-cli + fidelity-gate)"
      pattern: "Route: screen"
    - from: ".agents/ba-daily-operators/ba-tools/tests/test_mockup_trace_index.py"
      to: ".agents/ba-daily-operators/ba-tools/ba_tools/commands/index_cmd.py"
      via: "end-to-end INDEX mockup column + orphan, existing CLI"
      pattern: "index.*update"
---

<objective>
Prove the ba-mockup operator satisfies all three phase success criteria end-to-end
and confirm the hard constraints held (zero ba-tools changes, no synthetic render).
This is the Wave-2 integration gate: it runs after the test foundation (Plan 01)
and the operator (Plan 02) are both in place.

The three criteria are already pinned by automated tests (Plans 01/02). This plan
runs the FULL suite green, verifies the no-render and zero-ba-tools-change
constraints with explicit greps, and adds the one genuinely manual check that no
automated assertion can cover: does an actually-authored mockup read as a usable
screen (visual/structural quality), not merely as a syntactically valid file.

Purpose: Phase-level verification before `/gsd-verify-work`.
Output: 04-03-SUMMARY.md recording the verification result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ba-mockup-operator/04-VALIDATION.md
@.agents/ba-daily-operators/ba-core/workflows/ba-mockup.md
@.agents/ba-daily-operators/ba-core/agents/ba-mockup-author.md
</context>

<artifacts_this_phase_produces>
This plan creates (NEW):
- `.planning/phases/04-ba-mockup-operator/04-03-SUMMARY.md` — the phase verification record.

(No code artifacts. No new ba-tools symbols. NO ba_tools/ file is touched. This plan
only RUNS tests/greps already-built artifacts and records the outcome.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run the full suite + assert the no-render and zero-ba-tools-change hard constraints</name>
  <files>.planning/phases/04-ba-mockup-operator/04-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-ba-mockup-operator/04-VALIDATION.md (the per-criterion test map + sampling rate — the commands to run)
    - .agents/ba-daily-operators/ba-tools/tests/test_mockup_author.py (the six tests proving MOCK-01/MOCK-03 — created Plan 01)
    - .agents/ba-daily-operators/ba-tools/tests/test_mockup_trace_index.py (the integration tests proving MOCK-02 + criterion 3 — created Plan 01)
    - .agents/ba-daily-operators/ba-core/workflows/ba-mockup.md (the operator under test — created Plan 02; confirm no render route)
  </read_first>
  <action>
    Run the full pytest suite from the ba-tools package root and confirm it is entirely green, including the mockup pair. Then assert the two phase hard constraints with explicit checks:

    (1) No synthetic render anywhere in the new operator files: grep the four Plan-02 files (the workflow, the author prompt, the skill index, the skill yaml) for any of the render-invocation tokens `mmdc`, `mermaid-render`, `drawio`, `screenshot`, and a render route heading; the count of matches in instruction positions MUST be zero. The author prompt may NAME those tokens only inside an explicit prohibition rule, so filter prohibition lines with `grep -v` before counting render-invocation tokens; the no-render-cli test already enforces the screen-route slice.

    (2) Zero changes to the deterministic CLI package across the whole phase. Run `git diff --stat HEAD` scoped to the package source directory shown in the verify command below and require an empty result — no tracked change under the Python package source. The only additions under the ba-tools tree are test files + fixtures under the tests directory, never under the package source.

    <!-- planner-discipline-allow: ba_tools/ -->
    The exact path to scope the diff to is `.agents/ba-daily-operators/ba-tools/ba_tools/` (see the verify command).

    Record results in `04-03-SUMMARY.md`: full-suite pass count, the criterion→test mapping (MOCK-01/02/03 each to its passing test command), the two constraint-check outcomes, and any deviations. Do not edit any operator or CLI-package file in this task.
  </action>
  <verify>
    <automated>cd .agents/ba-daily-operators/ba-tools && "$(python -c 'import sys;print(sys.executable)')" -m pytest -q && git -C "$(git rev-parse --show-toplevel)" diff --stat HEAD -- '.agents/ba-daily-operators/ba-tools/ba_tools/' | grep -v '^#' | grep -c 'ba_tools/' | { read n; test "$n" -eq 0 && echo "no-ba_tools-change-ok"; }</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `pytest` (full suite) passes with zero failures, including `test_mockup_author.py` (all six tests green) and `test_mockup_trace_index.py` (all green).
    - Behavior assertion: `git diff --stat HEAD -- '.agents/ba-daily-operators/ba-tools/ba_tools/'` is empty — no `ba_tools/` package file changed this phase (zero new ba-tools commands).
    - Source assertion: grep over `ba-mockup.md` finds no `Route: render`, no `mmdc`, no `mermaid-render`, no `drawio` (D-05, DESIGN §11).
    - CLI output: the verify command prints `no-ba_tools-change-ok`.
  </acceptance_criteria>
  <done>Full suite green; no ba_tools/ file changed; no render route or synthetic-render token in the operator; results recorded in 04-03-SUMMARY.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — an authored mockup reads as a usable screen</name>
  <files>(no files — human verification checkpoint; no automated edits)</files>
  <action>
    Pause for human verification. Present the committed mockup fixtures (and, if an SRS
    slug exists, a freshly authored mockup) and the fidelity-gate behavior to the
    developer, then wait for the resume signal before advancing the phase. This is a
    blocking checkpoint — the executor performs no edits in this task; it only surfaces
    the artifacts and the verification steps below and halts on the resume signal.
  </action>
  <what-built>
    The ba-mockup operator (workflow + author prompt + skill) and its test foundation.
    Automated tests prove the artifacts are syntactically correct (req_ids carrier,
    doctype, frontmatter, no box-drawing, no render route, INDEX mockup column,
    orphan detection). What automation CANNOT prove: that a mockup the agent actually
    authors at runtime reads as a usable UI screen rather than a syntactically valid
    but content-empty stub.
  </what-built>
  <how-to-verify>
    1. From the repo root, open one of the committed fixtures to confirm the expected shape:
       - `.agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/authored_html.html` — open in a browser (it is self-contained; just double-click / open file://). Confirm it renders a recognizable screen (header, a form or content region, footer) with NO broken external assets and NO JavaScript.
       - `.agents/ba-daily-operators/ba-tools/tests/fixtures/mockup/authored_wireframe.md` — open in a Markdown preview. Confirm the headings/lists/tables read as a layout sketch, with no ASCII box-drawing.
    2. (Optional, recommended) Invoke the operator on a real SRS slug if one exists under `.ba-ops/srs/<slug>/`:
       run the ba-mockup `full` route with `--fidelity html` (and again with `--fidelity wireframe`), then open the produced `.ba-ops/mockup/<slug>/<screen>.html` / `.md`.
       Confirm: the first line carries `<!-- req_ids: [...] -->` (html) or frontmatter `req_ids:` (wireframe); the req_ids match real IDs in `requirements.json`; after the run, `.ba-ops/INDEX.md` shows those IDs in the Mockup column with no new orphans.
    3. Confirm invoking the workflow WITHOUT `--fidelity` (or with an invalid value) is rejected before any artifact is written.
  </how-to-verify>
  <resume-signal>Type "approved" if the mockups read as usable screens and the fidelity gate rejects missing/invalid fidelity, or describe the issues to fix.</resume-signal>
  <verify>
    <human-check>Developer confirms the html fixture renders a recognizable screen with no JS/broken assets, the wireframe fixture reads as a layout sketch with no box-drawing, and the workflow rejects missing/invalid `--fidelity`.</human-check>
  </verify>
  <done>Developer typed "approved" (or all reported issues were resolved and re-verified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification → repo | this plan only reads/greps and runs tests over committed artifacts; introduces no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering (V5 Input) | `--fidelity` gate | mitigate | Human-verify Step 3 confirms the workflow rejects missing/invalid fidelity at runtime (complements the Plan-01 text-assertion test). |
| T-4-04 | Tampering | `<script>` in `.html` | mitigate | Human-verify Step 1 confirms the rendered html has no JavaScript and no broken external assets (complements the no-`<script>` fixture assertion). |
| T-4-06 | Tampering | synthetic-render path slips into the operator | mitigate | Task 1 greps all four operator files for `mmdc`/`mermaid-render`/`drawio`/`Route: render` and requires zero render-invocation matches (DESIGN §11). |
| T-4-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase; zero new dependencies. No legitimacy checkpoint needed. |
</threat_model>

<verification>
- Full `pytest` suite green (all phases, including the mockup pair).
- `git diff --stat HEAD -- ba_tools/` empty — zero ba-tools changes.
- No render route / synthetic-render token in the operator (grep + the no-render-cli test).
- Human confirms an authored mockup reads as a usable screen and the fidelity gate rejects missing/invalid fidelity.
</verification>

<success_criteria>
- All three ROADMAP success criteria proven: fidelity-required + html→`.html`/wireframe→inline; req_ids→INDEX mockup column no-orphan; invented-ID orphan surfaced.
- Hard constraints confirmed: zero ba-tools commands; no synthetic render.
- 04-03-SUMMARY.md records the verification.
</success_criteria>

<output>
Create `.planning/phases/04-ba-mockup-operator/04-03-SUMMARY.md` when done.
</output>
